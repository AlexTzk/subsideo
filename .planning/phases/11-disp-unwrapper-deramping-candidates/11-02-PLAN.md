---
phase: 11-disp-unwrapper-deramping-candidates
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/subsideo/products/disp.py
  - run_eval_disp.py
  - run_eval_disp_egms.py
  - tests/validation/test_disp_candidates.py
autonomous: true
requirements: [DISP-08, DISP-09]
must_haves:
  truths:
    - "User can run SPURT native on SoCal and Bologna from cached CSLC stacks."
    - "SPURT candidate outputs are isolated from baseline native PHASS outputs."
    - "SPURT candidate comparisons continue to call `prepare_for_reference(method=REFERENCE_MULTILOOK_METHOD)` explicitly."
  artifacts:
    - path: "src/subsideo/products/disp.py"
      provides: "Validation-only unwrap-method override while preserving PHASS default"
      contains: "unwrap_method: Literal[\"phass\", \"spurt\"] = \"phass\""
    - path: "run_eval_disp.py"
      provides: "SoCal SPURT candidate run and outcome capture"
      contains: "spurt_native"
    - path: "run_eval_disp_egms.py"
      provides: "Bologna SPURT candidate run and outcome capture"
      contains: "spurt_native"
  key_links:
    - from: "run_eval_disp.py"
      to: "src/subsideo/products/disp.py"
      via: "run_disp(..., unwrap_method=\"spurt\")"
      pattern: "unwrap_method=\"spurt\""
    - from: "run_eval_disp.py"
      to: "src/subsideo/validation/compare_disp.py"
      via: "prepare_for_reference(method=REFERENCE_MULTILOOK_METHOD)"
      pattern: "method=REFERENCE_MULTILOOK_METHOD"
---

<objective>
Implement the SPURT native candidate as the required alternative unwrapper path.

Purpose: DISP-08 and D-01 through D-04 require SPURT native to run first on both validation cells without changing the production PHASS default.
Output: Product helper unwrap override plus SoCal/Bologna eval integration that records SPURT candidate outcomes.
</objective>

<execution_context>
@/Users/alex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-RESEARCH.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-PATTERNS.md
@src/subsideo/products/disp.py
@run_eval_disp.py
@run_eval_disp_egms.py
@src/subsideo/validation/compare_disp.py
@src/subsideo/validation/disp_candidates.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validation-only unwrap-method override</name>
  <files>src/subsideo/products/disp.py, tests/validation/test_disp_candidates.py</files>
  <read_first>
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
    src/subsideo/products/disp.py
    src/subsideo/validation/disp_candidates.py
  </read_first>
  <behavior>
    - Calling `run_disp(..., unwrap_method="phass")` uses `UnwrapMethod.PHASS`.
    - Calling `run_disp(..., unwrap_method="spurt")` uses `UnwrapMethod.SPURT`.
    - Calling `run_disp(..., unwrap_method="bad")` raises `ValueError("unwrap_method must be 'phass' or 'spurt'")`.
  </behavior>
  <action>
    Update `_run_dolphin_phase_linking` in `src/subsideo/products/disp.py` to accept `unwrap_method: Literal["phass", "spurt"] = "phass"` and map it exactly with `unwrap_method_enum = UnwrapMethod.SPURT if unwrap_method == "spurt" else UnwrapMethod.PHASS`. Pass `unwrap_method=unwrap_method_enum` into `UnwrapOptions`. Update `run_disp(...)` to accept the same `unwrap_method: Literal["phass", "spurt"] = "phass"` keyword after `era5_mode`, validate it with `if unwrap_method not in ("phass", "spurt"): raise ValueError("unwrap_method must be 'phass' or 'spurt'")`, and pass it to `_run_dolphin_phase_linking`. Preserve the default `"phass"` per D-06; do not change native production behavior for callers that do not pass the override.
  </action>
  <acceptance_criteria>
    `rg -n "unwrap_method: Literal\\[\"phass\", \"spurt\"\\] = \"phass\"" src/subsideo/products/disp.py` prints at least two lines.
    `rg -n "UnwrapMethod\\.SPURT|UnwrapMethod\\.PHASS" src/subsideo/products/disp.py` prints both enum names.
    `rg -n "unwrap_method must be 'phass' or 'spurt'" src/subsideo/products/disp.py` prints the exact validation message.
  </acceptance_criteria>
  <verify>
    <automated>micromamba run -n subsideo pytest tests/validation/test_disp_candidates.py -q</automated>
  </verify>
  <done>SPURT can be selected only through an explicit validation override and default PHASS remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire SPURT candidate runs into both DISP eval scripts</name>
  <files>run_eval_disp.py, run_eval_disp_egms.py, tests/validation/test_disp_candidates.py</files>
  <read_first>
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
    .planning/phases/10-disp-era5-ramp-diagnostics/10-04-SUMMARY.md
    run_eval_disp.py
    run_eval_disp_egms.py
    src/subsideo/products/disp.py
    src/subsideo/validation/compare_disp.py
    src/subsideo/validation/disp_candidates.py
    src/subsideo/validation/matrix_schema.py
  </read_first>
  <behavior>
    - SoCal writes a `DISPCandidateOutcome(candidate="spurt_native", cell="socal", ...)` into `eval-disp/metrics.json`.
    - Bologna writes a `DISPCandidateOutcome(candidate="spurt_native", cell="bologna", ...)` into `eval-disp-egms/metrics.json`.
    - Both scripts keep `REFERENCE_MULTILOOK_METHOD: Literal["block_mean"] = "block_mean"` and call `prepare_for_reference(..., method=REFERENCE_MULTILOOK_METHOD)`.
  </behavior>
  <action>
    In `run_eval_disp.py`, add a Stage 11 candidate block before the final `DISPCellMetrics(...)` construction that runs SPURT first per D-04 using `candidate_dir = candidate_output_dir(OUT_DIR, "spurt_native")` and `run_disp(sorted_h5, output_dir=candidate_dir, era5_mode="off", unwrap_method="spurt")`. Use `cell="socal"`. If `result.velocity_path` exists, compare that velocity against the existing OPERA reference through `prepare_for_reference(native_velocity=result.velocity_path, reference_grid=opera_da, method=REFERENCE_MULTILOOK_METHOD)`, compute correlation/bias/RMSE with the same existing helpers or local code path used for the baseline OPERA comparison, and build `DISPCandidateOutcome(candidate="spurt_native", cell="socal", status=candidate_status_from_metrics(...), cached_input_valid=True, partial_metrics=False, reference_correlation=..., reference_bias_mm_yr=..., reference_rmse_mm_yr=..., ramp_mean_magnitude_rad=..., ramp_direction_sigma_deg=..., attributed_source=...)`. If SPURT raises or produces no velocity, append `make_candidate_blocker(candidate="spurt_native", cell="socal", failed_stage="spurt_unwrap_or_timeseries", error_summary=f"{type(exc).__name__}: {exc}", evidence_paths=[str(candidate_dir / "candidate.log")], cached_input_valid=True, partial_metrics=False)`. Add the outcome to `candidate_outcomes=[...]` in `DISPCellMetrics`. Mirror the same implementation in `run_eval_disp_egms.py` with `OUT_DIR = eval-disp-egms`, `cell="bologna"`, and the existing EGMS `ReferenceGridSpec`/point comparison path. This implements D-01, D-02, and D-03 for the SPURT half of the four candidate-cell outcomes. Do not add ERA5-on variants; D-13 says ERA5 remains diagnostic only.
  </action>
  <acceptance_criteria>
    `rg -n "spurt_native|unwrap_method=\"spurt\"|candidate_output_dir|candidate_status_from_metrics|make_candidate_blocker" run_eval_disp.py run_eval_disp_egms.py` prints lines in both eval scripts.
    `rg -n "method=REFERENCE_MULTILOOK_METHOD" run_eval_disp.py run_eval_disp_egms.py` still prints the OPERA and EGMS comparison callsites.
    `rg -n "spurt.*era5|ERA5.*spurt" run_eval_disp.py run_eval_disp_egms.py` returns no required SPURT ERA5-on/off matrix.
  </acceptance_criteria>
  <verify>
    <automated>micromamba run -n subsideo python -m py_compile src/subsideo/products/disp.py run_eval_disp.py run_eval_disp_egms.py</automated>
    <automated>micromamba run -n subsideo pytest tests/validation/test_disp_candidates.py -q</automated>
  </verify>
  <done>Both eval scripts attempt SPURT first and preserve structured SPURT outcomes instead of terminal-only failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval script -> dolphin workflow | Local script passes candidate configuration into a heavy file-producing workflow. |
| candidate product -> reference adapter | Candidate velocity is compared to OPERA/EGMS through explicit method selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | `run_disp(..., unwrap_method=...)` | mitigate | Literal validation accepts only `"phass"` and `"spurt"`; default remains `"phass"`. |
| T-11-02-02 | Repudiation | SPURT failed runs | mitigate | Convert exceptions or missing `velocity.tif` into `DISPCandidateOutcome(status="BLOCKER")` with stage, error, evidence path, and cached-input validity. |
| T-11-02-03 | Tampering | reference comparison method | mitigate | Preserve explicit `prepare_for_reference(..., method=REFERENCE_MULTILOOK_METHOD)` for OPERA and EGMS candidate comparisons. |
</threat_model>

<verification>
Run `micromamba run -n subsideo python -m py_compile src/subsideo/products/disp.py run_eval_disp.py run_eval_disp_egms.py` and `micromamba run -n subsideo pytest tests/validation/test_disp_candidates.py -q`.
</verification>

<success_criteria>
SPURT native is a runnable validation candidate on both cells, with isolated outputs and schema-valid outcome capture.
</success_criteria>

<output>
After completion, create `.planning/phases/11-disp-unwrapper-deramping-candidates/11-02-SUMMARY.md`.
</output>
