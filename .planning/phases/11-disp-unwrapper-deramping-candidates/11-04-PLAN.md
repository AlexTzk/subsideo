---
phase: 11-disp-unwrapper-deramping-candidates
plan: 04
type: execute
wave: 4
depends_on: [11-02, 11-03]
files_modified:
  - src/subsideo/validation/matrix_writer.py
  - tests/reference_agreement/test_matrix_writer_disp.py
  - run_eval_disp.py
  - run_eval_disp_egms.py
  - eval-disp/metrics.json
  - eval-disp/meta.json
  - eval-disp-egms/metrics.json
  - eval-disp-egms/meta.json
  - results/matrix.md
  - CONCLUSIONS_DISP_N_AM.md
  - CONCLUSIONS_DISP_EU.md
autonomous: true
requirements: [DISP-07, DISP-08, DISP-09]
must_haves:
  truths:
    - "The four required candidate-cell outcomes exist: SPURT/SoCal, SPURT/Bologna, PHASS deramp/SoCal, and PHASS deramp/Bologna."
    - "Candidate status appears in sidecars, compact matrix hints, and conclusions without collapsing product-quality, reference-agreement, or ramp-attribution evidence."
    - "Generated sidecars validate as `DISPCellMetrics` and matrix generation remains manifest-driven."
  artifacts:
    - path: "eval-disp/metrics.json"
      provides: "SoCal candidate outcomes"
      contains: "candidate_outcomes"
    - path: "eval-disp-egms/metrics.json"
      provides: "Bologna candidate outcomes"
      contains: "candidate_outcomes"
    - path: "results/matrix.md"
      provides: "Compact Phase 11 candidate hints"
      contains: "cand="
    - path: "CONCLUSIONS_DISP_N_AM.md"
      provides: "SoCal Phase 11 candidate evidence section"
      contains: "Phase 11 unwrapper and deramping candidates"
    - path: "CONCLUSIONS_DISP_EU.md"
      provides: "Bologna Phase 11 candidate evidence section"
      contains: "Phase 11 unwrapper and deramping candidates"
  key_links:
    - from: "eval-disp/metrics.json"
      to: "src/subsideo/validation/matrix_writer.py"
      via: "DISPCellMetrics.candidate_outcomes compact renderer"
      pattern: "cand="
    - from: "CONCLUSIONS_DISP_N_AM.md"
      to: "eval-disp/metrics.json"
      via: "numbers copied from schema-valid sidecar"
      pattern: "spurt_native"
---

<objective>
Render and record Phase 11 candidate evidence after both candidate paths exist.

Purpose: D-09 through D-12 require PASS/FAIL/BLOCKER candidate evidence to be visible in canonical sidecars, conclusions, and compact matrix text.
Output: Matrix renderer/tests, live regenerated sidecars, regenerated matrix, and conclusion sections for both cells.
</objective>

<execution_context>
@/Users/alex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
@.planning/phases/10-disp-era5-ramp-diagnostics/10-04-SUMMARY.md
@src/subsideo/validation/matrix_writer.py
@tests/reference_agreement/test_matrix_writer_disp.py
@run_eval_disp.py
@run_eval_disp_egms.py
@CONCLUSIONS_DISP_N_AM.md
@CONCLUSIONS_DISP_EU.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render compact candidate hints in matrix cells</name>
  <files>src/subsideo/validation/matrix_writer.py, tests/reference_agreement/test_matrix_writer_disp.py</files>
  <read_first>
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
    src/subsideo/validation/matrix_writer.py
    src/subsideo/validation/matrix_schema.py
    tests/reference_agreement/test_matrix_writer_disp.py
  </read_first>
  <behavior>
    - `_render_disp_cell` appends `cand=spurt:PASS,deramp:FAIL` style text when candidate outcomes exist.
    - RA column continues to render only reference-agreement measurements.
    - Malformed DISP sidecars still return `None` and render as `RUN_FAILED` through existing fallback behavior.
  </behavior>
  <action>
    In `_render_disp_cell`, after ERA5/cause hint handling and before `pq_body = " / ".join(parts)`, append candidate summary text when `m.candidate_outcomes` is non-empty. Use exact short labels `spurt` for `candidate == "spurt_native"` and `deramp` for `candidate == "phass_post_deramp"`. Format as `cand=` followed by comma-joined `{label}:{status}` entries sorted in this order: `spurt_native`, then `phass_post_deramp`. If an outcome has `partial_metrics=True`, append `*` to its status, producing examples like `cand=spurt:BLOCKER*,deramp:FAIL`. Do not add candidate text to `ra_col`; D-12 requires product-quality, reference-agreement, and ramp-attribution evidence to remain separate. Extend `tests/reference_agreement/test_matrix_writer_disp.py` fixture factory to accept `candidate_outcomes` and assert candidate hints appear in `pq_col` while `ra_col` does not contain `cand=`.
  </action>
  <acceptance_criteria>
    `rg -n "cand=|spurt_native|phass_post_deramp|partial_metrics" src/subsideo/validation/matrix_writer.py tests/reference_agreement/test_matrix_writer_disp.py` prints renderer and test lines.
    `rg -n "ra_col.*cand=|cand=.*ra_col" src/subsideo/validation/matrix_writer.py` returns no lines.
    `rg -n "RUN_FAILED" tests/reference_agreement/test_matrix_writer_disp.py` still prints the malformed-sidecar test path.
  </acceptance_criteria>
  <verify>
    <automated>micromamba run -n subsideo pytest tests/reference_agreement/test_matrix_writer_disp.py -q</automated>
  </verify>
  <done>Matrix cells show compact candidate statuses without changing RA rendering semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Run both DISP candidate cells and regenerate matrix</name>
  <files>run_eval_disp.py, run_eval_disp_egms.py, eval-disp/metrics.json, eval-disp/meta.json, eval-disp-egms/metrics.json, eval-disp-egms/meta.json, results/matrix.md</files>
  <read_first>
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
    run_eval_disp.py
    run_eval_disp_egms.py
    src/subsideo/validation/matrix_schema.py
    src/subsideo/validation/matrix_writer.py
    Makefile
    results/matrix_manifest.yml
  </read_first>
  <action>
    Run `make eval-disp-nam`, then `make eval-disp-eu`, then `make results-matrix`. Validate `eval-disp/metrics.json` and `eval-disp-egms/metrics.json` by loading each with `DISPCellMetrics.model_validate_json`. Confirm the SoCal sidecar has exactly two `candidate_outcomes` with pairs `("spurt_native", "socal")` and `("phass_post_deramp", "socal")`. Confirm the Bologna sidecar has exactly two `candidate_outcomes` with pairs `("spurt_native", "bologna")` and `("phass_post_deramp", "bologna")`. If a candidate fails after partial work, keep the generated sidecar and require `status="BLOCKER"` plus the D-10 evidence fields instead of deleting the sidecar.
  </action>
  <acceptance_criteria>
    `micromamba run -n subsideo python -c "from pathlib import Path; from subsideo.validation.matrix_schema import DISPCellMetrics; m=DISPCellMetrics.model_validate_json(Path('eval-disp/metrics.json').read_text()); assert {(o.candidate,o.cell) for o in m.candidate_outcomes} == {('spurt_native','socal'),('phass_post_deramp','socal')}; print('socal ok')"` prints `socal ok`.
    `micromamba run -n subsideo python -c "from pathlib import Path; from subsideo.validation.matrix_schema import DISPCellMetrics; m=DISPCellMetrics.model_validate_json(Path('eval-disp-egms/metrics.json').read_text()); assert {(o.candidate,o.cell) for o in m.candidate_outcomes} == {('spurt_native','bologna'),('phass_post_deramp','bologna')}; print('bologna ok')"` prints `bologna ok`.
    `rg -n "cand=" results/matrix.md` prints at least one DISP matrix line.
  </acceptance_criteria>
  <verify>
    <automated>make eval-disp-nam</automated>
    <automated>make eval-disp-eu</automated>
    <automated>make results-matrix</automated>
  </verify>
  <done>Both live sidecars contain the full four-outcome Phase 11 candidate matrix and `results/matrix.md` reflects compact candidate hints.</done>
</task>

<task type="auto">
  <name>Task 3: Append Phase 11 candidate conclusion sections</name>
  <files>CONCLUSIONS_DISP_N_AM.md, CONCLUSIONS_DISP_EU.md</files>
  <read_first>
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
    .planning/phases/10-disp-era5-ramp-diagnostics/10-04-SUMMARY.md
    eval-disp/metrics.json
    eval-disp-egms/metrics.json
    CONCLUSIONS_DISP_N_AM.md
    CONCLUSIONS_DISP_EU.md
  </read_first>
  <action>
    Append `## Phase 11 unwrapper and deramping candidates` to both conclusion files. In `CONCLUSIONS_DISP_N_AM.md`, include a table with rows for `spurt_native / socal` and `phass_post_deramp / socal` and columns `candidate`, `status`, `partial_metrics`, `r`, `bias_mm_yr`, `rmse_mm_yr`, `mean_ramp_rad`, `deformation_sanity_flagged`, and `blocker_or_note`. In `CONCLUSIONS_DISP_EU.md`, include the same table with `bologna`. Populate numbers from `eval-disp/metrics.json` and `eval-disp-egms/metrics.json`; use `—` only for fields that are `null` in the sidecar. Add a short paragraph in each file saying: `Phase 11 remains validation-only: native 5 x 10 m production output and explicit prepare_for_reference(method=REFERENCE_MULTILOOK_METHOD) comparison discipline are unchanged. Phase 12 consumes these candidate outcomes to choose production posture.` Do not choose PASS, production deramping, unwrapper switch, 20 x 20 m fallback, or deferment posture in Phase 11.
  </action>
  <acceptance_criteria>
    `rg -n "Phase 11 unwrapper and deramping candidates|spurt_native|phass_post_deramp|prepare_for_reference\\(method=REFERENCE_MULTILOOK_METHOD\\)" CONCLUSIONS_DISP_N_AM.md CONCLUSIONS_DISP_EU.md` prints all required strings in both files.
    `rg -n "Phase 12 consumes these candidate outcomes to choose production posture" CONCLUSIONS_DISP_N_AM.md CONCLUSIONS_DISP_EU.md` prints two lines.
    `rg -n "production posture: PASS|switch unwrapper|20 x 20 m fallback" CONCLUSIONS_DISP_N_AM.md CONCLUSIONS_DISP_EU.md` returns no Phase 11 recommendation text.
  </acceptance_criteria>
  <verify>
    <automated>micromamba run -n subsideo pytest tests/reference_agreement/test_matrix_writer_disp.py tests/validation/test_disp_candidates.py -q</automated>
  </verify>
  <done>Conclusions report Phase 11 candidate evidence separately from final Phase 12 production-posture selection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated sidecars -> matrix | Machine-generated JSON controls release-facing matrix text. |
| generated sidecars -> conclusions | Human-facing conclusions copy measured candidate evidence from sidecars. |
| shell commands -> local cache | `make eval-disp-*` reads cached input stacks and writes local artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | matrix rendering | mitigate | Parse with `DISPCellMetrics.model_validate_json`; malformed sidecars keep existing `RUN_FAILED` fallback. |
| T-11-04-02 | Repudiation | conclusion numbers | mitigate | Populate candidate tables from schema-valid `metrics.json` sidecars, not terminal logs or matrix markdown. |
| T-11-04-03 | Denial of Service | live eval commands | accept | Existing supervisor wraps `make eval-disp-nam` and `make eval-disp-eu` with `EXPECTED_WALL_S`; long scientific runs are expected. |
| T-11-04-04 | Tampering | candidate status visibility | mitigate | Require `cand=` grep in `results/matrix.md` and exact candidate pair validation in both sidecars. |
</threat_model>

<verification>
Run the focused matrix/candidate tests, `make eval-disp-nam`, `make eval-disp-eu`, and `make results-matrix`.
</verification>

<success_criteria>
The phase produces canonical, schema-valid evidence for SPURT and PHASS post-deramping on both cells, with compact matrix visibility and conclusion sections ready for Phase 12.
</success_criteria>

<output>
After completion, create `.planning/phases/11-disp-unwrapper-deramping-candidates/11-04-SUMMARY.md`.
</output>
