---
phase: 11-disp-unwrapper-deramping-candidates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/subsideo/validation/matrix_schema.py
  - src/subsideo/validation/disp_candidates.py
  - tests/validation/test_disp_candidates.py
autonomous: true
requirements: [DISP-07, DISP-08, DISP-09]
must_haves:
  truths:
    - "Every Phase 11 candidate-cell outcome can be represented as PASS, FAIL, or BLOCKER."
    - "BLOCKER outcomes preserve failed stage, error summary, evidence paths, cached-input validity, and partial-metrics state."
    - "Candidate evidence is additive and does not replace product_quality, reference_agreement, or ramp_attribution."
  artifacts:
    - path: "src/subsideo/validation/matrix_schema.py"
      provides: "Strict Pydantic candidate evidence models on DISPCellMetrics"
      contains: "candidate_outcomes"
    - path: "src/subsideo/validation/disp_candidates.py"
      provides: "Shared candidate status, blocker, and output-directory helpers"
      exports: ["candidate_output_dir", "make_candidate_blocker", "candidate_status_from_metrics"]
    - path: "tests/validation/test_disp_candidates.py"
      provides: "Schema/helper regression tests"
  key_links:
    - from: "src/subsideo/validation/matrix_schema.py"
      to: "src/subsideo/validation/matrix_writer.py"
      via: "DISPCellMetrics.model_validate_json"
      pattern: "candidate_outcomes"
    - from: "run_eval_disp.py"
      to: "src/subsideo/validation/disp_candidates.py"
      via: "later plans import helper contracts"
      pattern: "candidate_status_from_metrics"
---

<objective>
Create the Phase 11 candidate evidence contract before any candidate runner writes sidecars.

Purpose: SPURT native and PHASS post-deramping need identical, schema-valid evidence for SoCal and Bologna, including structured blockers and partial metrics.
Output: Additive Pydantic models, shared helper functions, and tests that downstream plans implement against.
</objective>

<execution_context>
@/Users/alex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-RESEARCH.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-PATTERNS.md
@src/subsideo/validation/matrix_schema.py
@src/subsideo/validation/matrix_writer.py

<interfaces>
Existing DISP schema fields:
```python
DISPCellStatus = Literal["PASS", "FAIL", "CALIBRATING", "MIXED", "BLOCKER"]

class DISPCellMetrics(MetricsJson):
    product_quality: DISPProductQualityResultJson
    reference_agreement: ReferenceAgreementResultJson
    ramp_attribution: RampAttribution
    cell_status: DISPCellStatus
    era5_diagnostic: Era5Diagnostic | None = None
    cause_assessment: CauseAssessment | None = None
    terrain_diagnostics: TerrainDiagnostics | None = None
    orbit_provenance: list[OrbitCoverageDiagnostic] = Field(default_factory=list)
    dem_diagnostics: DemDiagnostics | None = None
    cache_provenance: list[CacheProvenance] = Field(default_factory=list)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict candidate evidence models</name>
  <files>src/subsideo/validation/matrix_schema.py, tests/validation/test_disp_candidates.py</files>
  <read_first>
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-PATTERNS.md
    src/subsideo/validation/matrix_schema.py
    tests/reference_agreement/test_matrix_writer_disp.py
  </read_first>
  <behavior>
    - `DISPCandidateOutcome(candidate="spurt_native", cell="socal", status="PASS", cached_input_valid=True)` serializes and validates inside `DISPCellMetrics.candidate_outcomes`.
    - Extra fields on `DISPCandidateOutcome` raise Pydantic validation errors because `ConfigDict(extra="forbid")` is active.
    - A `BLOCKER` outcome can store `failed_stage`, `error_summary`, `evidence_paths`, `cached_input_valid`, and `partial_metrics=True`.
  </behavior>
  <action>
    In `src/subsideo/validation/matrix_schema.py`, add these exact type aliases near the existing DISP literal namespace: `DISPCandidateName = Literal["spurt_native", "phass_post_deramp"]`, `DISPCandidateCell = Literal["socal", "bologna"]`, and `DISPCandidateStatus = Literal["PASS", "FAIL", "BLOCKER"]`. Add `DISPDeformationSanityCheck(BaseModel)` with `model_config = ConfigDict(extra="forbid")`, fields `cell: DISPCandidateCell`, `trend_delta_mm_yr: float | None = None`, `direction_change_deg: float | None = None`, `stable_residual_delta_mm_yr: float | None = None`, `flagged: bool = False`, and `flag_reason: str = ""`. Add `DISPCandidateOutcome(BaseModel)` with `model_config = ConfigDict(extra="forbid")`, fields `candidate`, `cell`, `status`, `failed_stage: str | None = None`, `error_summary: str | None = None`, `evidence_paths: list[str] = Field(default_factory=list)`, `cached_input_valid: bool`, `partial_metrics: bool = False`, `product_quality_status: str | None = None`, `reference_correlation: float | None = None`, `reference_bias_mm_yr: float | None = None`, `reference_rmse_mm_yr: float | None = None`, `ramp_mean_magnitude_rad: float | None = None`, `ramp_direction_sigma_deg: float | None = None`, `attributed_source: AttributedSource | None = None`, and `deformation_sanity: DISPDeformationSanityCheck | None = None`. Add `candidate_outcomes: list[DISPCandidateOutcome] = Field(default_factory=list, description="Phase 11 SPURT/native and PHASS post-deramping candidate-cell evidence.")` to `DISPCellMetrics` without removing any existing fields. Per D-09 through D-12, do not collapse product-quality, reference-agreement, or ramp-attribution into candidate status.
  </action>
  <acceptance_criteria>
    `rg -n "DISPCandidateName|DISPCandidateOutcome|DISPDeformationSanityCheck|candidate_outcomes" src/subsideo/validation/matrix_schema.py` prints all four names.
    `rg -n "extra=\"forbid\"" src/subsideo/validation/matrix_schema.py | wc -l` is greater than it was before this task.
    `rg -n "product_quality: DISPProductQualityResultJson|reference_agreement: ReferenceAgreementResultJson|ramp_attribution: RampAttribution" src/subsideo/validation/matrix_schema.py` still prints all three existing fields.
  </acceptance_criteria>
  <verify>
    <automated>micromamba run -n subsideo pytest tests/validation/test_disp_candidates.py -q</automated>
  </verify>
  <done>`DISPCellMetrics` validates legacy sidecars with no candidate outcomes and validates new sidecars with SPURT/PHASS candidate outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared candidate helper functions</name>
  <files>src/subsideo/validation/disp_candidates.py, tests/validation/test_disp_candidates.py</files>
  <read_first>
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-RESEARCH.md
    src/subsideo/validation/matrix_schema.py
    run_eval_disp.py
    run_eval_disp_egms.py
  </read_first>
  <behavior>
    - `candidate_output_dir(Path("eval-disp"), "spurt_native")` returns `Path("eval-disp/candidates/spurt_native")`.
    - `candidate_status_from_metrics("spurt_native", correlation=0.71, bias_mm_yr=2.0, ramp_mean_magnitude_rad=4.9, attributed_source="inconclusive")` returns `"PASS"`.
    - `candidate_status_from_metrics("phass_post_deramp", correlation=0.49, bias_mm_yr=2.0, ramp_mean_magnitude_rad=0.9, attributed_source="inconclusive")` returns `"PASS"`.
    - `make_candidate_blocker(...)` returns `DISPCandidateOutcome(status="BLOCKER", partial_metrics=True)` when `partial_metrics=True`.
  </behavior>
  <action>
    Create `src/subsideo/validation/disp_candidates.py` with `from __future__ import annotations`, `Path` import, and imports for `AttributedSource`, `DISPCandidateCell`, `DISPCandidateName`, `DISPCandidateOutcome`, and `DISPCandidateStatus` from `subsideo.validation.matrix_schema`. Implement `candidate_output_dir(base_dir: Path, candidate: DISPCandidateName) -> Path` returning `base_dir / "candidates" / candidate`. Implement `candidate_status_from_metrics(candidate: DISPCandidateName, *, correlation: float | None, bias_mm_yr: float | None, ramp_mean_magnitude_rad: float | None, attributed_source: AttributedSource | None) -> DISPCandidateStatus` with exact rules: return `"BLOCKER"` only when `correlation is None and ramp_mean_magnitude_rad is None`; for `candidate == "spurt_native"`, return `"PASS"` when `correlation >= 0.7 and ramp_mean_magnitude_rad < 5.0 and attributed_source == "inconclusive"`, else `"FAIL"`; for `candidate == "phass_post_deramp"`, return `"PASS"` when `(correlation >= 0.5) or (ramp_mean_magnitude_rad < 1.0)`, else `"FAIL"`. Implement `make_candidate_blocker(candidate, cell, failed_stage, error_summary, evidence_paths, cached_input_valid, partial_metrics=False) -> DISPCandidateOutcome` that sets `status="BLOCKER"` and preserves all evidence fields. These thresholds are from `DISP_UNWRAPPER_SELECTION_BRIEF.md` and D-01 through D-14; do not add ERA5-on/off as a candidate axis.
  </action>
  <acceptance_criteria>
    `rg -n "def candidate_output_dir|def candidate_status_from_metrics|def make_candidate_blocker" src/subsideo/validation/disp_candidates.py` prints all three functions.
    `rg -n "0\\.7|5\\.0|0\\.5|1\\.0" src/subsideo/validation/disp_candidates.py` prints the SPURT and PHASS candidate thresholds.
    `rg -n "era5" src/subsideo/validation/disp_candidates.py` returns no lines.
  </acceptance_criteria>
  <verify>
    <automated>micromamba run -n subsideo pytest tests/validation/test_disp_candidates.py -q</automated>
  </verify>
  <done>Candidate helpers produce deterministic PASS/FAIL/BLOCKER outcomes and candidate-specific output paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval script -> metrics sidecar | Local scientific output becomes matrix input. |
| candidate output directory -> baseline eval directory | Candidate artifacts must not overwrite canonical `eval-disp` / `eval-disp-egms` sidecars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | `DISPCellMetrics.candidate_outcomes` | mitigate | Use Pydantic `ConfigDict(extra="forbid")` and Literal enums for candidate, cell, and status. |
| T-11-01-02 | Repudiation | candidate blockers | mitigate | Require `failed_stage`, `error_summary`, `evidence_paths`, `cached_input_valid`, and `partial_metrics` fields in `DISPCandidateOutcome`. |
| T-11-01-03 | Tampering | candidate output routing | mitigate | `candidate_output_dir` must return `base_dir / "candidates" / candidate`, never the baseline directory itself. |
</threat_model>

<verification>
Run `micromamba run -n subsideo pytest tests/validation/test_disp_candidates.py -q`.
</verification>

<success_criteria>
The codebase has a strict, additive candidate evidence contract that can represent all four required candidate-cell outcomes from D-01 through D-03.
</success_criteria>

<output>
After completion, create `.planning/phases/11-disp-unwrapper-deramping-candidates/11-01-SUMMARY.md`.
</output>
