---
phase: 11-disp-unwrapper-deramping-candidates
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/subsideo/validation/selfconsistency.py
  - src/subsideo/products/disp.py
  - run_eval_disp.py
  - run_eval_disp_egms.py
  - tests/product_quality/test_selfconsistency_ramp.py
  - tests/validation/test_disp_candidates.py
autonomous: true
requirements: [DISP-07, DISP-09]
must_haves:
  truths:
    - "PHASS post-deramping subtracts fitted planar ramps from per-IFG unwrapped phases before a candidate time-series comparison is accepted."
    - "PHASS deramping is validation-only and does not change native production output."
    - "SoCal PHASS deramping records a deformation-signal sanity check that can block a Phase 12 production recommendation."
  artifacts:
    - path: "src/subsideo/validation/selfconsistency.py"
      provides: "Reusable deramped-IFG writer based on existing `fit_planar_ramp` coefficients"
      contains: "write_deramped_unwrapped_ifgs"
    - path: "run_eval_disp.py"
      provides: "SoCal PHASS post-deramp candidate outcome"
      contains: "phass_post_deramp"
    - path: "run_eval_disp_egms.py"
      provides: "Bologna PHASS post-deramp candidate outcome"
      contains: "phass_post_deramp"
  key_links:
    - from: "run_eval_disp.py"
      to: "src/subsideo/validation/selfconsistency.py"
      via: "write_deramped_unwrapped_ifgs before candidate comparison"
      pattern: "write_deramped_unwrapped_ifgs"
    - from: "run_eval_disp.py"
      to: "src/subsideo/validation/matrix_schema.py"
      via: "DISPDeformationSanityCheck in candidate outcome"
      pattern: "DISPDeformationSanityCheck"
---

<objective>
Implement PHASS post-deramping as a validation candidate with signal-preservation evidence.

Purpose: DISP-07 and D-05 require planar ramps to be subtracted from per-IFG unwrapped phases before time-series inversion or, if the toolchain cannot re-enter from deramped IFGs, to preserve a structured BLOCKER with partial deramp evidence.
Output: Deramped-IFG helper, candidate integration for both cells, and deformation-sanity evidence.
</objective>

<execution_context>
@/Users/alex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-RESEARCH.md
@.planning/phases/11-disp-unwrapper-deramping-candidates/11-PATTERNS.md
@src/subsideo/validation/selfconsistency.py
@src/subsideo/products/disp.py
@run_eval_disp.py
@run_eval_disp_egms.py
@tests/product_quality/test_selfconsistency_ramp.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deramped unwrapped-IFG writer</name>
  <files>src/subsideo/validation/selfconsistency.py, tests/product_quality/test_selfconsistency_ramp.py</files>
  <read_first>
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
    src/subsideo/validation/selfconsistency.py
    tests/product_quality/test_selfconsistency_ramp.py
  </read_first>
  <behavior>
    - A synthetic stack `plane + residual` is deramped so residual fitted slopes are approximately zero.
    - Non-planar residual values remain finite after deramping.
    - Invalid 2-D input to the array-level deramp helper raises `ValueError("ifgrams_stack must be 3-D")` through the existing `fit_planar_ramp` path.
  </behavior>
  <action>
    Add `deramp_ifg_stack(ifgrams_stack: np.ndarray, mask: np.ndarray | None = None) -> tuple[np.ndarray, dict[str, np.ndarray]]` to `src/subsideo/validation/selfconsistency.py`. It must call existing `fit_planar_ramp(ifgrams_stack, mask=mask)`, build `yy, xx = np.indices((height, width), dtype=np.float64)`, subtract `slope_x[idx] * xx + slope_y[idx] * yy + intercept_rad[idx]` for each IFG, preserve `np.nan` where the source phase is not finite, and return `(deramped_stack, ramp_data)`. Add `write_deramped_unwrapped_ifgs(unwrapped_paths: list[Path], output_dir: Path, mask: np.ndarray | None = None) -> tuple[list[Path], dict[str, np.ndarray]]` that reads each GeoTIFF band with rasterio, calls `deramp_ifg_stack`, writes one GeoTIFF per source path into `output_dir` using the source profile, names outputs `{source_stem}.deramped.tif`, and returns written paths plus ramp data. This implements D-05; do not add final-velocity-raster deramping as the main candidate.
  </action>
  <acceptance_criteria>
    `rg -n "def deramp_ifg_stack|def write_deramped_unwrapped_ifgs|fit_planar_ramp\\(" src/subsideo/validation/selfconsistency.py` prints the new helpers and a call to `fit_planar_ramp`.
    `rg -n "velocity\\.tif.*deramp|deramp.*velocity\\.tif" src/subsideo/validation/selfconsistency.py` returns no lines.
    `rg -n "deramped\\.tif|deramp_ifg_stack" tests/product_quality/test_selfconsistency_ramp.py` prints deramping tests.
  </acceptance_criteria>
  <verify>
    <automated>micromamba run -n subsideo pytest tests/product_quality/test_selfconsistency_ramp.py -q</automated>
  </verify>
  <done>Deramping happens on unwrapped IFG arrays/GeoTIFFs and is covered by synthetic plane-removal tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire PHASS post-deramp candidate outcomes into both eval scripts</name>
  <files>run_eval_disp.py, run_eval_disp_egms.py, src/subsideo/products/disp.py, tests/validation/test_disp_candidates.py</files>
  <read_first>
    .planning/phases/11-disp-unwrapper-deramping-candidates/11-CONTEXT.md
    .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md
    run_eval_disp.py
    run_eval_disp_egms.py
    src/subsideo/products/disp.py
    src/subsideo/validation/selfconsistency.py
    src/subsideo/validation/disp_candidates.py
    src/subsideo/validation/matrix_schema.py
  </read_first>
  <behavior>
    - SoCal writes `DISPCandidateOutcome(candidate="phass_post_deramp", cell="socal", deformation_sanity=DISPDeformationSanityCheck(...))`.
    - Bologna writes `DISPCandidateOutcome(candidate="phass_post_deramp", cell="bologna", ...)`.
    - If no valid time-series re-entry from deramped IFGs exists, the candidate outcome is `BLOCKER`, `failed_stage="deramped_ifg_timeseries_reentry"`, and `partial_metrics=True`.
  </behavior>
  <action>
    Add a PHASS post-deramp candidate block after the SPURT block in both eval scripts, preserving D-04 order. Use `candidate_dir = candidate_output_dir(OUT_DIR, "phass_post_deramp")`, read existing PHASS unwrapped paths from `OUT / "disp" / "dolphin" / "unwrapped"` or the currently used baseline `disp_dir / "dolphin" / "unwrapped"` path in each script, and call `write_deramped_unwrapped_ifgs(unw_files, candidate_dir / "deramped_unwrapped")`. Implement re-entry only through an explicit validation-only product helper parameter named `deramped_unwrapped_paths: list[Path] | None = None`; the parameter must default to `None`, must not be used by default production calls, and must not call private dolphin APIs whose names start with `_`. If that public/local hook produces `candidate_dir / "dolphin" / "timeseries" / "velocity.tif"`, compare it through the same explicit OPERA/EGMS `prepare_for_reference(..., method=REFERENCE_MULTILOOK_METHOD)` discipline and append a `DISPCandidateOutcome(candidate="phass_post_deramp", cell=..., status=candidate_status_from_metrics(...), cached_input_valid=True, partial_metrics=False, ...)`. If no public/local hook can consume deramped IFGs before time-series inversion, append a schema-valid `BLOCKER` with `failed_stage="deramped_ifg_timeseries_reentry"`, `error_summary` beginning with `"No supported deramped-IFG time-series re-entry:"`, `evidence_paths` including the deramped-unwrapped directory, `cached_input_valid=True`, and `partial_metrics=True`. For SoCal, always set `deformation_sanity=DISPDeformationSanityCheck(cell="socal", trend_delta_mm_yr=..., direction_change_deg=..., stable_residual_delta_mm_yr=..., flagged=..., flag_reason=...)`; set `flagged=True` when `abs(trend_delta_mm_yr) > 3.0` or `abs(stable_residual_delta_mm_yr) > 2.0`, otherwise `False`. For Bologna, include the same object when values are available; otherwise leave `deformation_sanity=None`. This implements D-01, D-02, D-03, D-05, D-07, D-08, and D-11 for the PHASS post-deramp half of the four candidate-cell outcomes.
  </action>
  <acceptance_criteria>
    `rg -n "phass_post_deramp|write_deramped_unwrapped_ifgs|deramped_ifg_timeseries_reentry|DISPDeformationSanityCheck" run_eval_disp.py run_eval_disp_egms.py` prints lines in both eval scripts.
    `rg -n "trend_delta_mm_yr|stable_residual_delta_mm_yr|3\\.0|2\\.0" run_eval_disp.py run_eval_disp_egms.py` prints the sanity-check thresholds.
    `rg -n "deramped_unwrapped_paths: list\\[Path\\] \\| None = None" src/subsideo/products/disp.py` prints the optional validation-only parameter if a product-helper re-entry hook is added.
  </acceptance_criteria>
  <verify>
    <automated>micromamba run -n subsideo python -m py_compile src/subsideo/validation/selfconsistency.py src/subsideo/products/disp.py run_eval_disp.py run_eval_disp_egms.py</automated>
    <automated>micromamba run -n subsideo pytest tests/product_quality/test_selfconsistency_ramp.py tests/validation/test_disp_candidates.py -q</automated>
  </verify>
  <done>Both cells attempt PHASS post-deramping before candidate comparison and record either comparable metrics or a structured partial BLOCKER.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline unwrapped IFGs -> deramped candidate IFGs | Local scientific raster artifacts are transformed and written as new candidate inputs. |
| deramped candidate -> production pipeline helper | Validation-only hook must not change default production DISP behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | deramped GeoTIFF writer | mitigate | Write to `candidate_dir / "deramped_unwrapped"` and preserve source profiles; never overwrite baseline unwrapped files. |
| T-11-03-02 | Repudiation | PHASS deramp partial failures | mitigate | Use `DISPCandidateOutcome(status="BLOCKER", failed_stage="deramped_ifg_timeseries_reentry", partial_metrics=True)` when no time-series re-entry exists. |
| T-11-03-03 | Information Disclosure | evidence paths in sidecars | accept | Paths are local workspace artifact paths without secrets; no credentials are written. |
| T-11-03-04 | Tampering | production default | mitigate | Any `deramped_unwrapped_paths` parameter defaults to `None`; default PHASS output remains unchanged per D-06. |
</threat_model>

<verification>
Run `micromamba run -n subsideo python -m py_compile src/subsideo/validation/selfconsistency.py src/subsideo/products/disp.py run_eval_disp.py run_eval_disp_egms.py` and focused pytest commands.
</verification>

<success_criteria>
PHASS post-deramping is implemented as an IFG-level validation candidate with structured metrics, blockers, and SoCal deformation-sanity evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/11-disp-unwrapper-deramping-candidates/11-03-SUMMARY.md`.
</output>
