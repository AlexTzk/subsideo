---
phase: 07-results-matrix-release-readiness
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - REL-04
  - REL-06

must_haves:
  truths:
    - "pytest passes on macOS M3 Max as the v1.1 final closure test"
    - "CHANGELOG.md has a [1.1.0] entry with the REL-04 TrueNAS deferral note"
    - "CHANGELOG.md v1.1 entry documents RTC:NAM DEFERRED, methodology doc additions, and REL-04 deferral"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1.1 release entry with REL-04 deferral note"
      contains: "[1.1.0]"
  key_links:
    - from: "CHANGELOG.md [1.1.0]"
      to: "results/matrix.md"
      via: "REL-01/REL-05 closure documented"
      pattern: "RTC.*DEFERRED"
---

<objective>
Run `pytest` as the v1.1 closure test and append the `[1.1.0]` CHANGELOG entry with the REL-04 TrueNAS deferral note. This plan depends on Plans 01 and 02 completing so the matrix and methodology doc are in final state before the closure test runs.

Purpose: REL-04 (deferred to v1.2, documented in CHANGELOG) + REL-06 (pytest closure test passes).

Output: CHANGELOG.md with [1.1.0] entry; pytest passing on macOS M3 Max.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-results-matrix-release-readiness/07-CONTEXT.md

<interfaces>
<!-- Existing CHANGELOG.md header and [0.1.0] entry pattern. -->
<!-- Executor must read the file before editing. -->

From CHANGELOG.md (lines 1-11):
```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [0.1.0] - 2026-04-09

Initial release. ...
```

New entry format:
```markdown
## [1.1.0] - 2026-04-28

### Added / Changed
...

### Deferred to v1.2
...
```

From 07-CONTEXT.md D-03 (exact REL-04 deferral language):
"Unblock: run `docker build` + `make eval-all` on TrueNAS Linux when homelab env is provisioned (v1.2)."
Infrastructure already committed (Phase 1 Plan 01-09): Dockerfile, Apptainer.def, env.lockfile.linux-64.txt, env.lockfile.osx-arm64.txt.

pytest command: `micromamba run -n subsideo pytest tests/unit/ -x -q`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pytest closure test</name>
  <files></files>
  <read_first>
    - CLAUDE.md (confirms micromamba env subsideo for Python commands)
  </read_first>
  <action>
Run the unit test suite as the v1.1 closure test. Use the unit marker to avoid integration/validation tests that require live credentials:

```bash
micromamba run -n subsideo pytest tests/unit/ -x -q --tb=short 2>&1 | tail -30
```

The test suite MUST exit 0. If any tests fail, read the failure output and fix them. Common Phase 7 failure mode: unit tests for matrix_writer.py that assert the old CALIBRATING string format without "binds". Fix these by updating expected strings to match the new `(CALIBRATING — binds v1.2)` format introduced in Plan 07-01.

After fixing any failures, re-run until `pytest tests/unit/ -x -q` exits 0.

Record the final pytest summary output for the SUMMARY.
  </action>
  <verify>
    <automated>micromamba run -n subsideo pytest tests/unit/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `micromamba run -n subsideo pytest tests/unit/ -x -q` exits 0
  </acceptance_criteria>
  <done>pytest exits 0. All unit tests pass on macOS M3 Max.</done>
</task>

<task type="auto">
  <name>Task 2: Append [1.1.0] entry to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (read the full file to see the existing [0.1.0] entry format before editing)
    - .planning/phases/07-results-matrix-release-readiness/07-CONTEXT.md (D-03: REL-04 deferral language; D-01/D-02: RTC:NAM deferral note)
    - .planning/phases/07-results-matrix-release-readiness/07-PATTERNS.md (§CHANGELOG.md pattern section)
  </read_first>
  <action>
Insert the [1.1.0] entry BEFORE the existing [0.1.0] entry (changelog is reverse-chronological — newest first). Insert immediately after line 6 (the "The format is based on..." line) and before the blank line that precedes [0.1.0].

Exact content to insert:

```markdown

## [1.1.0] - 2026-04-28

v1.1 N.Am./EU Validation Parity & Scientific PASS milestone. Drives every
product (RTC, CSLC, DISP, DIST, DSWx) to an unambiguous PASS, FAIL-with-named-upgrade-path,
or DEFERRED-with-dated-unblock across both N.Am. and EU regions.

### Validation Results

- **RTC-S1 EU:** 3/5 bursts PASS across alpine/plain/arid/boreal/wildfire terrain regimes
- **RTC-S1 N.Am.:** DEFERRED — N.Am. eval script (run_eval.py) not migrated to v1.1 harness sidecars in this milestone (EU was the focus); unblock: v1.2 N.Am. harness migration
- **CSLC-S1 self-consistency:** 3/3 AOIs CALIBRATING (SoCal coh=0.887, Coso-Searles coh=0.804, Iberian coh=0.868); eligible for v1.2 binding (≥3 data points)
- **DISP-S1:** SoCal r=0.049 FAIL / Bologna r=0.336 FAIL (both attributed_source=inconclusive); DISP Unwrapper Selection brief scoped for v1.2
- **DIST-S1 N.Am.:** DEFERRED — operational `OPERA_L3_DIST-ALERT-S1_V1` not yet published in CMR; CMR auto-supersede probe active
- **DIST-S1 EU:** 0/3 events PASS (Aveiro/Evros/Culebra); 3 attributable causes documented; chained prior_dist_s1_product retry filed upstream
- **DSWx-S2 N.Am.:** F1=0.9252 PASS (Lake Tahoe T10SFH, July 2021)
- **DSWx-S2 EU:** F1=0.8165 FAIL — fit-set quality review (HLS→S2 L2A spectral transfer gap; EU recalibration deferred to v1.2)

### Added

- `results/matrix.md` — 10-cell validation matrix (5 products × 2 regions) with
  product-quality and reference-agreement in structurally separate columns; CALIBRATING cells
  italicised with `binds v1.2` milestone annotation; DEFERRED cells carry dated unblock conditions
- `docs/validation_methodology.md` — §6 (OPERA UTC-hour frame selection by
  `harness.select_opera_frame_by_utc_hour()`) and §7 (cross-sensor precision-first
  framing for DIST-S1 vs EFFIS); top-level TOC linking all 7 sections
- `validation/bootstrap.py` — block-bootstrap 95% CI for DIST-S1 per-event F1 (Phase 5)
- `validation/effis.py` — EFFIS REST API access + dual rasterise for DIST-S1 EU cross-validation (Phase 5)
- `products/dswx_thresholds.py` — typed DSWEThresholds frozen+slots dataclass with
  provenance metadata and EU/N.Am. region selector (Phase 6)
- Dockerfile, Apptainer.def, env.lockfile.linux-64.txt, env.lockfile.osx-arm64.txt —
  reproducibility recipe (Phase 1)

### Deferred to v1.2

- **REL-04 TrueNAS Linux audit:** Full `make eval-all` on freshly-cloned repo inside
  the homelab TrueNAS Linux dev container. Infrastructure already committed (Dockerfile,
  Apptainer.def, lockfiles). Unblock: provision TrueNAS Linux dev container and run
  `docker build -f Dockerfile .` + `make eval-all` (v1.2).
- **RTC-S1 N.Am. harness migration:** Migrate run_eval.py to validation.harness
  (bounds_for_burst + metrics.json + meta.json write) and execute N.Am. RTC re-run (v1.2).
- **DSWx-S2 EU recalibration:** HLS→S2 L2A spectral transfer gap diagnosed; dedicated
  scene-level BOA offset correction required before joint WIGT×AWGT×PSWT2_MNDWI grid
  search converges (v1.2).
- **DIST-S1 N.Am. quantitative comparison:** Awaiting operational `OPERA_L3_DIST-ALERT-S1_V1`
  publication in CMR (CMR auto-supersede probe active in run_eval_dist.py).
- **DISP-S1 unwrapper selection:** PHASS+deramping / SPURT / tophu-SNAPHU / 20×20 m
  fallback candidates scoped in DISP_UNWRAPPER_SELECTION_BRIEF.md (v1.2 milestone).

```

After inserting, verify the entry is correctly positioned (appears before [0.1.0]) and the file parses cleanly.
  </action>
  <verify>
    <automated>grep -n "\[1.1.0\]" CHANGELOG.md</automated>
    <automated>grep -n "\[0.1.0\]" CHANGELOG.md</automated>
    <automated>python3 -c "content = open('CHANGELOG.md').read(); idx11 = content.index('[1.1.0]'); idx01 = content.index('[0.1.0]'); assert idx11 < idx01, f'1.1.0 ({idx11}) must appear before 0.1.0 ({idx01})'; print('Ordering OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[1.1.0\]" CHANGELOG.md` returns 1
    - [1.1.0] entry appears BEFORE [0.1.0] entry (reverse-chronological order)
    - `grep -c "REL-04" CHANGELOG.md` returns at least 1 (deferral documented)
    - `grep -c "TrueNAS" CHANGELOG.md` returns at least 1
    - `grep -c "v1.2 N.Am. harness migration" CHANGELOG.md` returns at least 1 (RTC:NAM unblock note)
  </acceptance_criteria>
  <done>CHANGELOG.md has [1.1.0] entry (reverse-chronological, before [0.1.0]) with validation results summary, what was added, and REL-04 + other v1.2 deferral notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest test runner | Executes test code under micromamba env subsideo; tests are local, no external network calls in unit tests |
| CHANGELOG.md (append) | Documentation file; no execution path; no credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | pytest output | accept | Unit tests run against local fixtures; no credentials or PII in test output. pytest runs under micromamba env subsideo (isolated env). |
| T-07-03-02 | Tampering | CHANGELOG.md | accept | Changelog is documentation; no execution path. Reverse-chronological insertion verified by line-number grep. |
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. pytest closure test — must exit 0
micromamba run -n subsideo pytest tests/unit/ -q --tb=line 2>&1 | tail -10

# 2. CHANGELOG ordering
python3 -c "
content = open('CHANGELOG.md').read()
idx11 = content.index('[1.1.0]')
idx01 = content.index('[0.1.0]')
assert idx11 < idx01, 'FAIL: 1.1.0 not before 0.1.0'
print('PASS: changelog ordering correct')
"

# 3. REL-04 deferral documented
grep -q "REL-04" CHANGELOG.md && echo "PASS: REL-04 in changelog" || echo "FAIL: REL-04 missing"

# 4. Matrix is complete (plan 01 dependency)
! grep -q "RUN_FAILED" results/matrix.md && echo "PASS: matrix complete" || echo "FAIL: RUN_FAILED in matrix"
```
</verification>

<success_criteria>
- `micromamba run -n subsideo pytest tests/unit/ -q` exits 0
- CHANGELOG.md contains `[1.1.0]` entry appearing before `[0.1.0]`
- CHANGELOG.md v1.1 entry mentions REL-04 TrueNAS deferral with "v1.2" unblock
- CHANGELOG.md v1.1 entry documents RTC:NAM DEFERRED cell with migration note
</success_criteria>

<output>
After completion, create `.planning/phases/07-results-matrix-release-readiness/07-03-SUMMARY.md`
</output>
