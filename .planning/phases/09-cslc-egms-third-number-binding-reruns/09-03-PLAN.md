---
phase: 09-cslc-egms-third-number-binding-reruns
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - run_eval_cslc_selfconsist_nam.py
  - tests/unit/test_run_eval_cslc_selfconsist_nam.py
autonomous: true
requirements:
  - CSLC-11
  - VAL-01
  - VAL-03
must_haves:
  truths:
    - "Covers CONTEXT decisions D-08, D-09, D-10, D-12, D-13, D-14, D-15, and D-16"
    - "Mojave/Coso-Searles is attempted first for amplitude sanity"
    - "Every attempted Mojave fallback records OPERA frame-search evidence"
    - "No new nearby-frame research project is introduced"
  artifacts:
    - path: "run_eval_cslc_selfconsist_nam.py"
      provides: "Mojave amplitude-sanity evidence"
      contains: "opera_frame_search"
  key_links:
    - from: "run_eval_cslc_selfconsist_nam.py::opera_frame_search"
      to: "src/subsideo/validation/matrix_schema.py::AOIResult.opera_frame_search"
      contract: "Mojave OPERA frame-search evidence is schema-valid"
    - from: "run_eval_cslc_selfconsist_nam.py::opera_frame_search"
      to: "eval-cslc-selfconsist-nam/metrics.json::per_aoi"
      contract: "Attempted Mojave fallback rows preserve OPERA availability evidence"
---

<objective>
Resolve Mojave amplitude sanity by recording OPERA frame-search evidence and either populated amplitude metrics or an explicit unavailable disposition.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cslc-egms-third-number-binding-reruns/09-CONTEXT.md
@.planning/phases/09-cslc-egms-third-number-binding-reruns/09-RESEARCH.md
@.planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable bounded Mojave amplitude-sanity evidence capture</name>
  <files>run_eval_cslc_selfconsist_nam.py, tests/unit/test_run_eval_cslc_selfconsist_nam.py</files>
  <read_first>
    - run_eval_cslc_selfconsist_nam.py
    - tests/unit/test_run_eval_cslc_selfconsist_nam.py
    - src/subsideo/validation/harness.py
    - src/subsideo/validation/compare_cslc.py
    - .planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md
  </read_first>
  <action>
In `run_eval_cslc_selfconsist_nam.py`, add an explicit field or helper output named `opera_frame_search` for each AOI where amplitude sanity is attempted.

For Mojave fallback leaf configs, set `run_amplitude_sanity=True` only for the existing runnable fallback chain entries:

- `Mojave/Coso-Searles`
- `Mojave/Pahranagat`
- `Mojave/Amargosa`

Do not add new AOIs.

Inside the existing first-epoch OPERA reference search block, capture:

```python
opera_frame_search = {
    "aoi_name": cfg.aoi_name,
    "burst_id": cfg.burst_id,
    "epoch_iso": epoch.isoformat(),
    "opera_burst_upper": opera_burst_upper,
    "granule_pattern": f"OPERA_L2_CSLC-S1_{opera_burst_upper}*",
    "reference_h5_count": len(ref_h5_candidates),
    "selected_reference_h5": str(opera_ref_h5) if ref_h5_candidates else None,
}
```

If amplitude sanity runs, include the above evidence in `AOIResult.opera_frame_search`. If no OPERA reference HDF5 is found, set candidate binding to `BINDING BLOCKER` with:

```python
reason_code="mojave_opera_frame_unavailable"
```

and include `{"opera_frame_search": opera_frame_search}` in `CSLCBlockerEvidence.evidence`. This is schema-valid because Plan 01 extends blocker evidence to allow one-level nested JSON and adds `AOIResult.opera_frame_search`.

Preserve fallback chain behavior: first `PASS` or `CALIBRATING` style successful AOI still wins structurally, but candidate BINDING verdict must reflect amplitude sanity availability for Mojave.
  </action>
  <verify>
    <automated>python3 -m py_compile run_eval_cslc_selfconsist_nam.py</automated>
    <automated>pytest tests/unit/test_run_eval_cslc_selfconsist_nam.py -q</automated>
    <automated>grep -c "opera_frame_search" run_eval_cslc_selfconsist_nam.py</automated>
    <automated>grep -c "mojave_opera_frame_unavailable" run_eval_cslc_selfconsist_nam.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile run_eval_cslc_selfconsist_nam.py` exits 0
    - N.Am. eval script tests exit 0
    - `grep -c "opera_frame_search" run_eval_cslc_selfconsist_nam.py` returns at least 1
    - `grep -c "mojave_opera_frame_unavailable" run_eval_cslc_selfconsist_nam.py` returns at least 1
    - `run_eval_cslc_selfconsist_nam.py` still contains no new AOI names beyond Coso-Searles, Pahranagat, and Amargosa for the Mojave fallback chain
  </acceptance_criteria>
  <done>Mojave amplitude sanity records frame-search evidence and unavailable dispositions.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for Mojave bounded fallback evidence</name>
  <files>tests/unit/test_run_eval_cslc_selfconsist_nam.py</files>
  <read_first>
    - tests/unit/test_run_eval_cslc_selfconsist_nam.py
    - run_eval_cslc_selfconsist_nam.py
  </read_first>
  <action>
Add source-level tests in `tests/unit/test_run_eval_cslc_selfconsist_nam.py` asserting:

1. `run_eval_cslc_selfconsist_nam.py` contains `Mojave/Coso-Searles` before `Mojave/Pahranagat` before `Mojave/Amargosa`.
2. The script contains `opera_frame_search`.
3. The script contains `reference_h5_count`.
4. The script contains `mojave_opera_frame_unavailable`.
5. The script does not contain `Hualapai` in the runnable fallback chain.
  </action>
  <verify>
    <automated>pytest tests/unit/test_run_eval_cslc_selfconsist_nam.py -q</automated>
  </verify>
  <acceptance_criteria>
    - N.Am. eval script tests exit 0
    - Test file contains `Mojave/Coso-Searles`
    - Test file contains `opera_frame_search`
    - Test file contains `mojave_opera_frame_unavailable`
  </acceptance_criteria>
  <done>Tests pin the bounded Mojave amplitude-sanity evidence policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OPERA reference search | External reference availability determines amplitude sanity evidence |
| Fallback chain | Phase 8 artifact constrains allowable Mojave attempts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Repudiation | Mojave unavailable disposition | mitigate | Persist frame-search query details and HDF5 count in sidecar evidence. |
| T-09-03-02 | Tampering | Fallback expansion | mitigate | Tests assert no new runnable Mojave AOIs beyond Phase 8 chain. |
| T-09-03-03 | Denial of Service | OPERA reference lookup | accept | Only first epoch is checked, matching existing amplitude-sanity pattern. |
</threat_model>

<verification>
Run:

```bash
python3 -m py_compile run_eval_cslc_selfconsist_nam.py
pytest tests/unit/test_run_eval_cslc_selfconsist_nam.py -q
```
</verification>

<success_criteria>
- Mojave/Coso-Searles is first attempted for amplitude sanity.
- Attempted Mojave fallbacks record OPERA frame-search evidence.
- Missing OPERA frame match becomes explicit unavailable/blocker evidence.
</success_criteria>
