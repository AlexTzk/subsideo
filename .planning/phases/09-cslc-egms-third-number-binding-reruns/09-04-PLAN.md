---
phase: 09-cslc-egms-third-number-binding-reruns
plan: 04
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
  - "09-03"
files_modified:
  - src/subsideo/validation/matrix_writer.py
  - tests/unit/test_matrix_writer.py
  - CONCLUSIONS_CSLC_SELFCONSIST_NAM.md
  - CONCLUSIONS_CSLC_SELFCONSIST_EU.md
autonomous: true
requirements:
  - CSLC-07
  - CSLC-10
  - CSLC-11
  - VAL-03
must_haves:
  truths:
    - "Covers CONTEXT decisions D-01 through D-16"
    - "Matrix renders explicit BINDING PASS/FAIL/BLOCKER when candidate fields exist"
    - "Legacy CALIBRATING sidecars still render in the old style"
    - "Conclusions distinguish product-quality, reference-agreement, and blocker evidence"
  artifacts:
    - path: "src/subsideo/validation/matrix_writer.py"
      provides: "CSLC candidate BINDING matrix rendering"
      contains: "BINDING BLOCKER"
  key_links:
    - from: "eval-cslc-selfconsist-nam/metrics.json::candidate_binding"
      to: "src/subsideo/validation/matrix_writer.py::_render_cslc_selfconsist_cell"
      contract: "N.Am. candidate verdict renders as BINDING PASS/FAIL/BLOCKER"
    - from: "eval-cslc-selfconsist-eu/metrics.json::candidate_binding"
      to: "results/matrix.md::CSLC EU row"
      contract: "EU candidate verdict and EGMS residual/blocker render in matrix"
---

<objective>
Render candidate CSLC BINDING outcomes in the results matrix and document the interpretation without collapsing evidence categories.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cslc-egms-third-number-binding-reruns/09-CONTEXT.md
@.planning/phases/09-cslc-egms-third-number-binding-reruns/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render candidate BINDING CSLC cells</name>
  <files>src/subsideo/validation/matrix_writer.py, tests/unit/test_matrix_writer.py</files>
  <read_first>
    - src/subsideo/validation/matrix_writer.py
    - src/subsideo/validation/matrix_schema.py
    - tests/unit/test_matrix_writer.py
  </read_first>
  <action>
Update `_render_cslc_selfconsist_cell` in `src/subsideo/validation/matrix_writer.py`:

1. After validating `metrics`, check `if metrics.candidate_binding is not None:`.
2. If present, render the candidate verdict instead of the legacy italic CALIBRATING body.
3. Product-quality column format must include:
   - candidate verdict literal: `BINDING PASS`, `BINDING FAIL`, or `BINDING BLOCKER`
   - `coh=...`
   - `resid=... mm/yr`
   - for EU, `egms_resid=... mm/yr` when present
   - for blockers, `blocker=<reason_code>`
4. Reference-agreement column must continue to render amplitude sanity separately as `amp_r=... / amp_rmse=... dB`, or `unavailable=<reason_code>` when the candidate blocker evidence reason is Mojave/OPERA related.
5. Do not italicise candidate BINDING rows.
6. Preserve the existing legacy path when `candidate_binding is None`.

Add tests in `tests/unit/test_matrix_writer.py`:

- Candidate `BINDING PASS` row is not italicised and contains `BINDING PASS`.
- Candidate `BINDING FAIL` row contains `BINDING FAIL`.
- Candidate `BINDING BLOCKER` row contains `BINDING BLOCKER` and `blocker=`.
- EU candidate row with EGMS residual contains `egms_resid=`.
- Legacy no-`candidate_binding` CSLC sidecar still renders `CALIBRATING`.
  </action>
  <verify>
    <automated>pytest tests/unit/test_matrix_writer.py -q</automated>
    <automated>grep -c "BINDING BLOCKER" src/subsideo/validation/matrix_writer.py</automated>
    <automated>grep -c "candidate_binding" tests/unit/test_matrix_writer.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_matrix_writer.py -q` exits 0
    - `grep -c "BINDING PASS" src/subsideo/validation/matrix_writer.py` returns at least 1
    - `grep -c "BINDING FAIL" src/subsideo/validation/matrix_writer.py` returns at least 1
    - `grep -c "BINDING BLOCKER" src/subsideo/validation/matrix_writer.py` returns at least 1
    - Candidate BINDING tests assert no Markdown italics around candidate rows
  </acceptance_criteria>
  <done>Matrix writer displays CSLC candidate BINDING verdicts and preserves legacy CALIBRATING behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update CSLC conclusions with Phase 9 evidence vocabulary</name>
  <files>CONCLUSIONS_CSLC_SELFCONSIST_NAM.md, CONCLUSIONS_CSLC_SELFCONSIST_EU.md</files>
  <read_first>
    - CONCLUSIONS_CSLC_SELFCONSIST_NAM.md
    - CONCLUSIONS_CSLC_SELFCONSIST_EU.md
    - .planning/phases/09-cslc-egms-third-number-binding-reruns/09-CONTEXT.md
    - .planning/phases/09-cslc-egms-third-number-binding-reruns/09-RESEARCH.md
  </read_first>
  <action>
Add a Phase 9 section skeleton to both conclusions files:

```markdown
## Phase 9 BINDING rerun evidence
```

For N.Am., include explicit TODO markers for Plan 05 to replace after reruns:

- SoCal candidate product-quality verdict
- Mojave/Coso-Searles candidate product-quality verdict
- Mojave OPERA amplitude-sanity disposition
- Phase 8 candidate thresholds: `median_of_persistent >= 0.75`, `residual_mm_yr <= 2.0`

For EU, include explicit TODO markers for Plan 05 to replace after reruns:

- Iberian candidate product-quality verdict
- EGMS L2a stable-PS residual or EGMS blocker reason
- `stable_std_max=2.0`
- `min_valid_points=100`
- Phase 8 candidate thresholds

Use wording that explicitly separates product-quality self-consistency, reference-agreement amplitude sanity, and blocker evidence. Every placeholder must begin with `TODO(Phase 9 rerun):` so Plan 05 can grep and replace it after live sidecars exist.
  </action>
  <verify>
    <automated>grep -c "Phase 9 BINDING rerun evidence" CONCLUSIONS_CSLC_SELFCONSIST_NAM.md</automated>
    <automated>grep -c "Phase 9 BINDING rerun evidence" CONCLUSIONS_CSLC_SELFCONSIST_EU.md</automated>
    <automated>grep -c "stable_std_max=2.0" CONCLUSIONS_CSLC_SELFCONSIST_EU.md</automated>
  </verify>
  <acceptance_criteria>
    - Both conclusions files contain `Phase 9 BINDING rerun evidence`
    - N.Am. conclusions contain `Mojave`
    - EU conclusions contain `EGMS L2a`
    - EU conclusions contain `stable_std_max=2.0`
    - Both conclusions files contain `median_of_persistent >= 0.75`
    - Both conclusions files contain `TODO(Phase 9 rerun):`
  </acceptance_criteria>
  <done>Conclusions use the Phase 9 BINDING vocabulary and preserve evidence separation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix output | `results/matrix.md` is the user-facing release summary |
| Conclusions docs | Human-readable science interpretation can accidentally overstate evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | Matrix rendering | mitigate | Tests pin PASS/FAIL/BLOCKER rendering and legacy CALIBRATING fallback. |
| T-09-04-02 | Repudiation | Blocker wording | mitigate | Render `blocker=<reason_code>` and document evidence categories. |
| T-09-04-03 | Information Disclosure | Error evidence in matrix | mitigate | Matrix shows reason code, not full exception trace or credentials. |
</threat_model>

<verification>
Run:

```bash
pytest tests/unit/test_matrix_writer.py -q
grep -c "Phase 9 BINDING rerun evidence" CONCLUSIONS_CSLC_SELFCONSIST_NAM.md
grep -c "Phase 9 BINDING rerun evidence" CONCLUSIONS_CSLC_SELFCONSIST_EU.md
```
</verification>

<success_criteria>
- Candidate BINDING rows are visible in matrix output.
- Legacy CALIBRATING rows remain supported.
- Conclusions describe Phase 9 evidence categories cleanly.
</success_criteria>
