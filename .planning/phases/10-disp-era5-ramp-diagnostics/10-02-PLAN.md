---
phase: 10-disp-era5-ramp-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/subsideo/products/disp.py
  - run_eval_disp.py
  - run_eval_disp_egms.py
  - tests/unit/test_disp_pipeline.py
requirements:
  - DISP-06
autonomous: true
must_haves:
  truths:
    - "Covers CONTEXT decisions D-01, D-02, D-03, D-04, D-09, D-17, D-18, D-19, D-20, and D-21"
    - "Use v1.1 no-ERA5/off results as warm baseline and write ERA5-on diagnostics separately"
    - "CDS API / ERA5 data access failures may become blockers; local code, adapter, conda, pyaps3, or MintPy integration failures remain implementation work"
    - "Do not double every Phase 11 candidate into ERA5 on/off variants unless the two-signal rule supports ERA5-on as required baseline"
  artifacts:
    - path: "src/subsideo/products/disp.py"
      provides: "ERA5 toggle in DISP pipeline"
      contains: "era5_mode"
    - path: "run_eval_disp.py"
      provides: "SoCal ERA5 diagnostic rerun path"
      contains: "ERA5_MODE"
    - path: "run_eval_disp_egms.py"
      provides: "Bologna ERA5 diagnostic rerun path"
      contains: "ERA5_MODE"
---

<objective>
Make ERA5 correction explicitly toggleable and route SoCal/Bologna ERA5-on diagnostic outputs without overwriting the v1.1 no-ERA5/off baseline caches.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-disp-era5-ramp-diagnostics/10-CONTEXT.md
@.planning/phases/10-disp-era5-ramp-diagnostics/10-RESEARCH.md
@.planning/phases/10-disp-era5-ramp-diagnostics/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `era5_mode` to the DISP pipeline</name>
  <files>src/subsideo/products/disp.py, tests/unit/test_disp_pipeline.py</files>
  <read_first>
    - src/subsideo/products/disp.py
    - tests/unit/test_disp_pipeline.py
    - .planning/phases/10-disp-era5-ramp-diagnostics/10-RESEARCH.md
  </read_first>
  <action>
In `src/subsideo/products/disp.py`, import `Literal` and add `era5_mode: Literal["on", "off"] = "on"` to `_generate_mintpy_template()` and `run_disp()`.

When `era5_mode == "on"`, keep existing behavior:

```text
mintpy.troposphericDelay.method = pyaps
mintpy.troposphericDelay.weatherModel = ERA5
```

When `era5_mode == "off"`, write:

```text
mintpy.troposphericDelay.method = no
mintpy.troposphericDelay.weatherModel = auto
```

Only call `_validate_cds_credentials(cdsapirc_path)` when `era5_mode == "on"`. If any other value is passed, raise `ValueError("era5_mode must be 'on' or 'off'")`.

Update tests in `tests/unit/test_disp_pipeline.py`:

1. Existing `_generate_mintpy_template(tmp_path, ...)` default still contains `method = pyaps` and `weatherModel = ERA5`.
2. `_generate_mintpy_template(tmp_path, ..., era5_mode="off")` contains `mintpy.troposphericDelay.method = no`.
3. `run_disp(..., era5_mode="off")` does not require a `.cdsapirc` when dependencies are mocked.
4. `run_disp(..., era5_mode="invalid")` raises `ValueError`.
  </action>
  <verify>
    <automated>pytest tests/unit/test_disp_pipeline.py -q</automated>
    <automated>grep -c "era5_mode" src/subsideo/products/disp.py</automated>
    <automated>grep -c "mintpy.troposphericDelay.method = no" src/subsideo/products/disp.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_disp_pipeline.py -q` exits 0
    - `src/subsideo/products/disp.py` contains `era5_mode: Literal["on", "off"] = "on"`
    - `src/subsideo/products/disp.py` contains `mintpy.troposphericDelay.method = no`
    - `_validate_cds_credentials(cdsapirc_path)` is guarded by `if era5_mode == "on"`
  </acceptance_criteria>
  <done>DISP pipeline can run with ERA5 on or off under testable configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Route ERA5-on diagnostic outputs in both eval scripts</name>
  <files>run_eval_disp.py, run_eval_disp_egms.py</files>
  <read_first>
    - run_eval_disp.py
    - run_eval_disp_egms.py
    - src/subsideo/products/disp.py
    - .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md
  </read_first>
  <action>
In both `run_eval_disp.py` and `run_eval_disp_egms.py`, add module-level constants near `REFERENCE_MULTILOOK_METHOD`:

```python
ERA5_MODE: Literal["on", "off"] = "on"
BASELINE_ERA5_MODE: Literal["off"] = "off"
```

In Stage 7, use an ERA5-specific DISP output directory when `ERA5_MODE == "on"`:

```python
disp_dir = OUT / ("disp-era5-on" if ERA5_MODE == "on" else "disp")
```

Pass `era5_mode=ERA5_MODE` into `run_disp(...)`.

Do not delete, rewrite, or rename the existing `OUT / "disp"` baseline directory. Existing v1.1 `metrics.json` and `meta.json` remain the warm baseline record.

In Stage 12, set `era5_diagnostic.mode` to `ERA5_MODE` once Plan 10-01 schema exists. If the executor has not yet wired full delta classification, write at least the mode and leave delta fields `None` rather than inventing values.
  </action>
  <verify>
    <automated>python3 -m py_compile run_eval_disp.py run_eval_disp_egms.py</automated>
    <automated>grep -c "disp-era5-on" run_eval_disp.py</automated>
    <automated>grep -c "era5_mode=ERA5_MODE" run_eval_disp_egms.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile run_eval_disp.py run_eval_disp_egms.py` exits 0
    - Both eval scripts contain `ERA5_MODE: Literal["on", "off"] = "on"`
    - Both eval scripts contain `disp-era5-on`
    - Both eval scripts pass `era5_mode=ERA5_MODE` to `run_disp`
    - Existing `OUT / "disp"` string remains present as the off/baseline path
  </acceptance_criteria>
  <done>SoCal and Bologna can generate ERA5-on diagnostics beside existing warm baselines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDS credentials | User-local token controls ERA5 data access |
| Eval cache directories | Baseline and ERA5-on outputs must not overwrite each other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Information Disclosure | `.cdsapirc` | mitigate | Validate existence/shape only; never serialize token contents to sidecars. |
| T-10-02-02 | Tampering | Baseline cache | mitigate | ERA5-on uses `disp-era5-on`, leaving `disp` baseline intact. |
| T-10-02-03 | Denial of Service | ERA5 downloads | accept with guard | Surface CDS/ERA5 data access failures as named blockers, not silent success. |
</threat_model>

<verification>
Run:

```bash
pytest tests/unit/test_disp_pipeline.py -q
python3 -m py_compile run_eval_disp.py run_eval_disp_egms.py
```
</verification>

<success_criteria>
- `run_disp()` supports ERA5 on/off.
- Both DISP eval scripts can run the ERA5-on diagnostic path separately from v1.1 baseline outputs.
- Missing CDS credentials only block ERA5-on, not local ERA5-off pipeline tests.
</success_criteria>
