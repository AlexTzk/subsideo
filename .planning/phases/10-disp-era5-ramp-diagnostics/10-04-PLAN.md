---
phase: 10-disp-era5-ramp-diagnostics
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified:
  - src/subsideo/validation/matrix_writer.py
  - tests/reference_agreement/test_matrix_writer_disp.py
  - CONCLUSIONS_DISP_N_AM.md
  - CONCLUSIONS_DISP_EU.md
  - docs/validation_methodology.md
requirements:
  - DISP-06
  - RTCSUP-02
autonomous: true
must_haves:
  truths:
    - "Covers CONTEXT decisions D-03, D-04, D-05, D-15, D-16, D-17, D-18, D-19, D-20, and D-21"
    - "Conclusions compare against v1.1 baselines: SoCal r=0.0490 bias=+23.6153 RMSE=59.5567; Bologna r=0.3358 bias=+3.4608 RMSE=5.2425"
    - "Sidecars schema-validate before conclusions are updated"
    - "If only one cell meets the two-signal rule, keep global Phase 11 order and annotate the cell-specific exception"
    - "If ERA5 does not meet the two-signal rule, Phase 11 starts with SPURT native, then PHASS deramping, then tophu/SNAPHU, then 20 x 20 m fallback"
  artifacts:
    - path: "CONCLUSIONS_DISP_N_AM.md"
      provides: "SoCal ERA5/ramp diagnostic verdict"
      contains: "Phase 10 ERA5 diagnostic"
    - path: "CONCLUSIONS_DISP_EU.md"
      provides: "Bologna ERA5/ramp diagnostic verdict"
      contains: "Phase 10 ERA5 diagnostic"
    - path: "docs/validation_methodology.md"
      provides: "Documented ERA5 diagnostic methodology"
      contains: "ERA5 tropospheric diagnostic"
---

<objective>
Render Phase 10 diagnostics in matrix/conclusions, run the SoCal and Bologna ERA5 diagnostic commands where credentials allow, and record Phase 11 ordering guidance.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-disp-era5-ramp-diagnostics/10-CONTEXT.md
@.planning/phases/10-disp-era5-ramp-diagnostics/10-RESEARCH.md
@.planning/phases/10-disp-era5-ramp-diagnostics/10-PATTERNS.md
@CONCLUSIONS_DISP_N_AM.md
@CONCLUSIONS_DISP_EU.md
@docs/validation_methodology.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact matrix rendering for ERA5 diagnostic flags</name>
  <files>src/subsideo/validation/matrix_writer.py, tests/reference_agreement/test_matrix_writer_disp.py</files>
  <read_first>
    - src/subsideo/validation/matrix_writer.py
    - tests/reference_agreement/test_matrix_writer_disp.py
    - src/subsideo/validation/matrix_schema.py
  </read_first>
  <action>
Update `_render_disp_cell()` so the product-quality column appends compact diagnostic fields when present:

- `era5=on` or `era5=off` from `m.era5_diagnostic.mode`
- `signals=N` from `len(m.era5_diagnostic.improvement_signals)`
- `narrowed=tropospheric` when `m.cause_assessment.eliminated_causes == ["tropospheric"]`

Do not render detailed provenance in the matrix cell; conclusions tables carry details.

Update `tests/reference_agreement/test_matrix_writer_disp.py` fixture to optionally include `era5_diagnostic` and `cause_assessment`. Add tests asserting:

```python
assert "era5=on" in pq_col
assert "signals=2" in pq_col
assert "narrowed=tropospheric" in pq_col
```
  </action>
  <verify>
    <automated>pytest tests/reference_agreement/test_matrix_writer_disp.py -q</automated>
    <automated>grep -c "era5=on" tests/reference_agreement/test_matrix_writer_disp.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/reference_agreement/test_matrix_writer_disp.py -q` exits 0
    - Matrix writer still renders old DISP fixtures with no ERA5 fields
    - New test fixture renders `era5=on`
    - New test fixture renders `signals=2`
    - New test fixture renders `narrowed=tropospheric`
  </acceptance_criteria>
  <done>Matrix cells surface Phase 10 diagnostic status without collapsing evidence categories.</done>
</task>

<task type="manual">
  <name>Task 2: Run or block live Phase 10 DISP diagnostics</name>
  <files>eval-disp/metrics.json, eval-disp/meta.json, eval-disp-egms/metrics.json, eval-disp-egms/meta.json</files>
  <read_first>
    - run_eval_disp.py
    - run_eval_disp_egms.py
    - .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md
    - .planning/phases/10-disp-era5-ramp-diagnostics/10-CONTEXT.md
  </read_first>
  <action>
Run:

```bash
make eval-disp-nam
make eval-disp-eu
```

If `.cdsapirc`, CDS API, ERA5 license, or ERA5 download access fails, capture a named blocker in the execution summary with:

- blocker code: `era5_data_access_blocked`
- whether `$HOME/.cdsapirc` exists
- command that failed
- exception or terminal line proving the failure
- unblock condition: "Refresh CDS personal access token and accept ERA5 pressure-level data terms, then rerun `make eval-disp-nam` and `make eval-disp-eu`"

Do not mark local code errors as this blocker. Fix local code, pyaps3 imports, MintPy config, schema validation, and eval-script integration issues inside this phase.
  </action>
  <verify>
    <manual>make eval-disp-nam</manual>
    <manual>make eval-disp-eu</manual>
    <automated>python - <<'PY'\nfrom pathlib import Path\nfrom subsideo.validation.matrix_schema import DISPCellMetrics\nfor p in [Path('eval-disp/metrics.json'), Path('eval-disp-egms/metrics.json')]:\n    if p.exists():\n        DISPCellMetrics.model_validate_json(p.read_text())\n        print(f'validated {p}')\nPY</automated>
  </verify>
  <acceptance_criteria>
    - `make eval-disp-nam` completes or records blocker `era5_data_access_blocked`
    - `make eval-disp-eu` completes or records blocker `era5_data_access_blocked`
    - Any existing `eval-disp/metrics.json` validates as `DISPCellMetrics`
    - Any existing `eval-disp-egms/metrics.json` validates as `DISPCellMetrics`
    - No local code/schema/import failure is hidden behind a CDS blocker
  </acceptance_criteria>
  <done>Live diagnostic results or honest ERA5 access blockers exist for both cells.</done>
</task>

<task type="auto">
  <name>Task 3: Update conclusions and methodology from schema-valid sidecars</name>
  <files>CONCLUSIONS_DISP_N_AM.md, CONCLUSIONS_DISP_EU.md, docs/validation_methodology.md</files>
  <read_first>
    - CONCLUSIONS_DISP_N_AM.md
    - CONCLUSIONS_DISP_EU.md
    - docs/validation_methodology.md
    - eval-disp/metrics.json
    - eval-disp-egms/metrics.json
    - .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md
    - .planning/phases/10-disp-era5-ramp-diagnostics/10-CONTEXT.md
  </read_first>
  <action>
Before editing conclusions, validate any present sidecars with `DISPCellMetrics.model_validate_json(...)`.

In both conclusions files, add a section titled exactly:

```markdown
## Phase 10 ERA5 diagnostic
```

Include tables for:

1. v1.1 baseline vs Phase 10 ERA5-on metrics using the fixed baseline values:
   - SoCal baseline: `r=0.0490`, `bias=+23.6153`, `RMSE=59.5567`
   - Bologna baseline: `r=0.3358`, `bias=+3.4608`, `RMSE=5.2425`
2. Ramp-attribution deltas: mean ramp magnitude, direction sigma, and attribution label.
3. Diagnostic provenance: orbit coverage, DEM source/hash/nodata fraction, slope p50/p90, stable-mask retention, terrain-vs-ramp correlation, cache mode summary.
4. Cause assessment: `eliminated_causes`, `remaining_causes`, `next_test`.

Add a Phase 11 guidance paragraph:

- If both cells meet the two-signal rule, state ERA5-on is required baseline for Phase 11 candidates.
- If one cell meets the rule, keep global v1.1 order and annotate the cell-specific exception.
- If neither meets the rule, state the v1.1 global order exactly: "SPURT native first, then PHASS deramping, then tophu/SNAPHU, then 20 x 20 m fallback."

In `docs/validation_methodology.md`, add a subsection titled exactly:

```markdown
### ERA5 tropospheric diagnostic
```

Document that ERA5 is diagnostic in Phase 10, that `inconclusive_narrowed` is human-facing only, and that product-quality, reference-agreement, and ramp-attribution remain separate.
  </action>
  <verify>
    <automated>grep -c "Phase 10 ERA5 diagnostic" CONCLUSIONS_DISP_N_AM.md</automated>
    <automated>grep -c "Phase 10 ERA5 diagnostic" CONCLUSIONS_DISP_EU.md</automated>
    <automated>grep -c "ERA5 tropospheric diagnostic" docs/validation_methodology.md</automated>
    <automated>grep -c "SPURT native first, then PHASS deramping, then tophu/SNAPHU, then 20 x 20 m fallback" CONCLUSIONS_DISP_N_AM.md CONCLUSIONS_DISP_EU.md</automated>
  </verify>
  <acceptance_criteria>
    - Both conclusions files contain `## Phase 10 ERA5 diagnostic`
    - `docs/validation_methodology.md` contains `### ERA5 tropospheric diagnostic`
    - Conclusions include SoCal baseline `r=0.0490`
    - Conclusions include Bologna baseline `r=0.3358`
    - At least one conclusions file contains the exact fallback order string
    - Conclusions do not claim PASS unless sidecars show reference-agreement PASS criteria
  </acceptance_criteria>
  <done>Phase 10 results are visible, traceable, and ready to feed Phase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human conclusions | Release reviewers may act on conclusions without inspecting raw sidecars |
| Live ERA5 run | Network/data access failures must not masquerade as science results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Repudiation | Conclusions | mitigate | Validate sidecars before conclusion edits and cite exact baseline/result values. |
| T-10-04-02 | Tampering | Phase 11 ordering | mitigate | Require two-signal rule and cross-cell support before changing global order. |
| T-10-04-03 | Information Disclosure | Run logs | mitigate | Record CDS blocker evidence without token values. |
</threat_model>

<verification>
Run:

```bash
pytest tests/reference_agreement/test_matrix_writer_disp.py -q
make eval-disp-nam
make eval-disp-eu
grep -c "Phase 10 ERA5 diagnostic" CONCLUSIONS_DISP_N_AM.md
grep -c "Phase 10 ERA5 diagnostic" CONCLUSIONS_DISP_EU.md
grep -c "ERA5 tropospheric diagnostic" docs/validation_methodology.md
```
</verification>

<success_criteria>
- Matrix and conclusions expose Phase 10 diagnostics from schema-valid sidecars.
- Live SoCal/Bologna diagnostic reruns either complete or produce named ERA5 access blockers.
- Phase 11 ordering guidance follows the two-signal and cross-cell rules.
</success_criteria>
