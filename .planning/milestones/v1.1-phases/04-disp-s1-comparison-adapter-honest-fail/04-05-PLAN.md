---
phase: 04-disp-s1-comparison-adapter-honest-fail
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - CONCLUSIONS_DISP_EGMS.md
  - CONCLUSIONS_DISP_EU.md
  - CONCLUSIONS_DISP_N_AM.md
  - docs/validation_methodology.md
  - .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md
autonomous: true
requirements: [DISP-03, DISP-04, DISP-05]
requirements_addressed: [DISP-03, DISP-04, DISP-05]
tags: [insar, validation, conclusions, brief, docs, ramp-attribution]

must_haves:
  truths:
    - "CONCLUSIONS_DISP_EGMS.md is renamed to CONCLUSIONS_DISP_EU.md via git mv (history preserved via git log --follow)"
    - "Both CONCLUSIONS_DISP_*.md files have v1.1 sections appended: Product Quality + Reference Agreement + Ramp Attribution + Link to Brief"
    - ".planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md exists with 4 candidates × 4 columns (description, success criterion, compute tier, dep delta)"
    - "docs/validation_methodology.md §3 'DISP comparison-adapter design — multilook method choice' appended (5-part PITFALLS+FEATURES dialogue per CONTEXT D-03)"
    - "docs/validation_methodology.md §3 explicitly states 'Native 5×10 m stays production default' per DISP-05"
    - "Brief cites the FRESH numbers from Plan 04-04 metrics.json files (correlation, bias, ramp aggregate, attributed_source)"
    - "v1.0 numbers (Resampling.bilinear) preserved in CONCLUSIONS as 'v1.0 baseline' continuity preamble"
    - "No §4 / §5 content added to docs/validation_methodology.md (Phase 3 D-15 append-only — Phase 4 owns §3 ONLY)"
  artifacts:
    - path: "CONCLUSIONS_DISP_EU.md"
      provides: "Renamed from CONCLUSIONS_DISP_EGMS.md + 4 new v1.1 sub-sections (PQ / RA / Ramp / Brief link)"
      contains: "## 13. Ramp Attribution"
    - path: "CONCLUSIONS_DISP_N_AM.md"
      provides: "4 new v1.1 sub-sections (PQ / RA / Ramp / Brief link)"
      contains: "## 13. Ramp Attribution"
    - path: ".planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md"
      provides: "One-page 4-candidate scoping brief — PHASS+deramping / SPURT native / tophu-SNAPHU tiled / 20×20 m fallback"
      contains: "PHASS+deramping"
    - path: "docs/validation_methodology.md"
      provides: "§3 multilook method ADR (5-part PITFALLS+FEATURES dialogue)"
      contains: "## 3. DISP comparison-adapter design — multilook method choice"
  key_links:
    - from: "CONCLUSIONS_DISP_N_AM.md"
      to: ".planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md"
      via: "v1.1 §14 link"
      pattern: "DISP_UNWRAPPER_SELECTION_BRIEF.md"
    - from: ".planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md"
      to: "eval-disp/metrics.json + eval-disp-egms/metrics.json"
      via: "Brief Context section cites the fresh FAIL numbers from Plan 04-04"
      pattern: "eval-disp"
    - from: "docs/validation_methodology.md"
      to: "src/subsideo/validation/compare_disp.py"
      via: "§3 cross-references prepare_for_reference module location and REFERENCE_MULTILOOK_METHOD constant"
      pattern: "REFERENCE_MULTILOOK_METHOD"
---

<objective>
Land all four documentation/brief artifacts in a single commit at Phase 4 close (per CONTEXT D-13/D-14/D-15/D-16). Reads the freshly produced metrics.json files from Plan 04-04 to populate concrete numbers in the brief and CONCLUSIONS appends. The §3 ADR sub-section in docs/validation_methodology.md is Phase 4's contribution to the append-only methodology doc per Phase 3 D-15.

Purpose: Wave 4 — closes Phase 4. The brief is the handoff to the DISP-V2-01 follow-up milestone; CONCLUSIONS docs surface the FAIL numbers + ramp-attribution + structural correctness framing; docs §3 closes the multilook ADR queue Phase 3 deferred. No code changes here — pure narrative + decision documentation.

Output: 1 git rename (CONCLUSIONS_DISP_EGMS.md → CONCLUSIONS_DISP_EU.md), 4 documentation artifact files (2 CONCLUSIONS appends, 1 brief, 1 §3 doc append). Brief is reviewed by user before commit per CONTEXT D-16.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-CONTEXT.md
@.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-RESEARCH.md
@.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-PATTERNS.md
@.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-04-PLAN.md
@./CLAUDE.md

<interfaces>
<!-- Inputs the brief + CONCLUSIONS appends consume. -->

From Plan 04-04 outputs (must exist before this plan runs):
- eval-disp/metrics.json — DISPCellMetrics for SoCal cell:
  - product_quality.coherence_source = "phase3-cached"
  - product_quality.measurements.coherence_median_of_persistent (≈ 0.887 from Phase 3 cache)
  - product_quality.measurements.residual_mm_yr (fresh from dolphin)
  - reference_agreement.measurements.correlation, bias_mm_yr, rmse_mm_yr, sample_count
  - ramp_attribution.aggregate (mean_magnitude_rad, direction_stability_sigma_deg, magnitude_vs_coherence_pearson_r, n_ifgs)
  - ramp_attribution.attributed_source (one of phass, orbit, mixed, inconclusive)
  - ramp_attribution.per_ifg (list of 14 PerIFGRamp entries)
  - cell_status (MIXED or BLOCKER)

- eval-disp-egms/metrics.json — DISPCellMetrics for Bologna cell:
  - product_quality.coherence_source = "fresh"
  - All same fields as above; n_ifgs ≈ 18 sequential pairs from 19-epoch S1A+S1B stack

From v1.0 baseline (preserved as preamble per CONTEXT D-13):
- CONCLUSIONS_DISP_N_AM.md (258 LOC) — SoCal v1.0: r=0.0365, bias=+23.62 mm/yr, RMSE 59.56 mm/yr, FAIL
- CONCLUSIONS_DISP_EGMS.md (304 LOC) — Bologna v1.0: r=0.32, bias=+3.35 mm/yr, RMSE 5.14 mm/yr, FAIL

From Phase 3 (existing methodology doc — Phase 4 appends ONLY §3):
- docs/validation_methodology.md (247 LOC) — §1 Cross-version phase impossibility + §2 PQ vs RA distinction
- Phase 3 D-15 append-only by phase: Phase 4 owns §3 ONLY (NOT §4 / §5 — those are Phase 5/6/7)

Phase 2 + Phase 3 templates (Brief location convention):
- .planning/milestones/v1.1-research/rtc_eu_burst_candidates.md (Phase 2; 67 LOC)
- .planning/milestones/v1.1-research/cslc_selfconsist_aoi_candidates.md (Phase 3; 253 LOC)
- Phase 4 brief mirrors this v1.1-research/ home + one-page convention
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git mv CONCLUSIONS_DISP_EGMS.md → CONCLUSIONS_DISP_EU.md (preserving v1.0 narrative as preamble)</name>
  <files>CONCLUSIONS_DISP_EGMS.md, CONCLUSIONS_DISP_EU.md</files>
  <read_first>
    - /Volumes/Geospatial/Geospatial/subsideo/CONCLUSIONS_DISP_EGMS.md (entire — 304 LOC; the v1.0 narrative that Phase 4 D-13 keeps verbatim as a "v1.0 baseline" preamble)
    - /Volumes/Geospatial/Geospatial/subsideo/results/matrix_manifest.yml (line 66 confirms manifest already references CONCLUSIONS_DISP_EU.md — Phase 4 D-21)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-CONTEXT.md (D-13 rename + preamble preservation; D-21 manifest already-aligned)
  </read_first>
  <action>
Execute the file rename via git mv so `git log --follow CONCLUSIONS_DISP_EU.md` traces back to the v1.0 narrative:

```bash
git mv CONCLUSIONS_DISP_EGMS.md CONCLUSIONS_DISP_EU.md
```

Critical: do NOT edit the file content in this task. The file's existing v1.0 narrative is preserved verbatim. Append-content edits happen in Task 2 below; doing them in a separate commit makes the rename trivially reviewable (git mv only changes the path).

Single command, single rename. Per CONTEXT D-13: the manifest's `conclusions_doc: CONCLUSIONS_DISP_EU.md` field is already correct (verified in Plan 04-04 Task 3); the rename is the actual change.
  </action>
  <verify>
    <automated>test -f CONCLUSIONS_DISP_EU.md && ! test -f CONCLUSIONS_DISP_EGMS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f CONCLUSIONS_DISP_EU.md` (file exists at new path).
    - `test -f CONCLUSIONS_DISP_EGMS.md && false || true` (file does NOT exist at old path).
    - `git log --follow --oneline -- CONCLUSIONS_DISP_EU.md | head -3` returns ≥ 2 commits (the rename commit + the original v1.0 history).
    - `git diff --staged --stat | grep -E "CONCLUSIONS_DISP_(EGMS|EU)\.md"` shows the rename (R100 marker if untouched, R<100% if modified — Task 1 should show R100 since we're not editing yet).
    - `wc -l CONCLUSIONS_DISP_EU.md` returns the same line count as the v1.0 file (≈ 304).
  </acceptance_criteria>
  <done>
    CONCLUSIONS_DISP_EGMS.md renamed to CONCLUSIONS_DISP_EU.md via git mv. File content unchanged (R100 in git diff). git log --follow traces back to v1.0 history. Manifest reference (already correct from Phase 1 D-08 + Plan 04-04 Task 3) now points to a real on-disk file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append v1.1 sections (Product Quality / Reference Agreement / Ramp Attribution / Brief link) to both CONCLUSIONS files</name>
  <files>CONCLUSIONS_DISP_N_AM.md, CONCLUSIONS_DISP_EU.md</files>
  <read_first>
    - /Volumes/Geospatial/Geospatial/subsideo/CONCLUSIONS_DISP_N_AM.md (entire — 258 LOC; v1.0 narrative preserved as preamble)
    - /Volumes/Geospatial/Geospatial/subsideo/CONCLUSIONS_DISP_EU.md (entire — 304 LOC post-rename; v1.0 narrative preserved as preamble)
    - /Volumes/Geospatial/Geospatial/subsideo/eval-disp/metrics.json (the fresh DISPCellMetrics JSON — read all measurement values for SoCal table population)
    - /Volumes/Geospatial/Geospatial/subsideo/eval-disp-egms/metrics.json (the fresh DISPCellMetrics JSON for Bologna)
    - /Volumes/Geospatial/Geospatial/subsideo/CONCLUSIONS_RTC_EU.md (entire — 298 LOC; Phase 2 multi-section append pattern with Calibration Framing + Investigation tables)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-CONTEXT.md (D-13 four sub-sections per file; D-Claude's-Discretion v1.0 numbers continuity rendering; D-19 cell_status = MIXED is the expected outcome)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-RESEARCH.md (lines 1327-1401 verbatim 4-section append template)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-PATTERNS.md (lines 629-688 multi-section CONCLUSIONS append discipline)
  </read_first>
  <action>
For BOTH `CONCLUSIONS_DISP_N_AM.md` and `CONCLUSIONS_DISP_EU.md`, APPEND the following 4 v1.1 sections AFTER the existing v1.0 narrative (find the last line of the existing file, append starting with a horizontal rule + the v1.1 header). DO NOT modify the existing v1.0 narrative — it stays verbatim as the "v1.0 baseline" preamble per CONTEXT D-13.

**Step 1 — Read fresh numbers from Plan 04-04 metrics.json:**

```bash
# SoCal numbers (fill in for CONCLUSIONS_DISP_N_AM.md)
NAM_COH=$(jq -r '.product_quality.measurements.coherence_median_of_persistent' eval-disp/metrics.json)
NAM_RESID=$(jq -r '.product_quality.measurements.residual_mm_yr' eval-disp/metrics.json)
NAM_R=$(jq -r '.reference_agreement.measurements.correlation' eval-disp/metrics.json)
NAM_BIAS=$(jq -r '.reference_agreement.measurements.bias_mm_yr' eval-disp/metrics.json)
NAM_RMSE=$(jq -r '.reference_agreement.measurements.rmse_mm_yr' eval-disp/metrics.json)
NAM_N=$(jq -r '.reference_agreement.measurements.sample_count' eval-disp/metrics.json)
NAM_RAMP_MEAN=$(jq -r '.ramp_attribution.aggregate.mean_magnitude_rad' eval-disp/metrics.json)
NAM_RAMP_SIGMA=$(jq -r '.ramp_attribution.aggregate.direction_stability_sigma_deg' eval-disp/metrics.json)
NAM_RAMP_R=$(jq -r '.ramp_attribution.aggregate.magnitude_vs_coherence_pearson_r' eval-disp/metrics.json)
NAM_ATTR=$(jq -r '.ramp_attribution.attributed_source' eval-disp/metrics.json)
NAM_NIFG=$(jq -r '.ramp_attribution.aggregate.n_ifgs' eval-disp/metrics.json)
NAM_COH_SRC=$(jq -r '.product_quality.coherence_source' eval-disp/metrics.json)

# Bologna numbers (fill in for CONCLUSIONS_DISP_EU.md)
EU_COH=$(jq -r '.product_quality.measurements.coherence_median_of_persistent' eval-disp-egms/metrics.json)
EU_RESID=$(jq -r '.product_quality.measurements.residual_mm_yr' eval-disp-egms/metrics.json)
# ... repeat for r, bias, rmse, n, ramp_mean, ramp_sigma, ramp_r, attr, nifg, coh_src
```

The executor MUST substitute the actual numerical values in the table cells below — do NOT leave placeholder X.X strings.

**Step 2 — Append the following template VERBATIM to the end of `CONCLUSIONS_DISP_N_AM.md`** (substitute the SoCal numbers for the X.X placeholders, append after a `---` horizontal rule):

```markdown

---

# v1.1 Update (Phase 4) — 2026-04-25

> The sections below are Phase 4 additions. The v1.0 narrative above is preserved as the v1.0 baseline (`Resampling.bilinear` ad-hoc multilook). Phase 4 D-13 keeps both audit trails alongside.

## 11. Product Quality (CALIBRATING)

### 11.1 Coherence (sequential 12-day, boxcar 5×5)

| Stat | Value | Source | Criterion | Verdict |
|------|-------|--------|-----------|---------|
| `coherence_median_of_persistent` | <NAM_COH> | <NAM_COH_SRC> | > 0.7 (CALIBRATING) | CALIBRATING |
| `coherence_mean` | <NAM_COH_MEAN> | <NAM_COH_SRC> | (informational) | — |
| `coherence_median` | <NAM_COH_MED> | <NAM_COH_SRC> | (informational) | — |
| `coherence_p25` / `p75` | <NAM_P25> / <NAM_P75> | <NAM_COH_SRC> | (informational) | — |
| `persistently_coherent_fraction` | <NAM_PCF> | <NAM_COH_SRC> | (informational) | — |

The gate stat `coherence_median_of_persistent` is read from `eval-cslc-selfconsist-nam/metrics.json` per CONTEXT D-08 cross-cell-read optimization (SoCal CSLC stack is identical to Phase 3 SoCal). `coherence_source = "phase3-cached"` is the auditable provenance flag in `eval-disp/metrics.json`.

### 11.2 Residual mean velocity (dolphin output, frame_anchor='median')

| Stat | Value | Criterion | Verdict |
|------|-------|-----------|---------|
| `residual_mm_yr` | <NAM_RESID> | < 5 mm/yr (CALIBRATING) | <PASS_OR_FAIL> |

Residual is computed FRESH from dolphin's `eval-disp/disp/dolphin/timeseries/velocity.tif` per CONTEXT D-08 (residual is what we're validating; never cross-cell-read). The unit conversion uses `SENTINEL1_WAVELENGTH_M = 0.05546576` and the standard InSAR sign convention (positive phase rate → target moving toward sensor → negative LOS surface motion).

## 12. Reference Agreement (BINDING)

| Metric | Value | Criterion | Verdict |
|--------|-------|-----------|---------|
| Pearson r | <NAM_R> | > 0.92 | <PASS_OR_FAIL> |
| `bias_mm_yr` | <NAM_BIAS> | < 3 mm/yr | <PASS_OR_FAIL> |
| `rmse_mm_yr` | <NAM_RMSE> | (informational) | — |
| Sample count | <NAM_N> | — | — |

**Comparison method:** `prepare_for_reference(method="block_mean")` per CONTEXT D-02 + the multilook ADR in `docs/validation_methodology.md` §3. The conservative kernel matches OPERA's own multilook semantically and minimises kernel-flattery attack surface.

**Continuity with v1.0:** the v1.0 numbers using `Resampling.bilinear` (the ad-hoc multilook in v1.0 Stage 9) were r=0.0365 / bias=+23.62 mm/yr / RMSE=59.56 mm/yr (this cell, see preamble §6). The Phase 4 block_mean numbers are reported above; any delta vs v1.0 reflects the kernel-choice effect (CONTEXT specifics: block_mean is the conservative floor, not a new optimisation).

## 13. Ramp Attribution

### 13.1 Per-IFG ramp parameters (top 5 by magnitude; full list in `eval-disp/metrics.json`)

| ifg_idx | ref_date | sec_date | magnitude_rad | direction_deg | coherence_mean |
|---------|----------|----------|---------------|---------------|----------------|
| <fill from sorted per_ifg list> | ... | ... | ... | ... | ... |

(Sort `eval-disp/metrics.json` `ramp_attribution.per_ifg` by `ramp_magnitude_rad` descending; show top 5 here. Full <NAM_NIFG>-row table is in the JSON.)

### 13.2 Aggregate

| Stat | Value | Cutoff | Implication |
|------|-------|--------|-------------|
| `mean_magnitude_rad` | <NAM_RAMP_MEAN> | (informational) | — |
| `direction_stability_sigma_deg` | <NAM_RAMP_SIGMA> | < 30° → orbit-class | <stable / random> direction |
| `magnitude_vs_coherence_pearson_r` | <NAM_RAMP_R> | > 0.5 → phass-class | <coh-correlated / not> |
| `n_ifgs` | <NAM_NIFG> | — | — |

### 13.3 Auto-attribute label

**Label:** `<NAM_ATTR>`

**Rule (CONTEXT D-Claude's-Discretion):** direction sigma <NAM_RAMP_SIGMA>° (cutoff 30° → <stable/random>); coh-correlation <NAM_RAMP_R> (cutoff 0.5 → <correlated/not>). Combined → `<NAM_ATTR>`.

### 13.4 Diagnostic deferrals (CONTEXT D-09)

- **(b) POEORB swap on RESORB epochs:** not run — all 15 SoCal epochs are POEORB-era (verified in v1.0 §2 + CONTEXT specifics).
- **(c) ERA5 toggle:** not run — `pyaps3 >= 0.3.6` ERA5 path is not enabled in the current pipeline (DISP-V2-02 in REQUIREMENTS).

### 13.5 Human review note

> Automated attribution: `<NAM_ATTR>`.
> Reviewed: pending. Human reviewer notes the diagnostic-(c) deferral when reasoning about a 'tropospheric' or 'mixed' label upgrade.

Per Phase 2 D-15 investigation discipline ("automation flags, doesn't replace narrative"), the matrix label comes from auto-attribute (audit trail in `metrics.json`); the canonical labelling for the brief is the human review.

## 14. DISP Unwrapper Selection — Handoff

See [`.planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md`](.planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md) for the candidate-prioritisation handoff. The four candidates (PHASS+deramping, SPURT native, tophu-SNAPHU tiled, 20×20 m fallback) each carry a success criterion derived from this cell's reference-agreement FAIL numbers + ramp-attribution label.

Per Phase 4 D-15 / D-16: this brief is the v1.1 closure handoff — the DISP-V2-01 follow-up milestone consumes it as input.

---

**Phase 4 closure verdict (this cell):** cell_status = `<NAM_CELL_STATUS>` = CALIBRATING product_quality + <PASS_OR_FAIL> reference_agreement (CONTEXT D-19: MIXED is the expected first-rollout status). The FAIL on reference-agreement is structurally correct — it documents the PHASS unwrapper limitation, not a subsideo-layer bug. The named upgrade path is in the brief.
```

**Step 3 — Append the analogous v1.1 sections to `CONCLUSIONS_DISP_EU.md`** with Bologna-specific values (substitute `EU_*` numbers; note `coherence_source = "fresh"`; note the 18-IFG sequential pair count from the 19-epoch S1A+S1B cross-constellation stack):

Replace `<NAM_*>` placeholders with `<EU_*>` values. Replace `eval-disp/metrics.json` references with `eval-disp-egms/metrics.json`. The §11.1 "Source" column shows `fresh` (NOT `phase3-cached`) because Phase 3 EU was Iberian Meseta, not Bologna (CONTEXT D-08).

Update the §13.4 diagnostic deferrals subsection for Bologna context:
- **(b) POEORB swap:** Bologna 2021 H1 is fully POEORB-era (S1A + S1B cross-constellation); not run because no RESORB epochs to swap.
- **(c) ERA5 toggle:** same as N.Am. — pyaps3 ERA5 path not enabled.

Update §11.1's prose to remove the `phase3-cached` mention (Bologna doesn't reuse Phase 3):
> The gate stat `coherence_median_of_persistent` is computed fresh from the 19-epoch CSLC stack via the Phase 3 boxcar 5×5 sequential 12-day convention. `coherence_source = "fresh"` is the auditable provenance flag in `eval-disp-egms/metrics.json`. (Phase 3 EU validation ran on Iberian Meseta — not Bologna — so no cross-cell read applies here.)

Update §12 "Continuity with v1.0" to cite Bologna's v1.0 numbers:
> the v1.0 numbers using `Resampling.bilinear` were r=0.32 / bias=+3.35 mm/yr / RMSE=5.14 mm/yr (this cell, see preamble §1).

**Step 4 — BLOCKER handling:** if Plan 04-04 produced `cell_status = "BLOCKER"` for either cell (Bologna stable-mask risk), document the BLOCKER outcome explicitly:

Replace the §11 / §12 / §13 tables with a BLOCKER notice section:

```markdown
## 11. Product Quality — BLOCKER

The Bologna stable-mask via WorldCover class 60 (bare/sparse vegetation) produced fewer than 100 valid pixels — a known risk per CONTEXT/RESEARCH §"Bologna-specific" (Po-plain croplands lack class 60 coverage). This is a BLOCKER per Phase 3 D-11 discipline.

**Outcome:**
- `cell_status = "BLOCKER"` in `eval-disp-egms/metrics.json`
- Stable-mask pixels: <N_STABLE>
- coherence + residual NOT computed (would have been on a sub-100-pixel mask, statistically meaningless)

**Why we did NOT loosen the class filter (M1 anti-pattern):** loosening to include class 30 (grassland) or class 20 (shrubland) is per-AOI tuning that CONTEXT D-06 forbids — the same builder + same parameters as Phase 3 SoCal is the discipline. The BLOCKER signal IS information for the follow-up milestone.

**Reference agreement still computed:** Stage 9 `prepare_for_reference(method="block_mean")` ran successfully against EGMS L2a PS points; the §12 numbers below are valid even without product_quality.
```

(Then keep §12 + §13 + §14 sections as designed; only §11 changes shape on BLOCKER.)
  </action>
  <verify>
    <automated>grep -cE "^## 1[1-4]\\." CONCLUSIONS_DISP_N_AM.md CONCLUSIONS_DISP_EU.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 11\\." CONCLUSIONS_DISP_N_AM.md` returns 1.
    - `grep -c "^## 12\\." CONCLUSIONS_DISP_N_AM.md` returns 1.
    - `grep -c "^## 13\\." CONCLUSIONS_DISP_N_AM.md` returns 1.
    - `grep -c "^## 14\\." CONCLUSIONS_DISP_N_AM.md` returns 1.
    - Same 4 grep checks pass on `CONCLUSIONS_DISP_EU.md`.
    - `grep -c "DISP_UNWRAPPER_SELECTION_BRIEF.md" CONCLUSIONS_DISP_N_AM.md` returns ≥ 1 (Brief link in §14).
    - `grep -c "DISP_UNWRAPPER_SELECTION_BRIEF.md" CONCLUSIONS_DISP_EU.md` returns ≥ 1.
    - `grep -c "phase3-cached" CONCLUSIONS_DISP_N_AM.md` returns ≥ 1 (provenance flag visible).
    - `grep -c "fresh" CONCLUSIONS_DISP_EU.md` returns ≥ 1 (Bologna provenance).
    - `grep -c "v1.0 baseline" CONCLUSIONS_DISP_N_AM.md` returns ≥ 1 (v1.0 narrative preserved-as-preamble framing).
    - `grep -c "v1.0 baseline" CONCLUSIONS_DISP_EU.md` returns ≥ 1.
    - `grep -E "Resampling.bilinear" CONCLUSIONS_DISP_N_AM.md | head -1` matches (continuity citation).
    - `grep -E "Resampling.bilinear" CONCLUSIONS_DISP_EU.md | head -1` matches.
    - `grep -E "block_mean" CONCLUSIONS_DISP_N_AM.md | head -1` matches.
    - `grep -E "block_mean" CONCLUSIONS_DISP_EU.md | head -1` matches.
    - No literal placeholder strings `<NAM_` or `<EU_` remain (all numerical values substituted): `! grep -E '<(NAM|EU)_' CONCLUSIONS_DISP_N_AM.md CONCLUSIONS_DISP_EU.md`.
    - `wc -l CONCLUSIONS_DISP_N_AM.md` returns ≥ 350 (was 258; v1.1 sections add ≥90 lines).
    - `wc -l CONCLUSIONS_DISP_EU.md` returns ≥ 400 (was 304 post-rename; v1.1 sections add ≥90 lines).
  </acceptance_criteria>
  <done>
    Both CONCLUSIONS_DISP_*.md files have v1.0 narrative preserved as preamble + 4 v1.1 sub-sections appended (§11 Product Quality / §12 Reference Agreement / §13 Ramp Attribution / §14 Brief link). Numbers substituted from Plan 04-04 metrics.json files; no placeholder strings remain. v1.0 vs v1.1 continuity cited in §12 with explicit kernel-difference framing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the DISP Unwrapper Selection scoping brief (4 candidates × 4 columns)</name>
  <files>.planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md</files>
  <read_first>
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/milestones/v1.1-research/rtc_eu_burst_candidates.md (Phase 2 probe artifact — 67 LOC; v1.1-research/ template style)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/milestones/v1.1-research/cslc_selfconsist_aoi_candidates.md (Phase 3 probe artifact — 253 LOC; denser candidate-evaluation template)
    - /Volumes/Geospatial/Geospatial/subsideo/eval-disp/metrics.json + eval-disp-egms/metrics.json (the fresh FAIL numbers the brief opens with)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-CONTEXT.md (D-13/D-14/D-15/D-16 brief discipline + 4-candidate framing + NO MintPy SBAS as 5th)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-RESEARCH.md (lines 1262-1325 verbatim brief skeleton; lines 1300-1311 attribution-driven prioritisation)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-PATTERNS.md (lines 813-879 v1.1-research/ artefact convention)
  </read_first>
  <action>
Create new file `.planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md`. Read the fresh metrics.json files first to populate concrete numbers. The brief is ONE markdown page (~150-250 LOC; not a plan, not an ADR).

```markdown
# DISP Unwrapper Selection Scoping Brief — v1.1-research handoff

**Drafted:** 2026-04-25 (Phase 4 close)
**Audience:** Future contributor picking up the DISP-V2-01 follow-up milestone
**Status:** Scoping brief; **NOT** a plan. Candidates proposed; success criteria proposed. The follow-up milestone commits.

> Per CONTEXT D-15 / D-16: 4-candidate framing intentional (NO MintPy SBAS as 5th candidate). Brief lives in `.planning/milestones/v1.1-research/` (the canonical home for forward-looking research artifacts). Author = Claude (research output); user reviews + greenlights before commit per CONTEXT D-16.

## Context

Phase 4 of v1.1 ran the existing dolphin/PHASS pipeline against:

- **N.Am. SoCal** `t144_308029_iw1` — 15 epochs Jan-Jun 2024, 14 sequential 12-day IFGs (POEORB-only era)
- **EU Bologna** `t117_249422_iw2` — 19 epochs Jan-Jun 2021, 18 sequential pairs (S1A+S1B cross-constellation, fully POEORB-era)

Both runs use cached CSLCs from `eval-disp/cslc/` and `eval-disp-egms/cslc/`; no SAFE re-downloads. Phase 4 changes are validation-side only (adapter, ramp-fit, separated metrics) — the dolphin/PHASS/MintPy pipeline algorithm is unchanged per CONTEXT D-20.

### Reference-agreement (block_mean adapter, fresh)

| Cell | r | bias_mm_yr | RMSE_mm_yr | n | Verdict |
|------|---|------------|------------|---|---------|
| N.Am. SoCal vs OPERA DISP-S1 30m | <NAM_R> | <NAM_BIAS> | <NAM_RMSE> | <NAM_N> | FAIL (r > 0.92, bias < 3 mm/yr) |
| EU Bologna vs EGMS L2a PS | <EU_R> | <EU_BIAS> | <EU_RMSE> | <EU_N> | FAIL |

(Continuity note: v1.0 numbers using `Resampling.bilinear` were r=0.0365 / bias=+23.62 mm/yr (N.Am.) and r=0.32 / bias=+3.35 mm/yr (EU). The block_mean numbers above are the conservative-kernel official metric per CONTEXT D-02 + `docs/validation_methodology.md` §3.)

### Product-quality (CALIBRATING, separately reported per CONTEXT D-08 + D-19)

| Cell | coh_med_of_persistent | coh_source | residual_mm_yr | cell_status |
|------|----------------------|------------|----------------|-------------|
| N.Am. SoCal | <NAM_COH> | phase3-cached | <NAM_RESID> | <NAM_CELL_STATUS> |
| EU Bologna | <EU_COH> | fresh | <EU_RESID> | <EU_CELL_STATUS> |

Coherence is computed at native 5×10 m on stable terrain (Phase 3 D-09 mask: WorldCover class 60 + slope < 10° + coast buffer 5 km + water buffer 500 m). Residual is computed fresh from dolphin's `velocity.tif` per CONTEXT D-08 (the residual is what we're validating; never cross-cell-read).

### Ramp attribution (CONTEXT D-09..D-12)

| Cell | mean_magnitude_rad | direction_sigma_deg | r(magnitude, coherence) | n_ifgs | attributed_source |
|------|-------------------|--------------------|-------------------------|--------|-------------------|
| N.Am. SoCal | <NAM_RAMP_MEAN> | <NAM_RAMP_SIGMA> | <NAM_RAMP_R> | <NAM_NIFG> | `<NAM_ATTR>` |
| EU Bologna | <EU_RAMP_MEAN> | <EU_RAMP_SIGMA> | <EU_RAMP_R> | <EU_NIFG> | `<EU_ATTR>` |

Per CONTEXT D-09, only diagnostic (a) per-IFG planar-ramp fit ran. Diagnostic (b) POEORB swap is no-op-on-current-stacks (both cells fully POEORB-era). Diagnostic (c) ERA5 toggle requires `pyaps3 >= 0.3.6` + valid `~/.cdsapirc` (DISP-V2-02 in REQUIREMENTS, deferred per CONTEXT D-09 + folded into Unwrapper Selection follow-up as secondary).

## Candidate approaches

| # | Approach | Description | Success criterion | Compute tier | Dep delta |
|---|----------|-------------|-------------------|--------------|-----------|
| 1 | **PHASS + post-deramping** | Keep PHASS unwrapper; subtract per-IFG planar-ramp fit BEFORE dolphin's network inversion. Re-runs from cached unwrapped IFGs. | r > 0.5 OR ramp magnitude < 1.0 rad on all IFGs (current EU ramp ~<EU_RAMP_MEAN> rad; cf. PHASS-class signature in `<NAM_ATTR>`/`<EU_ATTR>` labels) | **S** — post-process only (~5 min/cell, M3 Max) | numpy lstsq (already in dolphin); no new conda packages |
| 2 | **SPURT native** | Switch dolphin's `unwrap_method` from PHASS to SPURT (graph-based unwrapper for large grids, designed for OPERA DISP). One-line config. | r > 0.7 (intermediate target) AND `auto_attribute_ramp` returns `'inconclusive'` (no PHASS-class planar ramps) | **M** — re-unwrap from cached IFGs (~30 min/cell) | None (SPURT is shipped with dolphin 0.42.5 via conda-forge) |
| 3 | **tophu + SNAPHU multi-scale tiled** | Use tophu's tile-based decomposition (3×3 tiles, 30 m downsample factor) wrapping SNAPHU. Empirically validated for OPERA DISP large-scale. | r > 0.85 AND PASS reference-agreement gate on at least one of (N.Am., EU) | **L** — re-unwrap from cached IFGs at multi-scale (~60 min/cell) | tophu 0.2.1 + snaphu 0.4.1 conda-forge (already in conda-env.yml; v1.0 had hand-pip-install issue resolved in Phase 1 ENV-02) |
| 4 | **20×20 m fallback multilook** | Multilook CSLC stack to 20×20 m BEFORE phase linking. Reduces per-IFG pixel count by ~6× (5×10 → 20×20), makes any unwrapper tractable. | PASS reference-agreement gate at 30 m comparison after multilook (PASS = the v1.0 PROJECT.md bar of r > 0.92, bias < 3) | **L** — re-run dolphin from CSLC stack with downsample option (~3 h/cell cold) | None (dolphin native multilook config); only adds compute |

## Per-candidate notes

### 1. PHASS + post-deramping

The cheapest of the 4 — runs entirely on already-cached unwrapped IFGs. If the ramp-attribution label is `phass` (cell-by-cell PHASS-class signature), removing the ramp BEFORE network inversion may bring r above the FAIL threshold without re-unwrapping.

**Risk:** if the ramp signal contains real long-wavelength deformation (e.g., interseismic strain accumulation), deramping discards signal alongside artefact. SoCal is a tectonic AOI; this needs a sanity check against Nevada Geodetic Lab GPS residuals (CSLC-V2-01 future work).

### 2. SPURT native

If the PHASS-class label drives a real-vs-artefact decision per (1), SPURT becomes the production fix. dolphin 0.42.5's `unwrap_method='spurt'` is a one-line config switch in `run_disp.py`'s dolphin invocation. SPURT's graph-based unwrapping is the OPERA-recommended approach for large grids (PITFALLS / FEATURES alignment).

**Risk:** SPURT may not converge on all 14-18 IFGs in current cell stacks; failures need a fall-back path (PHASS or candidate 3).

### 3. tophu + SNAPHU multi-scale tiled

The compute-heaviest of the network-inversion candidates. tophu's tiling (3×3 spatial tiles + 30 m downsample) is what OPERA DISP uses internally; a faithful subsideo replication should match. v1.0 had a SNAPHU pip-install issue (CONCLUSIONS_DISP_N_AM.md §5.2 hung at integration); Phase 1 ENV-02 conda-forge resolution removes the historical blocker.

**Risk:** tophu is the heaviest compute path (~60 min/cell). Worth running only if (1) and (2) both FAIL.

### 4. 20×20 m fallback multilook

The cleanest "give up on native resolution but get an honest PASS" path. Multilooking CSLC stack to 20×20 m before phase linking reduces aliasing risk and makes any unwrapper tractable.

**Risk:** Pre-commits subsideo to a 4× resolution downgrade. Acceptable for a v1.2 release if (1)/(2)/(3) all FAIL their criteria, but should be the last fallback per CONTEXT 04-CONTEXT.md §Specifics ("the FAIL is the intended signal" — not a permission to give up on native).

## Attribution-driven prioritisation (per CONTEXT D-14)

Phase 4 ramp_attribution labels:
- N.Am. SoCal: `<NAM_ATTR>`
- EU Bologna: `<EU_ATTR>`

Decision tree:
- If both 'phass' → all 4 candidates remain viable; **prioritise (1) for fastest signal, (2) for production fix.**
- If 'orbit' → candidates (1)+(2) become lower-priority (don't address orbit-state-vector errors); diagnostic (b) POEORB swap re-prioritises if the future window has RESORB epochs.
- If 'tropospheric' → all candidates lower-priority; diagnostic (c) ERA5 toggle re-prioritises (DISP-V2-02).
- If 'mixed' or 'inconclusive' → diagnostic (b) + (c) BEFORE candidate evaluation.

The brief author's recommendation: **start with (1) PHASS + post-deramping** (S compute tier; ~5 min/cell). If (1) achieves r > 0.5 on either cell, that's the cheapest possible v1.2 fix and a clean deramping baseline for evaluating (2)/(3)/(4). If (1) FAILS its success criterion, jump to (2) SPURT native.

## Out of scope for this brief

- **MintPy SBAS as 5th candidate** — intentional 4-candidate framing per CONTEXT D-15. Could be added in the follow-up milestone if (1)+(2)+(3)+(4) all FAIL their per-candidate success criteria.
- **ERA5 tropospheric correction integration** — DISP-V2-02 in REQUIREMENTS; folded into the follow-up milestone as secondary (per CONCLUSIONS_DISP_EGMS / EU §6 candidate-mitigations).
- **Production unwrapper choice** — this brief proposes; the follow-up milestone commits.
- **Specific candidate threshold values** — the success criteria above are first-pass targets derived from the FAIL numbers in the §Context tables. The follow-up milestone tunes these against measured candidate r/bias.

## Open questions (for the v1.2 milestone roadmapper)

1. **Diagnostic (c) ERA5 toggle:** does the v1.2 milestone include integration of `pyaps3 >= 0.3.6` ERA5 download path (CDS API key required), or does it stay deferred? CONTEXT D-09 says "available; deferred"; the follow-up milestone may absorb DISP-V2-02 here.
2. **Candidate ordering after attribution:** if both cells are `phass`, the brief recommends (1) → (2) → (3) → (4). If either cell is `mixed` or `inconclusive`, do we order differently?
3. **Held-out test cell:** v1.2 should validate on a cell NOT in v1.1 (neither SoCal nor Bologna) to avoid overfitting candidate selection. Candidate AOIs: a SoCal winter window (forces RESORB epochs → unlocks diagnostic b), or an N. African desert burst (low coherence; tests SPURT robustness).
4. **Production resolution decision:** if (4) 20×20 m fallback achieves PASS, is the production default downgraded to 20×20 m, or kept at native 5×10 m with the multilook as a validation-only fallback (mirroring `prepare_for_reference` validation-only discipline)?

## Sources

- v1.0 CONCLUSIONS: `CONCLUSIONS_DISP_N_AM.md` §5.1 (PHASS planar-ramp discovery), `CONCLUSIONS_DISP_EU.md` §4.3 (38 of 38 IFGs flagged).
- Phase 4 metrics.json: `eval-disp/metrics.json`, `eval-disp-egms/metrics.json`.
- Phase 4 methodology: `docs/validation_methodology.md` §3 (multilook ADR — Phase 4 D-03).
- Research: `.planning/research/PITFALLS.md` §P3.1 / §P3.2; `.planning/research/FEATURES.md` lines 67-76 / 142-143.
- v1.1 REQUIREMENTS: DISP-V2-01 (this brief is the v1.1 deliverable that scopes the V2 milestone), DISP-V2-02 (ERA5 tropospheric correction, secondary in follow-up).

---

**As of 2026-04-25, this brief is the v1.1 closure handoff. The follow-up milestone roadmapper consumes it as input when v1.2 (or whichever version) starts.**
```

Substitute all `<NAM_*>` and `<EU_*>` placeholders with actual numerical values from the Plan 04-04 metrics.json files. Do NOT leave placeholders in the committed brief.

After substitution, the brief should be ≈ 150-180 LOC. If it grows past 250 LOC, trim — single dense markdown page per CONTEXT D-15.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md && wc -l .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md`.
    - `grep -c "PHASS + post-deramping" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1.
    - `grep -c "SPURT native" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1.
    - `grep -c "tophu" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1.
    - `grep -c "20.20 m" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1.
    - `grep -c "MintPy SBAS" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1 (in the §"Out of scope" sub-section ONLY — verifying the explicit no-5th-candidate framing per D-15).
    - `grep -c "Success criterion" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1 (column header in the candidate table).
    - `grep -c "Compute tier" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1 (column header).
    - `grep -c "Dep delta" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1 (column header).
    - `! grep -E '<(NAM|EU)_' .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` (no placeholder strings remain).
    - `wc -l .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns a value between 100 and 280 (single dense page; 150-250 target).
    - `grep -c "DISP-V2-01" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1 (REQUIREMENTS trace to follow-up milestone).
    - `grep -c "v1.0" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md` returns ≥ 1 (continuity note with v1.0 baseline numbers).
  </acceptance_criteria>
  <done>
    DISP_UNWRAPPER_SELECTION_BRIEF.md exists at .planning/milestones/v1.1-research/. Single dense page (~150-250 LOC) with 4-candidate framing × 4 columns (description, success criterion, compute tier, dep delta). Cites both cells' fresh FAIL numbers + ramp aggregate + attributed_source from Plan 04-04 metrics.json. Explicitly excludes MintPy SBAS as 5th candidate per CONTEXT D-15.
  </done>
</task>

<task type="auto">
  <name>Task 4: Append §3 multilook method ADR to docs/validation_methodology.md (5-part PITFALLS+FEATURES dialogue)</name>
  <files>docs/validation_methodology.md</files>
  <read_first>
    - /Volumes/Geospatial/Geospatial/subsideo/docs/validation_methodology.md (entire — 247 LOC; lines 1-15 module-level append-only convention; lines 17-129 §1 structure pattern; lines 234-241 cross-section reference table)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/research/PITFALLS.md §P3.1 (lines 391-417) — Gaussian σ=0.5×ref physics argument
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/research/FEATURES.md (lines 142-143 anti-feature multilook framing)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-CONTEXT.md (D-03 §3 5-part structure)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-RESEARCH.md (lines 1408-1496 verbatim §3 doc skeleton)
    - /Volumes/Geospatial/Geospatial/subsideo/.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-PATTERNS.md (lines 690-772 docs/validation_methodology.md append-only pattern)
  </read_first>
  <action>
APPEND §3 to `docs/validation_methodology.md` after the existing §2 (verified ends at line 247). Phase 4 owns §3 ONLY per Phase 3 D-15 append-only doc policy — do NOT add §4 (DSWE F1 ceiling — Phase 5/6) or §5 (cross-sensor precision-first — Phase 5/7).

Append the following content VERBATIM to the end of the file (preserve any trailing newline):

```markdown

---

## 3. DISP comparison-adapter design — multilook method choice

<a name="multilook-method"></a>

**TL;DR:** `subsideo.validation.compare_disp.prepare_for_reference()` requires
an explicit `method=` argument selected from
`Literal["gaussian", "block_mean", "bilinear", "nearest"]`. Phase 4 eval scripts
pass `method="block_mean"` -- the conservative kernel that doesn't inflate
reference-agreement r through kernel artefact. Switching the kernel
post-measurement requires CONCLUSIONS-level documentation per the
criterion-immutability principle.

### 3.1 Problem statement

subsideo's native DISP velocity is at 5 m × 10 m posting (OPERA CSLC spec).
The two reference products it must compare against -- OPERA DISP-S1
(30 m × 30 m) and EGMS L2a PS (point cloud at PS coordinates) -- live on
different grids. Comparing requires multilooking subsideo's velocity onto the
reference grid. The multilook method is a comparison-adapter design choice
that materially changes the reported `correlation` and `bias_mm_yr` values
(PITFALLS P3.1 warning sign: r differs by > 0.03 when switching between
kernels on the same data).

Native production output stays at 5×10 m (DISP-05 + ROADMAP key decision
"Native CSLC/DISP resolution stays production default"). The adapter is
**validation-only** infrastructure -- it never writes back to the product
(Phase 4 D-17 + research ARCHITECTURE §3 placement rationale).

### 3.2 PITFALLS P3.1 argument — Gaussian σ=0.5×reference is physically consistent

PITFALLS P3.1 (`.planning/research/PITFALLS.md` §P3.1) argues that OPERA's
30 m DISP output is itself produced from multilooked interferograms with an
effective Gaussian smoothing kernel. Multilooking subsideo's 5×10 m output
to 30 m via `gaussian_filter(σ_pix = 15m / native_spacing)` then nearest-
sampling onto the reference grid is the apples-to-apples comparison: both
fields carry the same effective spatial-frequency content. Block-mean (the
"averaging" rasterio resampler) under-smooths by truncating high frequencies
discretely rather than rolling them off; bilinear smooths but blurs
discontinuities; nearest preserves sub-pixel offsets and aliases.

**The argument's strength:** kernel matching is the standard practice in
remote-sensing validation when the reference is itself a multi-look product
(SAR / hyperspectral / PSI). The physics of velocity-difference power
across spatial frequencies says that if our product and the reference both
carry the same low-pass response, the comparison r is the apples-to-apples
"how well do these chains agree at the comparison cell size" number.

### 3.3 FEATURES anti-feature argument — block_mean is the kernel-flattery floor

FEATURES (`.planning/research/FEATURES.md` lines 71 + 142-143 anti-feature
table) argues the inverse: kernel choice can inflate reported r because
each kernel rolls off velocity-difference power differently. Picking the
kernel that gives the highest r is a kernel-choice attack surface -- the
reported r in `results/matrix.md` becomes a function of "we picked the
nicest kernel" rather than "the chains agree at the comparison cell size."
Block-mean (`Resampling.average` in rasterio) is the conservative floor:
it matches what OPERA itself uses for its CSLC multilook, and its
high-frequency truncation is the most pessimistic of the four kernels for
reference-agreement r. Anyone arguing Gaussian gives a higher r can rerun
with `method="gaussian"`; we don't pre-commit to the optimistic kernel for
the published metric.

**The argument's strength:** as a milestone-publish artifact (not a paper),
the kernel choice that gives the lower-bound r is the one we ship as the
official number. The anti-feature framing names this trade-off explicitly
and resists the M1 target-creep anti-pattern (don't tighten OR relax
criteria based on the resulting measurement).

### 3.4 Decision: block_mean as the eval-script default

`subsideo.validation.compare_disp.prepare_for_reference()` accepts all four
kernels -- both arguments above are correct on their own terms; the choice
is a posture, not a science argument (see Phase 4 04-CONTEXT.md §Specifics).
The **eval-script default** is `block_mean` because:

1. **Floor behaviour** -- block_mean's reported r is the most pessimistic
   of the four. If we PASS at block_mean, we PASS at any kernel; if we FAIL
   at block_mean, the FAIL is unambiguous (not a kernel artefact).
2. **OPERA parity** -- OPERA's own multilook in the CSLC pipeline is a
   block-average. Same kernel = same effective smoothing = honest comparison
   of two block-averaged products.
3. **No goalpost-moving (M1)** -- switching to Gaussian post-measurement
   because it gives a higher r is exactly the M1 target-creep anti-pattern
   `criteria.py` is designed to prevent. A future PR titled "switch to
   Gaussian for higher r" would be self-evidently wrong.

The eval-script constant `REFERENCE_MULTILOOK_METHOD: Literal["block_mean"] =
"block_mean"` lives at module top in both `run_eval_disp.py` and
`run_eval_disp_egms.py` (Phase 4 D-04, mirroring Phase 1 D-11
`EXPECTED_WALL_S` pattern). The supervisor AST-parses it; switching the
kernel requires a visible PR diff to the constant, not a silent runtime
change.

### 3.5 Constraint: kernel choice is comparison-method, not product-quality

The kernel choice is **a comparison-method decision**, NOT a product-quality
decision. **Native 5×10 m stays the production default** (DISP-05 in
REQUIREMENTS; ROADMAP key decision; Phase 4 D-17). The eval script's
`REFERENCE_MULTILOOK_METHOD` constant lives at module top so it's auditable
in the git diff (mirrors the Phase 1 D-11 `EXPECTED_WALL_S` pattern).
Switching kernels post-measurement requires:

1. A PR diff to the constant (visible in
   `git log --grep="REFERENCE_MULTILOOK_METHOD"`).
2. A CONCLUSIONS sub-section documenting the new kernel's measured r/bias
   and citing this §3.5 constraint.

There is no env-var override or CLI flag. The kernel is a published-artifact
parameter; it must be code-visible.

(For the alternative-kernel rerun e.g. `method="gaussian"` for kernel-
comparison study: it's deferred to the Unwrapper Selection follow-up
milestone per Phase 4 04-CONTEXT.md §Deferred. CONCLUSIONS may cite
"block_mean reference shows r=X; the same data with method='gaussian' would
yield r=Y" as a v1.2/v2 footnote.)
```

**Critical preservation:** do NOT modify §1 (CSLC cross-version phase impossibility) or §2 (Product-quality vs reference-agreement distinction). Do NOT add a §4 or §5 stub. Phase 4 owns §3 ONLY per Phase 3 D-15 append-only.

Update the §2.6 cross-section reference table (lines 234-241) ONLY to mark §3 as "now landed" — find lines like:
```
| DISP ramp-attribution (reference-agreement diagnostic depth) | Phase 4 will append (PHASS N.Am./EU re-runs as authoritative evidence) |
```

If such a row exists, change "Phase 4 will append" to "Phase 4 landed in §3 (this doc)" so the cross-reference is current. If the existing §2.6 table doesn't reference DISP, leave §2.6 unchanged.
  </action>
  <verify>
    <automated>grep -cE "^## 3\\." docs/validation_methodology.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 3\\. DISP comparison-adapter design" docs/validation_methodology.md` returns 1.
    - `grep -c "^### 3.1 Problem statement" docs/validation_methodology.md` returns 1.
    - `grep -c "^### 3.2 PITFALLS P3.1 argument" docs/validation_methodology.md` returns 1.
    - `grep -c "^### 3.3 FEATURES anti-feature argument" docs/validation_methodology.md` returns 1.
    - `grep -c "^### 3.4 Decision: block_mean" docs/validation_methodology.md` returns 1.
    - `grep -c "^### 3.5 Constraint:" docs/validation_methodology.md` returns 1.
    - `grep -c "REFERENCE_MULTILOOK_METHOD" docs/validation_methodology.md` returns ≥ 1.
    - `grep -c "Native 5.10 m stays" docs/validation_methodology.md` returns ≥ 1 (DISP-05 explicit statement, regex `5.10` matches both `5x10` and `5×10`).
    - `grep -cE "^## 1\\. CSLC cross-version" docs/validation_methodology.md` returns 1 (§1 unchanged from Phase 3).
    - `grep -cE "^## 2\\. " docs/validation_methodology.md` returns 1 (§2 unchanged from Phase 3).
    - `grep -cE "^## 4\\. " docs/validation_methodology.md` returns 0 (Phase 4 owns §3 ONLY; §4 is Phase 5/6).
    - `grep -cE "^## 5\\. " docs/validation_methodology.md` returns 0 (§5 is Phase 5/7).
    - `wc -l docs/validation_methodology.md` returns ≥ 320 (was 247; §3 adds ≥70 lines).
    - File still parses as markdown: `head -1 docs/validation_methodology.md` returns "# Validation Methodology".
  </acceptance_criteria>
  <done>
    docs/validation_methodology.md has §3 appended (5-part PITFALLS+FEATURES dialogue per CONTEXT D-03). §1 and §2 untouched. No §4 or §5 added (Phase 3 D-15 append-only by phase). REFERENCE_MULTILOOK_METHOD constant cross-referenced; "Native 5×10 m stays production default" explicit per DISP-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer ↔ docs | Documentation files are reviewed by user before commit per CONTEXT D-16 (brief specifically) |
| brief ↔ metrics.json | Brief reads numerical values from Plan 04-04 metrics.json files; Pydantic validation in Plan 04-01 already guarantees those files are well-formed |
| CONCLUSIONS ↔ git history | git mv preserves history via --follow; v1.0 narrative remains accessible from CONCLUSIONS_DISP_EU.md history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | T (Tampering) | brief content | accept | Brief is committed only after user review per CONTEXT D-16; tampered content surfaces in PR review. |
| T-04-05-02 | I (Information disclosure) | docs § content | accept | All numerical values cited are already public (criteria.py thresholds, measured FAIL numbers in metrics.json which is committed under v1.1 disclosure). No PII; no credentials. |
| T-04-05-03 | R (Repudiation) | git mv vs git rm + git add | mitigate | Use `git mv` (not `rm` + `add`) so git tracks the rename; `git log --follow CONCLUSIONS_DISP_EU.md` traces back to v1.0 history. Verifiable in `git log` post-commit. |
| T-04-05-04 | T (Tampering) | docs §3 vs Phase 5/6/7 §4/§5 | mitigate | Phase 3 D-15 append-only by phase enforced via acceptance-criteria grep checks (§4/§5 must NOT exist). Future Phase 5/6/7 phases own those sections. |
</threat_model>

<verification>
Plan-level verification (run after all 4 tasks complete):

```bash
# Rename happened cleanly
test -f CONCLUSIONS_DISP_EU.md
! test -f CONCLUSIONS_DISP_EGMS.md
git log --follow --oneline -- CONCLUSIONS_DISP_EU.md | head -3

# Both CONCLUSIONS files have all 4 v1.1 sections
for f in CONCLUSIONS_DISP_N_AM.md CONCLUSIONS_DISP_EU.md; do
  echo "=== $f ==="
  grep -cE "^## 1[1-4]\\." "$f"  # expect 4 (sections 11, 12, 13, 14)
  grep -c "DISP_UNWRAPPER_SELECTION_BRIEF.md" "$f"  # expect ≥ 1
done

# Brief exists and has 4 candidates
test -f .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md
for c in "PHASS + post-deramping" "SPURT native" "tophu" "20.20 m"; do
  grep -c "$c" .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md
done

# Methodology doc has §3 + does NOT have §4/§5
grep -c "^## 3\\. DISP comparison-adapter design" docs/validation_methodology.md  # expect 1
grep -cE "^## 4\\. " docs/validation_methodology.md  # expect 0
grep -cE "^## 5\\. " docs/validation_methodology.md  # expect 0

# No placeholders anywhere
! grep -rE '<(NAM|EU)_[A-Z_]+>' CONCLUSIONS_DISP_N_AM.md CONCLUSIONS_DISP_EU.md .planning/milestones/v1.1-research/DISP_UNWRAPPER_SELECTION_BRIEF.md
```
</verification>

<success_criteria>
- CONCLUSIONS_DISP_EGMS.md renamed to CONCLUSIONS_DISP_EU.md via git mv (history preserved)
- Both CONCLUSIONS files have v1.0 narrative as preamble + 4 v1.1 sub-sections appended
- DISP_UNWRAPPER_SELECTION_BRIEF.md exists at .planning/milestones/v1.1-research/ with 4 candidates × 4 columns
- docs/validation_methodology.md has §3 appended; no §4 or §5 (Phase 3 D-15 append-only)
- All numerical values substituted from Plan 04-04 metrics.json files (no `<NAM_*>` / `<EU_*>` placeholders)
- Brief explicitly excludes MintPy SBAS as 5th candidate (D-15)
- Native 5×10 m stays production default explicitly stated in §3.5 (DISP-05)
- v1.0 baseline numbers preserved in CONCLUSIONS as continuity citation
</success_criteria>

<output>
After completion, create `.planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-05-SUMMARY.md` per the template, listing:
- The git mv operation (CONCLUSIONS_DISP_EGMS.md → CONCLUSIONS_DISP_EU.md)
- The 4 v1.1 sub-sections appended to each CONCLUSIONS file
- The brief location + the 4 candidates' success criteria as committed
- The §3 ADR doc-section structure (5 sub-sections)
- Phase 4 closure verdict (cell statuses for both cells; whether the brief is user-greenlit or pending)
</output>
</content>
</invoke>