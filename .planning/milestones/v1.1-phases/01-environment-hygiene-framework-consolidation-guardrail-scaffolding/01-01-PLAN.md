---
phase: 01-environment-hygiene-framework-consolidation-guardrail-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - conda-env.yml
autonomous: true
requirements: [ENV-01, ENV-02, ENV-09, ENV-10]

must_haves:
  truths:
    - "Running `micromamba env create -f conda-env.yml` on a clean machine produces a working subsideo env with Python 3.11, numpy<2, isce3 0.25.10, tophu 0.2.1, dist-s1 2.0.14, rio-cogeo 6.0.0, and py-spy 0.4.1 — all without a second pip install step"
    - "`micromamba run -n subsideo python -c 'import numpy, tophu, rio_cogeo, dist_s1, py_spy; print(numpy.__version__, tophu.__version__, rio_cogeo.__version__, dist_s1.__version__)'` prints versions matching the pins (numpy <2, tophu 0.2.1, rio-cogeo 6.0.0, dist-s1 2.0.14)"
    - "`micromamba run -n subsideo python -c 'import tophu'` succeeds (tophu resolves via conda-forge, not PyPI 404)"
    - "`conda-env.yml` declares py-spy on the conda-forge layer (ops diagnostic — zero pyproject.toml churn per D-15)"
  artifacts:
    - path: "conda-env.yml"
      provides: "Two-layer environment manifest: conda-forge heavies + pip editable install of subsideo[validation,viz] + rio-cogeo==6.0.0"
      contains: "numpy>=1.26,<2.0, tophu=0.2.1, dist-s1=2.0.14, rio-cogeo==6.0.0, py-spy=0.4.1, -e .[validation,viz]"
  key_links:
    - from: "conda-env.yml"
      to: "pyproject.toml [project.optional-dependencies] validation + viz"
      via: "pip: section with `-e .[validation,viz]`"
      pattern: "pip:\\s*\\n\\s*-\\s*-e \\.\\[validation,viz\\]"
    - from: "conda-env.yml numpy<2 pin"
      to: "Plan 01-03 cslc.py patch removal"
      via: "pin eliminates compat shims required under numpy>=2"
      pattern: "numpy>=1\\.26,<2\\.0"
---

<objective>
Author a single two-layer `conda-env.yml` at repo root that, on a fresh clone, yields a working subsideo environment via `micromamba env create -f conda-env.yml` with zero additional pip install steps. This is the foundation Wave 1 deliverable: every downstream plan in Phase 1 assumes these pins are in place. The file carries (a) all conda-forge binary deps at pinned versions, (b) `py-spy=0.4.1` for the watchdog diagnostic added in Plan 07, (c) a trailing `pip:` section that installs the project editable with `[validation,viz]` extras and the frozen `rio-cogeo==6.0.0` pin (7.x drops Python 3.10 support — research STACK §3 correction to BOOTSTRAP).

Purpose: ENV-01 + ENV-02 (numpy side) + ENV-09 (env deps for supervisor) + ENV-10 (env.yml half of the reproducibility recipe; lockfiles + Docker come in Plan 01-09).

Output: one new file, `conda-env.yml` at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-hygiene-framework-consolidation-guardrail-scaffolding/01-CONTEXT.md
@.planning/phases/01-environment-hygiene-framework-consolidation-guardrail-scaffolding/01-RESEARCH.md
@.planning/phases/01-environment-hygiene-framework-consolidation-guardrail-scaffolding/01-PATTERNS.md
@pyproject.toml
@CLAUDE.md

<interfaces>
<!-- Contract extracted from RESEARCH.md Example 8 (§A, D-15) and STACK §Standard Stack. Executor uses these version pins verbatim — verified 2026-04-21 against conda-forge + PyPI. -->

pyproject.toml optional-dependencies (read-only; Plan 01 does NOT edit pyproject.toml):
- `validation`: opera-utils[disp]>=0.25, pandas>=2.2, scikit-image>=0.22, statsmodels>=0.14
- `viz`: matplotlib>=3.8, cartopy>=0.22, folium>=0.16, hvplot>=0.10, ipywidgets>=8.1, jupyter>=1.0

Verified version pins (as of 2026-04-21):
- conda-forge tophu: latest 0.2.1 — VERIFIED `curl api.anaconda.org/package/conda-forge/tophu`
- conda-forge dist-s1: latest 2.0.14 — VERIFIED
- conda-forge py-spy: latest 0.4.1 — VERIFIED
- PyPI rio-cogeo: latest 7.0.2 (do NOT use; STAY on 6.0.0) — VERIFIED

Research STACK §1 sunset note (must appear as comment at top of conda-env.yml):
"Remove numpy<2 pin when isce3 ≥ 0.26 + compass numpy-2 + s1reader numpy-2 are all on conda-forge"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write conda-env.yml with two-layer pinning</name>
  <read_first>
    - conda-env.yml (may not exist yet — confirm with `ls conda-env.yml`; if it exists, read it before overwriting)
    - pyproject.toml (read `[project.optional-dependencies]` `validation` + `viz` lines 120-131 + 111-118 — to confirm the extras exist and cover what conda-env.yml's pip layer delegates to)
    - .planning/phases/01-environment-hygiene-framework-consolidation-guardrail-scaffolding/01-CONTEXT.md (§D-15 two-layer structure, §D-17 no CUDA layer, §Deferred — no conda-lock)
    - .planning/phases/01-environment-hygiene-framework-consolidation-guardrail-scaffolding/01-RESEARCH.md §Standard Stack + Example 8 (verbatim canonical `conda-env.yml` contents)
    - CLAUDE.md (subsideo build conventions, two-layer install reminder — never pip install isce3/dolphin/tophu)
  </read_first>
  <files>conda-env.yml</files>
  <action>
Create `conda-env.yml` at the repo root (absolute path: `/Volumes/Geospatial/Geospatial/subsideo/conda-env.yml`). The file MUST contain exactly the structure below — no deviations, no extra deps, no CUDA layer (D-17), no conda-lock directives (D-deferred). Use the Write tool, not heredoc.

```yaml
# conda-env.yml — subsideo v1.1 environment (Phase 1 ENV-01 / D-15)
#
# Install: `micromamba env create -f conda-env.yml`
# The pip: layer installs subsideo editable + the validation/viz extras from
# pyproject.toml; no second pip install is required after env create.
#
# Sunset conditions (re-evaluate at each milestone):
#   numpy<2.0           -> isce3 >= 0.26 + compass numpy-2 + s1reader numpy-2 all on conda-forge (ENV-02 sunset)
#   rio-cogeo==6.0.0    -> subsideo baseline Python bumps to >=3.11 (v1.2 scope)
#   tophu (conda-forge) -> dolphin >=0.43 makes tophu optional AND tophu ships to PyPI
#
name: subsideo
channels:
  - conda-forge
  - nodefaults
dependencies:
  # Env-name note: on the host (macOS dev / Linux TrueNAS), `micromamba env create
  # -f conda-env.yml` creates an env literally named `subsideo` per the `name:`
  # field above. Inside the Docker image built in Plan 01-09, `micromamba install
  # -y -n base -f conda-env.yml` installs the same package set into the pre-
  # existing `base` env supplied by mambaorg/micromamba — both envs are package-
  # identical; only the name differs. See Plan 01-09 Task 2 for details.
  #
  # -- Runtime ------------------------------------------------
  - python=3.11
  - numpy>=1.26,<2.0        # Phase 1 ENV-01/02 pin (removes 4 cslc.py monkey-patches)
  - scipy>=1.14,<2
  - pip

  # -- SAR / InSAR core (conda-forge only; NEVER pip) ---------
  - isce3=0.25.10
  - compass=0.5.6
  - s1reader=0.2.5
  - dolphin=0.42.5
  - tophu=0.2.1             # research STACK §2 correction: conda-forge only (PyPI 404)
  - snaphu-py=0.4.1
  - mintpy=1.6.3
  - dist-s1=2.0.14          # bump from 2.0.13 (per STATE.md Phase 1 corrections)

  # -- Raster / vector I/O ------------------------------------
  - gdal>=3.8
  - rasterio>=1.5,<2
  - rioxarray>=0.22,<1
  - geopandas>=1.0,<2
  - shapely>=2.0,<3
  - pyproj>=3.7,<4
  - xarray>=2024.11
  - h5py>=3.12,<4
  - scikit-image>=0.24

  # -- DEM + orbits -------------------------------------------
  - dem-stitcher>=2.5,<3
  - sentineleof>=0.11,<1

  # -- Tropospheric correction --------------------------------
  - pyaps3>=0.3.6
  - cdsapi

  # -- Dev + test --------------------------------------------
  - pytest>=8
  - pytest-cov
  - pytest-mock
  - ruff
  - mypy
  - pre-commit

  # -- Ops (Phase 1 watchdog stack dumps; D-13) ---------------
  - py-spy=0.4.1

  # -- Pip layer (pure Python on top of conda) ----------------
  - pip:
      - -e .[validation,viz]
      - rio-cogeo==6.0.0    # Phase 1 ENV-03 pin; 7.x drops Python 3.10
```

Details that MUST be honoured (do NOT change in any way):
- `name: subsideo` — matches CLAUDE.md, existing documentation references.
- `channels: [conda-forge, nodefaults]` — prevents accidental `defaults` pulls that cause ABI drift.
- `python=3.11` (exact `=` pin, not `>=`) — isce3 0.25.10 conda-forge builds target 3.10-3.11; 3.12 not yet tested.
- `numpy>=1.26,<2.0` (two-sided) — MUST be two-sided; a bare `numpy<2.0` accepts numpy 1.20 which breaks rasterio.
- `isce3=0.25.10` — single `=` pin, matches v1.0 baseline.
- `dist-s1=2.0.14` — bump from currently-installed 2.0.13.
- `tophu=0.2.1` — under `dependencies:` (conda-forge), NEVER under `pip:` (doesn't exist on PyPI — STACK §2 correction).
- `rio-cogeo==6.0.0` — under `pip:` with double-equals exact pin (there's no 6.0.0 on conda-forge at time of writing; pip-layer is fine because rio-cogeo is pure Python).
- `py-spy=0.4.1` — under `dependencies:` (conda-forge). Do NOT add py-spy to pyproject.toml (D-15, Open Question 1 recommendation: conda-env.yml only).
- `-e .[validation,viz]` — MUST use the `-e .` editable install; `[validation,viz]` MUST match pyproject.toml extras names exactly (verify via `grep -nE '^(validation|viz)\s*=' pyproject.toml`).
- Do NOT add: `aiohttp`, `aiofiles`, `fiona`, `pyogrio`, `owslib`, `tenacity` (either deferred to v2 or Phase 5 or Claude's Discretion "optional" left to Plan 01-06 if the planner chose tenacity — which they did NOT in this plan set per Open Question 1 recommendation of roll-your-own backoff).
- Do NOT add CUDA, torch, or GPU deps (D-17 CPU-only).

After writing the file, run `micromamba env create --dry-run -f conda-env.yml 2>&1 | tail -40` (or equivalent) to sanity-check that the solver would accept the constraints. If the solver cannot satisfy (e.g. tophu=0.2.1 is unavailable on arm64), stop and report — do NOT relax pins without ADR.
  </action>
  <verify>
    <automated>test -f conda-env.yml && micromamba run -n subsideo python -c "import yaml; y=yaml.safe_load(open('conda-env.yml')); assert y['name']=='subsideo'; assert 'conda-forge' in y['channels']; deps={d if isinstance(d,str) else None for d in y['dependencies']}; assert any(d and d.startswith('numpy>=1.26,<2') for d in deps), 'numpy pin wrong'; assert any(d and d.startswith('tophu=0.2.1') for d in deps), 'tophu pin wrong'; assert any(d and d.startswith('dist-s1=2.0.14') for d in deps), 'dist-s1 bump missing'; assert any(d and d.startswith('py-spy=0.4.1') for d in deps), 'py-spy missing'; pip_section=next((d for d in y['dependencies'] if isinstance(d,dict) and 'pip' in d), None); assert pip_section is not None, 'pip section missing'; assert '-e .[validation,viz]' in pip_section['pip'], 'editable install missing'; assert 'rio-cogeo==6.0.0' in pip_section['pip'], 'rio-cogeo pin wrong'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ls conda-env.yml` succeeds.
    - `grep -E '^\s+-\s+numpy>=1\.26,<2\.0' conda-env.yml` returns exactly 1 line.
    - `grep -E '^\s+-\s+tophu=0\.2\.1' conda-env.yml` returns exactly 1 line.
    - `grep -E '^\s+-\s+dist-s1=2\.0\.14' conda-env.yml` returns exactly 1 line.
    - `grep -E '^\s+-\s+py-spy=0\.4\.1' conda-env.yml` returns exactly 1 line.
    - `grep -E '^\s+-\s+rio-cogeo==6\.0\.0' conda-env.yml` returns exactly 1 line under the pip: layer.
    - `grep -E '^\s+-\s+-e \.\[validation,viz\]' conda-env.yml` returns exactly 1 line.
    - `grep -cE '^\s+-\s+tophu' conda-env.yml` returns `1` (no duplicate pip-layer tophu entry).
    - No `isce3-cuda`, `cudatoolkit`, or `pytorch-cuda` strings appear anywhere in the file.
    - pyproject.toml is NOT modified by this task.
  </acceptance_criteria>
  <done>conda-env.yml exists at repo root with exactly the structure specified; all pins verified; pyproject.toml untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Validate conda-env.yml solves on macOS arm64 (dev machine acceptance)</name>
  <read_first>
    - conda-env.yml (the file written in Task 1)
  </read_first>
  <files>conda-env.yml (read-only)</files>
  <action>
Confirm that the env.yml can be solved end-to-end on the current M3 Max (osx-arm64) dev machine WITHOUT actually destroying the existing env. Run:

```bash
micromamba create --dry-run -n subsideo-phase1-probe -f conda-env.yml 2>&1 | tee /tmp/subsideo-env-probe.log
echo "EXIT=$?"
```

Expected: solver succeeds and prints the full transaction. If it fails, the error will typically be one of:
- `PackagesNotFoundError: tophu=0.2.1` — means conda-forge doesn't have 0.2.1 for osx-arm64 (re-verify via `curl -s https://api.anaconda.org/package/conda-forge/tophu | jq -r '.files[] | .attrs.subdir + "/" + .version' | sort -u | grep osx-arm64`). If 0.2.1 is indeed missing for arm64, stop and report — do NOT relax the pin.
- `Conflict: rasterio requires numpy>=X.Y.Z`, numpy<2.0 causes downgrade chain — re-verify the compiled set; if shapely/rasterio get pulled below their v1.0-baseline, document and proceed (Plan 01-09 will lockfile the exact solve).

If the probe solves cleanly, delete the probe env marker (`micromamba env list | grep subsideo-phase1-probe` — if it was physically created, `micromamba env remove -n subsideo-phase1-probe -y`). Do NOT install the env for real in this task — that happens in Plan 01-09 (reproducibility recipe).

Document the probe result in a Summary note (one-liner): "conda-env.yml solved cleanly on osx-arm64 dry-run" OR "solve conflict: <paste 5-line excerpt>".

This task MUST NOT modify the user's active `subsideo` environment. `--dry-run` is mandatory.
  </action>
  <verify>
    <automated>micromamba create --dry-run -n subsideo-phase1-probe -f conda-env.yml 2>&1 | grep -qiE 'Transaction|All requested packages already installed|would install' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run solver produces a transaction plan (or "already installed" output) without `PackagesNotFoundError` or `Conflict:` in the output.
    - Active `subsideo` env is unchanged: `micromamba run -n subsideo python -c 'import numpy; print(numpy.__version__)'` still reports whatever it reported before Task 1 (Task 1 writes a file; does not install).
    - If probe env was created, it is cleaned up: `micromamba env list | grep -c subsideo-phase1-probe` returns `0`.
  </acceptance_criteria>
  <done>Solver accepts conda-env.yml on osx-arm64 dry-run; no destructive env change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conda-forge → subsideo env | External package index; solver trusts content-addressed conda-forge |
| PyPI (rio-cogeo) → subsideo env | External package index; `==6.0.0` pin prevents unreviewed upgrade |
| User `.env` → subsideo env | Credentials stay out of conda-env.yml; read by scripts at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | conda-env.yml (version drift) | mitigate | Exact `=` pins on isce3/compass/s1reader/tophu/dist-s1/py-spy; `==` pin on rio-cogeo; Plan 01-09 produces per-platform explicit lockfile |
| T-01-02 | Tampering | rio-cogeo upstream (future breaking change) | mitigate | `rio-cogeo==6.0.0` exact pin; sunset comment at top of file cites STACK §3 rationale |
| T-01-03 | Information disclosure | conda-env.yml inadvertently committed credentials | accept | No credential fields in the file; CDSE/Earthdata env vars stay in `.env` only |
| T-01-04 | Denial of service | conda-forge unavailable during `env create` | accept | Deferred to Plan 01-09 lockfile + Dockerfile offline-capable from cached layers |
| T-01-05 | Elevation of privilege | pip editable install running arbitrary setup.py | mitigate | `-e .` limited to the in-tree subsideo package (hatchling backend, no setup.py) |
</threat_model>

<verification>
- `yamllint conda-env.yml` (if available) or `micromamba run -n subsideo python -c 'import yaml; yaml.safe_load(open("conda-env.yml"))'` parses without error.
- All pin-verification grep checks from Task 1 acceptance pass.
- Dry-run solver accepts the manifest on osx-arm64 (Task 2).
- No pyproject.toml edits in this plan (Plan 01-06 leaves pyproject.toml alone per M9 in PATTERNS.md, except for the `reference_agreement` marker added in Plan 01-05).
</verification>

<success_criteria>
- `conda-env.yml` exists at repo root with conda-forge binaries layer + pip layer, all pins verbatim from RESEARCH.md Example 8.
- Solver dry-run on osx-arm64 returns a transaction plan without conflict.
- Downstream plans can reference `conda-env.yml` as the canonical environment manifest (Plans 01-02, 01-03, 01-09 all consume these pins).
- Every one of ENV-01, ENV-02 (numpy side), ENV-09 (supervisor deps), ENV-10 (env.yml portion) has at least one grep-verifiable line in the committed file.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-hygiene-framework-consolidation-guardrail-scaffolding/01-01-SUMMARY.md` using the standard summary template.
</output>
