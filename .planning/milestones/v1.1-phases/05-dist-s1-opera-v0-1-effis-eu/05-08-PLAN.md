---
phase: 05-dist-s1-opera-v0-1-effis-eu
plan: 08
type: execute
wave: 4
depends_on: [05-06, 05-07]
files_modified:
  - run_eval_dist_eu_nov15.py
directories_modified:
  - eval-dist/                    # rename TARGET (renamed FROM existing eval-dist/ which becomes eval-dist-park-fire/) — then re-created fresh and populated by Plan 05-06's `make eval-dist-nam` smoke test (DEFERRED metrics.json + meta.json)
  - eval-dist-park-fire/          # rename DESTINATION (renamed FROM existing eval-dist/) — preserves v1.0 Park Fire cache (dist_output/, opera_reference/, run.log)
  - eval-dist-eu/                 # DELETED (or renamed to eval-dist-eu-v1.0-sept28/ if user chooses preserve-as-baseline alternative in Task 2 Step 3)
  - eval-dist-eu-nov15/           # DELETED (or renamed to eval-dist-eu-v1.0-nov15/ in the preserve alternative)
  - eval-dist_eu/                 # CANONICAL (already created by Plan 05-02; this plan does NOT touch it directly — Plan 05-07's `make eval-dist-eu` populates per-event subdirectories + metrics.json + meta.json before this cleanup runs)
autonomous: false
requirements: [DIST-04, DIST-06]
tags: [cleanup, rename, consolidation, cache-dir, git-mv]

must_haves:
  truths:
    - "After execution, the on-disk directory `eval-dist/` exists and is freshly populated by Plan 05-06's run_eval_dist.py invocation (DEFERRED metrics.json + meta.json); the Park Fire v1.0 cache is preserved at `eval-dist-park-fire/` with all v1.0 contents intact (dist_output/, opera_reference/, run.log)"
    - "After execution, the on-disk directory `eval-dist_eu/` (underscore) exists and contains the 3-event aggregate from Plan 05-07; the v1.0 hyphenated `eval-dist-eu/` and `eval-dist-eu-nov15/` directories no longer exist as separate top-level directories (their relevant contents migrated into eval-dist_eu/aveiro/sept28/ and eval-dist_eu/aveiro/nov15/ respectively, OR git-rm'd if Plan 05-07 already produced fresh equivalents)"
    - "`run_eval_dist_eu_nov15.py` no longer exists as a top-level script (git-rm'd; content migrated into Plan 05-07 aveiro chained_retry sub-stage per CONTEXT D-11 + D-13)"
    - "matrix_manifest.yml is unchanged (no edits — `cache_dir: eval-dist` and `cache_dir: eval-dist_eu` already match the post-cleanup state per Plan 05-05 lines 68-82)"
    - "`make eval-dist-nam` (re-)runs cleanly against the new `eval-dist/` directory, producing `eval-dist/metrics.json` with `cell_status='DEFERRED'`"
    - "`make eval-dist-eu` (re-)runs cleanly against the new `eval-dist_eu/` directory; OR if it was already invoked in Plan 05-07's checkpoint, `eval-dist_eu/metrics.json` exists with `total=3, per_event` non-empty"
    - "All directory-level operations (eval-dist → eval-dist-park-fire rename; eval-dist-eu / eval-dist-eu-nov15 deletion or rename; eval-dist_eu fresh population) are explicitly enumerated in the `directories_modified` frontmatter field — Warning 8 fix from checker iteration 1 ensuring the source-audit cross-check captures cache-dir changes alongside file changes"
  artifacts:
    - path: "eval-dist-park-fire/"
      provides: "v1.0 Park Fire cache preservation (renamed from eval-dist/)"
      contains: "dist_output"
    - path: "eval-dist/"
      provides: "Fresh T11SLT cache populated by Plan 05-06 run_eval_dist.py invocation"
      contains: "metrics.json"
    - path: "eval-dist_eu/"
      provides: "Consolidated 3-event EU cache populated by Plan 05-07 run_eval_dist_eu.py invocation"
      contains: "metrics.json"
  key_links:
    - from: "matrix_manifest.yml"
      to: "eval-dist/metrics.json"
      via: "cache_dir: eval-dist (already locked in matrix_manifest.yml line 72)"
      pattern: "cache_dir: eval-dist$"
    - from: "matrix_manifest.yml"
      to: "eval-dist_eu/metrics.json"
      via: "cache_dir: eval-dist_eu (underscore convention)"
      pattern: "cache_dir: eval-dist_eu"
---

<objective>
Cleanup and consolidate the cache directory structure after Plans 05-06 + 05-07 produce fresh sidecars under the canonical paths. This plan does NOT produce new validation artifacts — it only resolves three on-disk inconsistencies that accumulated from v1.0:

1. **Rename `eval-dist/` → `eval-dist-park-fire/`** (CONTEXT D-05) — preserves the v1.0 Park Fire cache (dist_output/, opera_reference/, run.log) as a historical-baseline artifact. The new `eval-dist/` is then populated freshly by Plan 05-06's `run_eval_dist.py` invocation (which produces a DEFERRED metrics.json — no large RTC/DIST data this time, just JSON sidecars).

2. **Consolidate `eval-dist-eu/` (hyphen) + `eval-dist-eu-nov15/` (hyphen) → `eval-dist_eu/` (underscore)** (CONTEXT D-11 + Open Question 4) — matrix_manifest.yml line 79 has `cache_dir: eval-dist_eu` (underscore) as the locked canonical path. The on-disk hyphenated directories are v1.0 leftovers; their relevant contents migrate into the underscore-canonical structure (sept28 + nov15 sub-paths under `eval-dist_eu/aveiro/`).

3. **Delete `run_eval_dist_eu_nov15.py`** (CONTEXT D-11) — content migrated into Plan 05-07's aveiro chained_retry sub-stage; the standalone Nov15 script is now dead code.

Purpose: This plan ships the cache-dir hygiene that Phase 4 STATE.md flagged as a "follow-up todo" (the supervisor cache_dir derivation and the manifest authoritative path were inconsistent). Resolving it now means Phase 7's `make eval-all` closure test reads only the canonical paths; future v1.x consumers see a clean directory layout.

The frontmatter's `directories_modified` field (Warning 8 fix from checker iteration 1) explicitly enumerates the directory-level operations so the source-audit cross-check captures them alongside the file-level operations.

Output: Three filesystem operations (one rename, one consolidation, one delete) + a smoke-test re-invocation of `make eval-dist-nam` to verify the renamed directory is correctly populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-CONTEXT.md
@.planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md
@.planning/STATE.md
@results/matrix_manifest.yml
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv eval-dist/ → eval-dist-park-fire/ (preserve v1.0 cache)</name>
  <files>eval-dist/, eval-dist-park-fire/</files>
  <read_first>
    - results/matrix_manifest.yml (lines 68-82 — confirm `cache_dir: eval-dist` is the locked canonical path; the rename to `eval-dist-park-fire/` does NOT touch the manifest)
    - eval-dist/ directory contents — list with `ls -la eval-dist/` to record what's being preserved (should include `dist_output/`, `opera_reference/`, `run.log`, possibly `metrics.json` from a prior v1.1 invocation)
    - .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-CONTEXT.md D-05 (Park Fire cache rename rationale)
  </read_first>
  <action>
The current state on the working tree (verified 2026-04-25):

```
eval-dist/
├── dist_output/         (v1.0 Park Fire 10TFK output — 8 .tif files)
├── opera_reference/     (empty, OPERA reference was never published for Park Fire)
└── run.log              (v1.0 run log — 3 KB)
```

After Plan 05-06 + 05-08 cleanup the desired state is:

```
eval-dist-park-fire/     (renamed from eval-dist/; preserves all v1.0 contents)
├── dist_output/
├── opera_reference/
└── run.log

eval-dist/               (fresh; populated by Plan 05-06's run_eval_dist.py)
├── metrics.json         (DistNamCellMetrics with cell_status='DEFERRED')
└── meta.json            (MetaJson)
```

**Step 1.** Verify the current state matches expectations:

```bash
ls -la eval-dist/ 2>&1
```

Expected output: 3 entries (dist_output/, opera_reference/, run.log). If a `metrics.json` already exists from a prior Plan 05-06 invocation (Wave 2 was completed before this Wave 4 cleanup), it must be moved aside before the rename — see Step 4.

**Step 2.** If `eval-dist/metrics.json` exists from Plan 05-06, save it to a temporary location:

```bash
if [ -f eval-dist/metrics.json ]; then
    mv eval-dist/metrics.json /tmp/eval-dist-metrics-from-plan-05-06.json
fi
if [ -f eval-dist/meta.json ]; then
    mv eval-dist/meta.json /tmp/eval-dist-meta-from-plan-05-06.json
fi
```

**Step 3.** Rename via git-mv (preserves history):

```bash
git mv eval-dist eval-dist-park-fire
```

If git-mv fails because `eval-dist/` is gitignored at runtime (likely; `eval-*/` is the typical gitignore pattern for cache dirs), use plain shell mv:

```bash
mv eval-dist eval-dist-park-fire
```

Verify after the rename:

```bash
ls -la eval-dist-park-fire/ | head -5
test ! -d eval-dist || echo "ERROR: eval-dist still exists"
```

**Step 4.** Re-create `eval-dist/` and restore the Plan 05-06 sidecars (if they existed):

```bash
mkdir -p eval-dist/
if [ -f /tmp/eval-dist-metrics-from-plan-05-06.json ]; then
    mv /tmp/eval-dist-metrics-from-plan-05-06.json eval-dist/metrics.json
fi
if [ -f /tmp/eval-dist-meta-from-plan-05-06.json ]; then
    mv /tmp/eval-dist-meta-from-plan-05-06.json eval-dist/meta.json
fi
```

**Step 5.** If Plan 05-06 did NOT yet populate eval-dist/ (i.e., the Wave 2 plan ran but the metrics.json files were not created — or this Wave 4 plan runs in a fresh execution where Wave 2 hasn't been physically executed), invoke `make eval-dist-nam` now to populate them:

```bash
test -f eval-dist/metrics.json || make eval-dist-nam
```

This produces a fresh `eval-dist/metrics.json` (`DistNamCellMetrics` with `cell_status='DEFERRED'`) and `eval-dist/meta.json` per Plan 05-06's logic.

**Step 6.** Verify the final state:

```bash
ls -la eval-dist/ eval-dist-park-fire/
```

Expected:
- `eval-dist/` contains `metrics.json` + `meta.json` (small JSON files; ~1 KB each)
- `eval-dist-park-fire/` contains `dist_output/`, `opera_reference/`, `run.log` (the v1.0 Park Fire cache)
  </action>
  <verify>
    <automated>test -d eval-dist-park-fire && test -f eval-dist-park-fire/run.log && test -d eval-dist-park-fire/dist_output && test -d eval-dist/ && test -f eval-dist/metrics.json && micromamba run -n subsideo python3 -c "from subsideo.validation.matrix_schema import DistNamCellMetrics; m = DistNamCellMetrics.model_validate_json(open('eval-dist/metrics.json').read()); assert m.cell_status == 'DEFERRED', f'cell_status={m.cell_status} (expected DEFERRED)'; print(f'OK: cell_status={m.cell_status}, cmr_probe_outcome={m.cmr_probe_outcome}')"</automated>
  </verify>
  <acceptance_criteria>
    - `eval-dist-park-fire/` directory exists and contains the v1.0 Park Fire cache (dist_output/ subdirectory + run.log file)
    - `eval-dist/` directory exists (recreated fresh after the rename)
    - `eval-dist/metrics.json` exists and parses as DistNamCellMetrics with `cell_status='DEFERRED'`
    - `eval-dist/meta.json` exists and parses as MetaJson
    - The original `eval-dist/dist_output/` (Park Fire content) is NOT in the new `eval-dist/` — only at `eval-dist-park-fire/dist_output/`
    - matrix_manifest.yml is unchanged (no edits)
  </acceptance_criteria>
  <done>Park Fire v1.0 cache preserved at eval-dist-park-fire/; new eval-dist/ populated with the deferred-cell metrics.json from Plan 05-06.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate eval-dist-eu/ (hyphen) + eval-dist-eu-nov15/ (hyphen) → eval-dist_eu/ (underscore)</name>
  <files>eval-dist-eu/, eval-dist-eu-nov15/, eval-dist_eu/</files>
  <read_first>
    - results/matrix_manifest.yml line 79 — `cache_dir: eval-dist_eu` (underscore is canonical)
    - eval-dist-eu/ + eval-dist-eu-nov15/ directory contents (ls -la both); confirm what v1.0 left behind
    - .planning/STATE.md "Phase 4 follow-up todo: reconcile supervisor cache_dir derivation with on-disk hyphen convention" — this plan resolves that todo for the dist:eu cell
    - .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md Open Question 4 (cache directory consolidation: option (a) git mv preferred over option (b) edit manifest)
  </read_first>
  <action>
The current state on the working tree:

```
eval-dist-eu/                   (v1.0 Aveiro Sept 28 cache — hyphen)
├── dist_output/                (8 .tif files from v1.0 Aveiro Sept 28 dist-s1 run)
├── ems_reference/              (Copernicus EMS reference for v1.0 — replaced by EFFIS WFS in Phase 5)
├── enumeration_result.json     (dist_s1_enumerator output for Sept 28)
└── reference/                  (empty)

eval-dist-eu-nov15/             (v1.0 Aveiro Nov 15 cache — hyphen)
├── dist_output/                (8 .tif files from v1.0 Aveiro Nov 15 dist-s1 run)
├── ems_reference/
└── enumeration_result.json     (Nov 15 enumerator output)

eval-dist_eu/                   (created by Plan 05-02 + 05-07; underscore — canonical per matrix_manifest.yml)
├── effis_endpoint_lock.txt     (Plan 05-02 output)
├── (potentially) aveiro/       (Plan 05-07 chained sub-dirs: sept28/, oct10/, chained/)
├── (potentially) evros/
├── (potentially) spain_culebra/
├── (potentially) metrics.json  (if Plan 05-07 already ran)
└── (potentially) meta.json     (if Plan 05-07 already ran)
```

The desired final state:

```
eval-dist_eu/                   (only canonical EU DIST cache)
├── effis_endpoint_lock.txt
├── aveiro/
│   ├── effis_perimeters/perimeters.geojson
│   ├── dist_output/            (Sept 28 unchained baseline; from Plan 05-07)
│   ├── sept28/                 (chained sub-stage; from Plan 05-07)
│   ├── oct10/                  (chained sub-stage — the missing middle, from Plan 05-07)
│   ├── chained/                (chained sub-stage; Nov 15 with prior=Oct 10; from Plan 05-07)
│   └── (preserved from v1.0 hyphen dirs if any artifact is still useful)
├── evros/                      (from Plan 05-07; may not yet exist if eval failed)
├── spain_culebra/              (from Plan 05-07; may not yet exist if eval failed)
├── metrics.json                (DistEUCellMetrics from Plan 05-07)
└── meta.json
```

**Step 1.** Inventory the current state:

```bash
ls -la eval-dist-eu/ eval-dist-eu-nov15/ eval-dist_eu/ 2>&1
```

**Step 2.** Decide whether to migrate v1.0 hyphen contents into `eval-dist_eu/aveiro/` OR delete them outright.

The v1.0 Aveiro Sept 28 + Nov 15 dist_output/ contents were produced with `dist-s1 2.0.13` (per v1.0 STATE.md notes) — Phase 5 uses 2.0.14, so the v1.0 cached outputs may be incompatible with Phase 5's chained_retry pass criterion (the `DistS1ProductDirectory.from_path()` validation may reject 2.0.13 output if the layer-name format changed).

**Recommendation:** DELETE the hyphen directories. Plan 05-07 produces fresh outputs under the chained sub-stage directory layout; the v1.0 hyphen contents are stale and not referenced by the new `run_eval_dist_eu.py`. The historical record is preserved in git history for the v1.0 milestone, and CONCLUSIONS_DIST_EU.md (Plan 05-09) preserves the v1.0 narrative as historical-baseline preamble.

**Step 3.** Delete the v1.0 hyphen directories:

```bash
rm -rf eval-dist-eu/
rm -rf eval-dist-eu-nov15/
```

If the user prefers to PRESERVE the v1.0 hyphen contents (analogous to the eval-dist-park-fire/ rename for N.Am.), the alternative is:

```bash
# Alternative: preserve as historical baseline
mv eval-dist-eu eval-dist-eu-v1.0-sept28
mv eval-dist-eu-nov15 eval-dist-eu-v1.0-nov15
```

Default: delete (the v1.0 EU baseline is materially identical to Phase 5's aveiro Sept 28 + Nov 15 chained-retry intermediate outputs at the file level; the narrative-level preservation lives in CONCLUSIONS_DIST_EU.md).

**Step 4.** If `eval-dist_eu/metrics.json` does NOT yet exist (i.e., Plan 05-07 did not yet execute or its checkpoint was not approved), do NOT invoke `make eval-dist-eu` here — that's an 8-hour budget event and belongs in Plan 05-07's checkpoint flow, not this cleanup plan. Instead, leave `eval-dist_eu/` containing only the Plan 05-02 lock file; Plan 05-09 will refuse to run until `eval-dist_eu/metrics.json` exists.

**Step 5.** Verify the final state:

```bash
ls -la eval-dist_eu/
test ! -d eval-dist-eu || echo "ERROR: eval-dist-eu still exists"
test ! -d eval-dist-eu-nov15 || echo "ERROR: eval-dist-eu-nov15 still exists"
```
  </action>
  <verify>
    <automated>test ! -d eval-dist-eu && test ! -d eval-dist-eu-nov15 && test -d eval-dist_eu && test -f eval-dist_eu/effis_endpoint_lock.txt && grep -q 'cache_dir: eval-dist_eu' results/matrix_manifest.yml</automated>
  </verify>
  <acceptance_criteria>
    - `eval-dist-eu/` directory does NOT exist
    - `eval-dist-eu-nov15/` directory does NOT exist
    - `eval-dist_eu/` directory exists and contains `effis_endpoint_lock.txt` (from Plan 05-02)
    - `matrix_manifest.yml` contains the literal string `cache_dir: eval-dist_eu` (unchanged from before)
    - If `eval-dist_eu/metrics.json` exists (Plan 05-07 already ran), it parses as DistEUCellMetrics: verify with `micromamba run -n subsideo python3 -c "from subsideo.validation.matrix_schema import DistEUCellMetrics; DistEUCellMetrics.model_validate_json(open('eval-dist_eu/metrics.json').read())"` — but DO NOT FAIL the task if metrics.json is absent (Plan 05-07's live execution may not have completed at this point in the wave sequence)
    - If the user chose the "preserve as historical baseline" alternative in Step 3, the directories `eval-dist-eu-v1.0-sept28/` and `eval-dist-eu-v1.0-nov15/` exist instead — both are acceptable end-states; the verify command checks only that the original hyphenated names are gone
  </acceptance_criteria>
  <done>v1.0 hyphenated EU DIST cache directories cleaned up; canonical `eval-dist_eu/` is the only top-level EU DIST cache; matrix_manifest.yml unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: git rm run_eval_dist_eu_nov15.py</name>
  <files>run_eval_dist_eu_nov15.py</files>
  <read_first>
    - run_eval_dist_eu_nov15.py (verify it still exists and has not been touched by Plan 05-07; the rewritten run_eval_dist_eu.py absorbed its content into the chained_retry sub-stage)
    - run_eval_dist_eu.py (verify Plan 05-07 successfully landed; if not, this task aborts to avoid losing the Nov 15 logic before its destination is ready)
    - .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-CONTEXT.md D-11 (the v1.0 nov15 script deletion is a Phase 5 closure deliverable, not an early-wave action)
  </read_first>
  <action>
**Step 1.** Verify Plan 05-07 successfully landed the new `run_eval_dist_eu.py`:

```bash
grep -q "EVENTS: list\[EventConfig\]" run_eval_dist_eu.py && grep -q "_chained_retry_for_aveiro" run_eval_dist_eu.py && echo "Plan 05-07 OK"
```

If either grep fails, Plan 05-07 did not complete; this task ABORTS (do not delete the v1.0 nov15 script before its content has migrated). Surface the issue and wait for Plan 05-07 to complete + the user checkpoint to be approved.

**Step 2.** Confirm the v1.0 nov15 script's content has migrated (the Nov 15 dist-s1 invocation is now part of the chained_retry sub-stage):

```bash
grep -c "post_dates=\[date(2024, 9, 28), date(2024, 10, 10), date(2024, 11, 15)\]" run_eval_dist_eu.py | awk '$1 == 1 {exit 0} {exit 1}'
```

This confirms the aveiro EventConfig has all three dates (Sept 28 + Oct 10 + Nov 15) in its post_dates list — the migration target.

**Step 3.** Delete via git-rm (preserves history):

```bash
git rm run_eval_dist_eu_nov15.py
```

Or if not tracked (defensively):

```bash
test -f run_eval_dist_eu_nov15.py && rm run_eval_dist_eu_nov15.py
```

**Step 4.** Verify the deletion:

```bash
test ! -f run_eval_dist_eu_nov15.py
```

The Nov 15 logic is now exclusively in `run_eval_dist_eu.py`'s `_chained_retry_for_aveiro` function — there is no longer a standalone Nov 15 entry point. The old script's git history is preserved (git log can show the file's contents at any prior commit).

**Step 5.** Verify Makefile is unchanged (it was already correct — no `eval-dist-eu-nov15` target was ever added; `make eval-dist-eu` runs the consolidated script):

```bash
grep -c "run_eval_dist_eu_nov15" Makefile | awk '$1 == 0 {exit 0} {exit 1}'
```
  </action>
  <verify>
    <automated>test ! -f run_eval_dist_eu_nov15.py && grep -q "EVENTS: list\[EventConfig\]" run_eval_dist_eu.py && grep -c "run_eval_dist_eu_nov15" Makefile | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `run_eval_dist_eu_nov15.py` does NOT exist as a top-level file
    - `run_eval_dist_eu.py` exists and contains the EVENTS list with the aveiro 3-date post_dates entry (verifies content migration before the delete)
    - `Makefile` does NOT reference `run_eval_dist_eu_nov15` (verify via grep — should be 0 matches; the canonical target is `make eval-dist-eu` which runs the consolidated script)
    - Git history for `run_eval_dist_eu_nov15.py` is preserved: `git log --oneline -- run_eval_dist_eu_nov15.py` shows the file's last commit (Apr 22 per recent log)
  </acceptance_criteria>
  <done>v1.0 standalone Nov 15 script git-rm'd; Nov 15 dist-s1 logic exclusively lives in the consolidated run_eval_dist_eu.py via the aveiro chained_retry sub-stage; Makefile target unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cache directory cleanup completed: `eval-dist/` renamed to `eval-dist-park-fire/` (preserves v1.0 Park Fire); new `eval-dist/` populated with DEFERRED metrics.json + meta.json from Plan 05-06; `eval-dist-eu/` + `eval-dist-eu-nov15/` (hyphen) consolidated into / replaced by `eval-dist_eu/` (underscore matches matrix_manifest.yml line 79); `run_eval_dist_eu_nov15.py` git-rm'd (content migrated to Plan 05-07's chained_retry sub-stage). matrix_manifest.yml unchanged.</what-built>
  <how-to-verify>
1. Run `ls -la eval-dist eval-dist-park-fire eval-dist_eu 2>&1` and verify:
   - `eval-dist/` contains `metrics.json` + `meta.json` (small files, ~1 KB each)
   - `eval-dist-park-fire/` contains `dist_output/` + `opera_reference/` + `run.log` (the v1.0 Park Fire cache, untouched)
   - `eval-dist_eu/` contains `effis_endpoint_lock.txt` plus (if Plan 05-07 has run) `metrics.json` + `meta.json` + per-event subdirectories
2. Run `ls eval-dist-eu eval-dist-eu-nov15 2>&1 | grep -c "No such"` and verify the output is `2` (both hyphenated dirs are gone).
3. Run `test -f run_eval_dist_eu_nov15.py && echo PRESENT || echo ABSENT` and verify the output is `ABSENT`.
4. Run `make eval-dist-nam` and verify it completes in ~1 minute (the deferred path is fast); produces `eval-dist/metrics.json` parseable as DistNamCellMetrics with `cell_status='DEFERRED'`. (This is a re-invocation; the existing metrics.json from Plan 05-06 should be overwritten with identical content.)
5. Optionally re-run `make eval-dist-eu` if Plan 05-07's checkpoint did not approve a full execution. Caveat: this is an 8-hour budget event; only do this if needed.
6. Verify `git status` shows the deleted files (`eval-dist-eu/`, `eval-dist-eu-nov15/`, `run_eval_dist_eu_nov15.py`) and the renamed file (`eval-dist/` ↔ `eval-dist-park-fire/`). The git diff should be a clean rename + delete pair, no unintended modifications.
7. If everything looks correct, type `approved`. Plan 05-09 will then write CONCLUSIONS_DIST_N_AM.md + CONCLUSIONS_DIST_EU.md from the now-canonical metrics.json files.
8. If something looks off (e.g., Park Fire's run.log was lost in the rename, or eval-dist_eu/metrics.json is malformed), describe the issue. Common causes: (a) the rename was applied to the wrong directory; (b) Plan 05-07 did not actually run; (c) gitignore patterns interfered with the git-mv (use plain mv instead).
  </how-to-verify>
  <resume-signal>Type "approved" once the four directories + one deleted script verify correctly. Type the issue description if anything is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → cache dirs | Directory rename + delete operations on user-facing on-disk caches. Validated paths only (literal directory names within the project root). |
| `git mv` → git index | git-tracked filesystem operations preserve history; gitignored directories use plain `mv` instead. |
| `rm -rf` → user data | Destructive operation; only invoked on the v1.0 hyphenated EU DIST cache directories which are stale per Phase 5 D-11; user-checkpoint approves before execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | path traversal in rename | mitigate | All paths are LITERAL hardcoded strings (`eval-dist`, `eval-dist-park-fire`, `eval-dist-eu`, etc.); no user input flows into the path. |
| T-05-08-02 | Information disclosure | v1.0 Park Fire cache exposed | accept | Park Fire cache contains only public Sentinel-1 + dist-s1 output; no PII. The rename to `eval-dist-park-fire/` makes its content easier to find for future reviewers. |
| T-05-08-03 | Denial of service | accidental deletion of new metrics.json | mitigate | Step 2 of Task 1 explicitly saves `eval-dist/metrics.json` to `/tmp/` before the rename and restores it after; verify command checks both directories exist with expected contents. |
| T-05-08-04 | Elevation of privilege | None | accept | No shell-out beyond `git mv`/`mv`/`rm -rf`/`make`; no privilege escalation surface. |
| T-05-08-05 | Repudiation | which dirs were renamed/deleted | mitigate | git status + git diff captures rename + delete; the user-checkpoint task explicitly walks through `git status` to confirm. The frontmatter `directories_modified` field (Warning 8 fix) is the canonical enumeration so the source-audit cross-check picks up cache-dir changes alongside file changes. |
| T-05-08-06 | Tampering | accidental deletion of run_eval_dist_eu_nov15.py before content migration | mitigate | Task 3 Step 1 verifies Plan 05-07 already landed (checks for EVENTS list + chained_retry helper); aborts on failure to prevent premature deletion. Step 2 verifies the 3-date aveiro post_dates list is in place. |
| T-05-08-07 | Tampering | gitignore interference with git-mv | mitigate | Both `git mv` and plain `mv` paths are documented; if eval-*/ is gitignored, the plain `mv` path is used and verified afterward. |
</threat_model>

<verification>
- `eval-dist/` exists with `metrics.json` + `meta.json` (DEFERRED cell sidecars from Plan 05-06).
- `eval-dist-park-fire/` exists with v1.0 Park Fire cache contents.
- `eval-dist-eu/` and `eval-dist-eu-nov15/` do NOT exist.
- `eval-dist_eu/` exists with at minimum `effis_endpoint_lock.txt` (Plan 05-02 output) and (if Plan 05-07 ran) `metrics.json` + `meta.json`.
- `run_eval_dist_eu_nov15.py` does NOT exist as a top-level file.
- `Makefile` and `matrix_manifest.yml` are unchanged.
- `git status` shows the rename + delete operations cleanly (no unintended modifications).
</verification>

<success_criteria>
- v1.0 Park Fire cache preserved at `eval-dist-park-fire/` (D-05 directive).
- New `eval-dist/` populated freshly by Plan 05-06 (DEFERRED cell).
- v1.0 hyphenated EU DIST cache directories deleted; canonical underscore path is the only one.
- v1.0 standalone Nov 15 script deleted; content migrated into Plan 05-07's chained_retry sub-stage.
- matrix_manifest.yml unchanged (no edits — both `eval-dist` and `eval-dist_eu` were already canonical from Plan 1 D-08).
- `directories_modified` frontmatter field enumerates all 5 directory-level operations (Warning 8 fix from checker iteration 1).
</success_criteria>

<output>
After completion, create `.planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-08-SUMMARY.md` documenting: (1) the rename + delete operations performed, (2) the contents of `eval-dist-park-fire/` (which v1.0 files are now archived there), (3) whether the v1.0 hyphenated EU dirs were deleted outright or preserved as `eval-dist-eu-v1.0-*` (the user's choice in Task 2 Step 3), (4) confirmation that Plan 05-09 can now read both `eval-dist/metrics.json` and `eval-dist_eu/metrics.json` from canonical paths, (5) the directories_modified frontmatter enumeration (Warning 8 fix from checker iteration 1).
</output>
</content>
</invoke>