---
phase: 05-dist-s1-opera-v0-1-effis-eu
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03]
tags: [roadmap, requirements, defer, scope-amendment, research-resolution-log]

must_haves:
  truths:
    - "ROADMAP.md Phase 5 success criteria 1, 2, 3 explicitly read 'deferred to v1.2' with rationale"
    - "REQUIREMENTS.md DIST-01, DIST-02, DIST-03 traceability rows show status='Deferred to v1.2' (not 'Pending')"
    - "Both files cite the two evidence anchors: RESEARCH Probe 1 (no canonical CloudFront URL — sample is regenerated locally) AND RESEARCH Probe 6 (OPERA_L3_DIST-ALERT-S1_V1 returns empty in CMR as of 2026-04-25)"
    - "EMSR686 correction (NOT EMSR649) for Evros 2023 is recorded in ROADMAP Phase 5 'Plans' rationale"
    - "Spain Sierra de la Culebra (NOT Romania clear-cuts) substitution is recorded with rationale (EFFIS coverage gap; Probe 4)"
    - "ROADMAP.md scope-amendment block records the D-18 amendment (EFFIS direct-owslib path with RETRY_POLICY['EFFIS'] consulted via tenacity/urllib3 wrapper inside effis.py — NOT routed through harness.download_reference_with_retry whose Path-returning contract is incompatible with WFS in-memory streaming)"
    - "ROADMAP.md scope-amendment block records the D-24 amendment (matrix_writer DIST branches insert AFTER disp:* — the structurally meaningful invariant — without locking BEFORE cslc:* / dswx:*; downstream test relaxes to assert only disp_call < dist_eu_call AND dist_eu_call < dist_nam_call)"
    - "RESEARCH.md ## Resolution Log section maps Risks A-M and Q1-Q5 to dispositions (resolved / accepted-as-narrative / accepted-as-blocker / deferred-to-v1.2), with each row citing the resolving plan or rationale"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 success criteria amendment + rationale block + D-18 + D-24 amendment paragraphs"
      contains: "deferred to v1.2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DIST-01..03 status field showing deferral"
      contains: "Deferred to v1.2"
    - path: ".planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md"
      provides: "Resolution Log section enumerating Risks A-M + Q1-Q5 with dispositions"
      contains: "## Resolution Log"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "consistent deferral status for DIST-01/02/03"
      pattern: "Deferred to v1.2"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md"
      via: "rationale references RESEARCH Probe 1 + Probe 6"
      pattern: "Probe 1|Probe 6"
    - from: ".planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md"
      to: ".planning/phases/05-dist-s1-opera-v0-1-effis-eu/ (sibling plans)"
      via: "Resolution Log links each Risk/Q to the resolving plan ID (e.g. Q5 → Plan 05-07 Oct 10 invocation)"
      pattern: "Plan 05-0[1-9]"
---

<objective>
Amend ROADMAP.md and REQUIREMENTS.md to record the Phase 5 scope amendment that defers DIST-01/02/03 (config-drift gate, F1+CI vs operational reference, CloudFront sample fetch) to v1.2 once OPERA_L3_DIST-ALERT-S1_V1 publishes operationally. Also amend the same scope-amendment block to record the D-18 amendment (EFFIS direct owslib path; harness retry policy consulted but not the dispatch entry point) and the D-24 amendment (matrix_writer DIST branches insert AFTER disp:* — the structurally meaningful invariant — with the BEFORE cslc:* part relaxed to leave room for re-ordering as the dispatch chain evolves). Finally, add a Resolution Log section to 05-RESEARCH.md mapping Open Risks A-M and Open Questions Q1-Q5 to dispositions per the Dim 11 verifier protocol.

Purpose: This plan codifies the user-approved scope amendment in the project-level source artifacts so downstream phase work, the verifier, and future readers see the deferral as a documented decision, not a dropped requirement. RESEARCH Probe 1 found that the OPERA v0.1 sample is structurally a local-regeneration recipe (not a CloudFront download); RESEARCH Probe 6 confirmed the operational collection is empty in CMR. Without this amendment the verifier would (correctly) flag DIST-01/02/03 as missing from Phase 5 plans — that signal is wrong because we are deliberately deferring, not skipping. The D-18 amendment resolves the harness-routing-vs-direct-owslib structural conflict surfaced in checker iteration 1 (harness.download_reference_with_retry returns Path; owslib WFS responses are in-memory streams). The D-24 amendment relaxes the dispatch-ordering invariant from a 4-way chain (disp → dist_eu → dist_nam → cslc) to the 2-way structurally-meaningful pair (disp → dist_*) — leaving the pre-cslc constraint as a contemporary observation rather than a forward lock.

Output: Three amended files with stable deferral / amendment / resolution markers that REL-05 can read at milestone close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-CONTEXT.md
@.planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP.md Phase 5 with deferral markers + D-18 + D-24 amendments + EMSR686 + Spain substitution</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 157-173 are the existing Phase 5 block; need to see exact current text before editing)
    - .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md (Probe 1 lines ~122-165; Probe 4 lines ~349-375; Probe 6 lines ~402-461; Probe 8 lines ~483-547 — these are the four evidence anchors)
    - .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-CONTEXT.md sections "decisions" + "Deferred Ideas" (D-01..D-04, D-18, D-22..D-25, Romania substitution note)
    - .planning/phases/04-disp-s1-comparison-adapter-honest-fail/04-CONTEXT.md D-13 (the v1.0-narrative-preserved-as-baseline pattern this plan inherits) and D-08 (Phase 4 dispatch-ordering invariant text — the source the D-24 amendment relaxes)
  </read_first>
  <action>
Edit `.planning/ROADMAP.md` between the existing `### Phase 5: DIST-S1 OPERA v0.1 + EFFIS EU` heading and the `### Phase 6:` heading (currently lines 157-173). Apply ONLY the changes below — do not restructure existing prose, do not delete success criteria, and do not change the Goal text wholesale.

1. **Insert a 'Scope amendment (2026-04-25)' callout block** immediately after the Phase 5 `**Goal**:` paragraph (line ~159) and BEFORE the `**Depends on**:` line. Use this exact block (markdown):

   ```markdown
   **Scope amendment (2026-04-25):** Phase 5 success criteria 1, 2, and 3 are deferred to v1.2.
   - **Why criterion 1 (DIST-01 CloudFront fetch) is deferred:** RESEARCH Probe 1 found that the OPERA DIST-S1 v0.1 product has no canonical CloudFront URL; OPERA-ADT publishes a notebook recipe (`notebooks/A__E2E_Workflow_Demo.ipynb` Cell 5) that *regenerates* the sample locally via `run_dist_s1_workflow(mgrs_tile_id='11SLT', post_date='2025-01-21', track_number=71, ...)`. The "fetch-and-compare" framing is structurally inapplicable.
   - **Why criteria 2 + 3 (DIST-02 config-drift + DIST-03 F1+CI) are deferred:** RESEARCH Probe 6 confirmed that `earthaccess.search_data(short_name='OPERA_L3_DIST-ALERT-S1_V1', ...)` returns an empty result set as of 2026-04-25 (the operational collection has not been published). Without an operational reference, F1+CI computation has no target.
   - **What still ships in Phase 5:** criteria 4 (DIST-04 CMR auto-supersede probe; Stage 0 of `run_eval_dist.py` runs every invocation and writes a `cell_status='DEFERRED'` metrics.json on miss; auto-fills when operational publishes), 5 (DIST-05 EFFIS WFS cross-validation), 6 (DIST-06 EU 3-event aggregate), and 7 (DIST-07 chained `prior_dist_s1_product` retry on Aveiro stack). Bootstrap CI methodology (`validation/bootstrap.py`) ships now and is used for EU per-event F1.
   - **EMSR correction:** the Evros 2023 wildfire EMS activation is **EMSR686**, not EMSR649 (RESEARCH Probe 8 — EMSR649 was an Italian flood). The roadmap text below previously said "EMSR649"; treat all references to that activation ID in Phase 5 work as EMSR686.
   - **Romania substitution:** the third EU event in Phase 5 is **Spain Sierra de la Culebra June 2022** (~26,000 ha; RESEARCH Probe 4 ADR), not the originally-listed "2022 Romanian forest clear-cuts". EFFIS is fire-only and does not cover clear-cut logging (PITFALLS P4.5); Romania 2022 had no fire-event EFFIS coverage that aligns with subsideo's pipeline window.
   - **D-18 amendment (EFFIS WFS dispatch):** CONTEXT.md D-18 originally bound EFFIS WFS access to `harness.download_reference_with_retry(source='effis')`. That contract returns a `Path` to a downloaded artifact, which is incompatible with `owslib.wfs.WebFeatureService.getfeature(...)` whose response is an in-memory stream parsed by `geopandas.read_file(BytesIO(...))`. D-18 is therefore amended: `validation/effis.py` calls `owslib.wfs.WebFeatureService` directly. Retry semantics still come from `harness.RETRY_POLICY['EFFIS']` (Plan 05-04 lands the dict entry); `effis.py` reads that policy and applies it inline via a thin retry wrapper (e.g. `tenacity.Retrying(...)` keyed on the policy's `retry_on`/`abort_on` lists, or an equivalent urllib3 retry adapter on the owslib session). The harness still owns the policy declaration; only the dispatch entry-point shifts.
   - **D-24 amendment (matrix_writer dispatch ordering):** CONTEXT.md D-24 originally locked DIST render branches to insert "BEFORE DSWx (which Phase 6 will add) but AFTER existing DISP branches per Phase 4 D-08 ordering invariant". The structurally-meaningful invariant is the AFTER-disp:* part: schemas are disjoint, but disp:* and dist:eu both have valid metrics.json sidecars when both phases land, and disp:* must be checked first because its `ramp_attribution` discriminator is unambiguous. The BEFORE part (cslc:* / dswx:*) is a contemporary observation, not a forward lock — Phase 6 may legitimately re-order DSWx ahead of DIST as new schemas land. Plan 05-05's `test_dispatch_ordering_dist_before_cslc` test therefore asserts only `disp_call < dist_eu_call AND dist_eu_call < dist_nam_call` (the structurally meaningful pair), leaving the pre-cslc relationship as an unverified contemporary state.
   ```

2. **Annotate the existing 6 success criteria with status tags** by appending `[deferred to v1.2]` (criteria 1, 2, 3) or `[Phase 5 deliverable]` (criteria 4, 5, 6) at the end of each criterion's bullet text. Keep the existing wording verbatim — only append the status tag.

   Concretely (lines ~166-172 of the current file):
   - Append ` [deferred to v1.2]` to criterion 1 (`User runs the DIST LA eval...preserved on disk regardless of downstream comparison outcome (DIST-01)`).
   - Append ` [deferred to v1.2]` to criterion 2 (`Before T11SLT quantitative comparison runs, a config-drift gate...continues (DIST-02, prevents PITFALLS P4.1)`).
   - Append ` [deferred to v1.2]` to criterion 3 (`When the config-drift gate passes, the T11SLT comparison reports F1...without tightening toward v0.1's own score (DIST-03, prevents PITFALLS P4.2 single-tile variance + M1 target-creep)`).
   - Append ` [Phase 5 deliverable]` to criterion 4 (`make eval-dist-nam includes a CMR probe...without manual intervention and no re-planning (DIST-04)`).
   - Append ` [Phase 5 deliverable]` to criterion 5 (`User runs EFFIS cross-validation...EMSR649 + 2022 Romanian forest clear-cuts (DIST-06)`). ALSO inline-correct the parenthetical text: replace `2023 Evros Greece EMSR649` with `2023 Evros Greece EMSR686` and replace `2022 Romanian forest clear-cuts` with `2022 Spain Sierra de la Culebra wildfire (substituted from Romania per EFFIS coverage gap)`.
   - Append ` [Phase 5 deliverable]` to criterion 6 (`After Phase 1's _mp.py bundle lands, the chained prior_dist_s1_product run...non-blocking to milestone closure (DIST-07)`).

3. **Update the Phase 5 `**Plans**: TBD` line** (currently line 173 of the existing file). Replace with:

   ```markdown
   **Plans**: 9 plans across 6 waves

   Plans:
   - [ ] 05-01-PLAN.md — ROADMAP + REQUIREMENTS scope amendment (defer DIST-01/02/03; correct EMSR686; document Spain substitution; D-18 + D-24 amendments; RESEARCH Resolution Log) [Wave 0] [DIST-01, DIST-02, DIST-03]
   - [ ] 05-02-PLAN.md — EFFIS WFS endpoint GetCapabilities probe + lock artifact [Wave 0] [DIST-05, DIST-06]
   - [ ] 05-03-PLAN.md — Pure-additive scaffolding: bootstrap.py module + matrix_schema.py 7 EU types + minimal DistNamCellMetrics-Deferred [Wave 1] [DIST-03, DIST-05, DIST-06, DIST-07]
   - [ ] 05-04-PLAN.md — harness.RETRY_POLICY['EFFIS'] + pyproject.toml owslib pip pin [Wave 1] [DIST-05]
   - [ ] 05-05-PLAN.md — validation/effis.py (WFS query + dual rasterise) + matrix_writer dist:nam (DEFERRED) + dist:eu (X/3 PASS) render branches [Wave 2] [DIST-04, DIST-05, DIST-06]
   - [ ] 05-06-PLAN.md — run_eval_dist.py rewrite: CMR Stage 0 auto-supersede probe + DEFERRED metrics.json write + D-16 archival + unit tests [Wave 2] [DIST-01, DIST-02, DIST-03, DIST-04]
   - [ ] 05-07-PLAN.md — run_eval_dist_eu.py rewrite as declarative EVENTS list (Aveiro chained triple including missing Oct 10 + Evros EMSR686 + Spain Culebra) + track_number probe [Wave 3] [DIST-05, DIST-06, DIST-07]
   - [ ] 05-08-PLAN.md — Cache directory cleanup: eval-dist → eval-dist-park-fire rename; eval-dist-eu* → eval-dist_eu consolidation; delete run_eval_dist_eu_nov15.py [Wave 4] [DIST-04, DIST-06]
   - [ ] 05-09-PLAN.md — Docs: docs/validation_methodology.md §4 append + CONCLUSIONS_DIST_N_AM.md (deferred sub-section) + CONCLUSIONS_DIST_EU.md (3-event sub-section) [Wave 5] [DIST-04, DIST-05, DIST-06, DIST-07]

   **Requirements coverage audit** (all 7 Phase 5 requirement IDs):
   DIST-01 (01, 06), DIST-02 (01, 06), DIST-03 (01, 03, 06), DIST-04 (01, 05, 06, 08, 09), DIST-05 (02, 03, 04, 05, 07, 09), DIST-06 (01, 02, 03, 05, 07, 08, 09), DIST-07 (03, 07, 09). Every requirement appears in at least one plan.
   ```

4. **Update the v1.1 progress table** at the bottom of ROADMAP.md (currently row `| 5. DIST-S1 OPERA v0.1 + EFFIS EU | v1.1 | 0/0 | Not started | - |`). Change `0/0` to `0/9` and `Not started` to `Planned`. Keep the dash in the Completed column.

Do NOT touch any other phase entry or Goal text. Do NOT modify the v1.0 details.

This implements user-locked decisions per the planning context `<scope_amendments>` block items 1, 2, and 7 + the D-18 + D-24 amendments per checker iteration 1 feedback.
  </action>
  <verify>
    <automated>grep -n "deferred to v1.2" .planning/ROADMAP.md | wc -l | awk '$1 >= 4 {exit 0} {exit 1}' && grep -n "EMSR686" .planning/ROADMAP.md && grep -n "Sierra de la Culebra" .planning/ROADMAP.md && grep -n "0/9 | Planned" .planning/ROADMAP.md && grep -c "05-0[1-9]-PLAN.md" .planning/ROADMAP.md | awk '$1 == 9 {exit 0} {exit 1}' && grep -n "D-18 amendment" .planning/ROADMAP.md && grep -n "D-24 amendment" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deferred to v1.2" .planning/ROADMAP.md` outputs at least 4 (1 in scope-amendment block + 3 on success criteria 1/2/3)
    - `grep -n "EMSR686" .planning/ROADMAP.md` outputs at least one match
    - `grep -n "Sierra de la Culebra" .planning/ROADMAP.md` outputs at least one match
    - `grep -n "0/9 | Planned" .planning/ROADMAP.md` outputs exactly one match (Phase 5 progress row)
    - `grep -c "05-0[1-9]-PLAN.md" .planning/ROADMAP.md` outputs exactly 9 (the plan-list block)
    - `grep -n "Phase 5 deliverable" .planning/ROADMAP.md` outputs exactly 3 matches (criteria 4, 5, 6)
    - `grep -n "D-18 amendment" .planning/ROADMAP.md` outputs at least one match (the EFFIS direct-owslib amendment paragraph)
    - `grep -n "D-24 amendment" .planning/ROADMAP.md` outputs at least one match (the dispatch-ordering relaxation paragraph)
    - The amendment block appears AFTER `**Goal**:` and BEFORE `**Depends on**:` (verify by line-number ordering with `grep -n`)
    - Scope amendment block contains `RESEARCH Probe 1` (verbatim) and `RESEARCH Probe 6` (verbatim)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 5 section reads correctly: success criteria 1/2/3 marked `[deferred to v1.2]` with evidence-anchored rationale; criteria 4/5/6 marked `[Phase 5 deliverable]` with EMSR686 + Spain Culebra corrections inline; D-18 + D-24 amendments recorded in scope block; Plans block lists 9 plans across 6 waves; progress table shows `0/9 | Planned`; v1.0 entries untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Amend REQUIREMENTS.md DIST-01/02/03 to status='Deferred to v1.2' with rationale</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 62-70 are the DIST-01..07 v1.1 requirement block; lines 192-198 are the traceability table rows for DIST-01..07; lines 213-225 are the per-phase coverage summary)
    - .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md Probe 1 + Probe 6 (the two evidence anchors used in Task 1)
    - .planning/REQUIREMENTS.md DISP-V2-04 entry (lines ~117-125) as the prior pattern for cross-referencing v2 follow-up work
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` to record DIST-01/02/03 as deferred to v1.2. Apply ONLY the changes below.

1. **Replace the three DIST-01/02/03 v1.1 requirement bullets** (currently lines 64-66 of the existing file) with the same text but prepended with a `[deferred to v1.2]` tag. Keep the existing requirement description verbatim — only add the tag at the start of the description, immediately after the `**DIST-XX**:` marker. Concretely, the new bullets read:

   ```markdown
   - [ ] **DIST-01**: [deferred to v1.2 — see Scope amendment in ROADMAP.md Phase 5; OPERA v0.1 sample has no canonical CloudFront URL per RESEARCH Probe 1] The OPERA DIST v0.1 sample for MGRS tile T11SLT is fetched via CloudFront direct-download with exponential-backoff retry (via harness) and cached under `eval-dist/opera_reference/v0.1_T11SLT/` (the sample is preserved on disk regardless of downstream comparison outcome).
   - [ ] **DIST-02**: [deferred to v1.2 — operational reference not yet published per RESEARCH Probe 6 + DIST-01 deferral] Before running the T11SLT quantitative comparison, a config-drift gate extracts the OPERA v0.1 sample's 7 key processing parameters (confirmation-count threshold, pre-image strategy, post-date buffer, baseline window length, despeckle settings + 2 further) and compares against dist-s1 2.0.14 defaults; material deltas cause skip-and-defer (matrix cell reads "deferred pending operational reference publication").
   - [ ] **DIST-03**: [deferred to v1.2 — F1+CI vs operational reference is moot until reference exists per RESEARCH Probe 6] When the config-drift gate passes, T11SLT comparison computes F1 / precision / recall / accuracy with block-bootstrap confidence interval (1 km blocks, B=500); matrix cell reports point estimate AND CI; criteria remain F1 > 0.80 and accuracy > 0.85 without tightening toward v0.1's own score.
   ```

2. **Update the traceability table rows for DIST-01/02/03** (currently lines 192-194 of the existing file). Change the `Status` column from `Pending` to `Deferred to v1.2` for DIST-01, DIST-02, and DIST-03. Keep DIST-04, DIST-05, DIST-06, DIST-07 as `Pending`. Concretely:

   ```markdown
   | DIST-01 | Phase 5 | Deferred to v1.2 |
   | DIST-02 | Phase 5 | Deferred to v1.2 |
   | DIST-03 | Phase 5 | Deferred to v1.2 |
   | DIST-04 | Phase 5 | Pending |
   | DIST-05 | Phase 5 | Pending |
   | DIST-06 | Phase 5 | Pending |
   | DIST-07 | Phase 5 | Pending |
   ```

3. **Add a brief note under the v2 Requirements DIST Future Work section** (currently lines 113-119, the `### DIST Future Work` block). Insert a new bullet after `DIST-V2-04` reading:

   ```markdown
   - **DIST-V2-05**: Re-evaluate DIST-01 / DIST-02 / DIST-03 against operational `OPERA_L3_DIST-ALERT-S1_V1` once the collection publishes in CMR. Inherits the bootstrap CI methodology (`validation/bootstrap.py`) and matrix schema scaffolding shipped in v1.1 Phase 5; only the eval-script Stage 1+2 (regenerate-sample / config-drift / F1+CI) is new work. The CMR auto-supersede probe in `run_eval_dist.py` Stage 0 (DIST-04, shipped in v1.1) makes this a "next eval invocation" event, not a re-planning event. The Phase 5 D-16 archival hook (Plan 05-06: rename existing `eval-dist/metrics.json` → `eval-dist/archive/v0.1_metrics_<mtime>.json` before writing fresh sidecars) is already in place; v1.2 only adds the post-archival population code path.
   ```

4. **Update the per-phase counts block** (currently lines 218-225). The numerical totals do not change (Phase 5 still has 7 requirements), but add a parenthetical clarification to the Phase 5 line:

   Replace:
   ```
   - Phase 5 (DIST-S1 OPERA v0.1 + EFFIS EU): 7 (DIST-01..07)
   ```

   With:
   ```
   - Phase 5 (DIST-S1 OPERA v0.1 + EFFIS EU): 7 (DIST-01..07; DIST-01/02/03 deferred to v1.2 per scope amendment 2026-04-25 — see ROADMAP.md Phase 5 scope amendment block)
   ```

5. **Update the bottom-of-file metadata line** (currently the last line: `*Last updated: 2026-04-20 after roadmap creation — CSLC-01 and CSLC-02 moved to Phase 1 (shared stable_terrain.py + selfconsistency.py per research SUMMARY §0.5.5); 49/49 mapped, 0 orphans*`). Append `; 2026-04-25 DIST-01/02/03 marked Deferred to v1.2 per Phase 5 scope amendment (RESEARCH Probes 1 + 6)` to the existing line.

Do NOT touch the requirement count math (49 v1.1 requirements still maps to 49). Deferral is a status, not a removal.

This implements user-locked decisions per the planning context `<scope_amendments>` block item 7.
  </action>
  <verify>
    <automated>grep -c "Deferred to v1.2" .planning/REQUIREMENTS.md | awk '$1 >= 6 {exit 0} {exit 1}' && grep -n "DIST-V2-05" .planning/REQUIREMENTS.md && grep -n "deferred to v1.2 per scope amendment" .planning/REQUIREMENTS.md && grep -n "DIST-01 | Phase 5 | Deferred to v1.2" .planning/REQUIREMENTS.md && grep -n "DIST-04 | Phase 5 | Pending" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Deferred to v1.2" .planning/REQUIREMENTS.md` outputs at least 6 (3 v1.1 bullets + 3 traceability rows; the per-phase counts line + DIST-V2-05 + bottom metadata line raise the count further)
    - `grep -n "DIST-V2-05" .planning/REQUIREMENTS.md` outputs exactly one match
    - `grep -n "DIST-01 | Phase 5 | Deferred to v1.2" .planning/REQUIREMENTS.md` outputs exactly one match
    - `grep -n "DIST-04 | Phase 5 | Pending" .planning/REQUIREMENTS.md` outputs exactly one match (DIST-04 stays Pending)
    - Both Probes are cited verbatim: `grep -c "RESEARCH Probe 1" .planning/REQUIREMENTS.md` >= 1 AND `grep -c "RESEARCH Probe 6" .planning/REQUIREMENTS.md` >= 1
    - Total v1.1 requirement count statement still says `49 total` (the deferral is a status, not a removal): `grep -c "49 total" .planning/REQUIREMENTS.md` >= 1
  </acceptance_criteria>
  <done>REQUIREMENTS.md DIST-01/02/03 v1.1 entries prepend `[deferred to v1.2 — ...]` with evidence-anchored rationales; traceability rows show `Deferred to v1.2`; DIST-V2-05 added to v2 future work with D-16 archival hook callout; per-phase counts annotated; bottom metadata appended with date stamp.</done>
</task>

<task type="auto">
  <name>Task 3: Append Resolution Log section to 05-RESEARCH.md (Risks A-M + Q1-Q5 dispositions)</name>
  <files>.planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md</files>
  <read_first>
    - .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md (ENTIRE file; specifically lines 207-680 for Risks A-M scattered through Probes 1-9, and lines 971-991 for Open Questions Q1-Q5)
    - .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md line 1059 (current `## RESEARCH COMPLETE` marker — the new section inserts BEFORE this marker, AFTER the `## Sources (extended URL list)` and `## Metadata` blocks)
  </read_first>
  <action>
**Append** a new `## Resolution Log` section to `.planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md` immediately BEFORE the existing `## RESEARCH COMPLETE` marker (at line 1059 in the current file).

The Resolution Log enumerates each Open Risk (A through M, scattered across Probes 1-9 in the existing prose) and each Open Question (Q1-Q5 from the `## Open Questions` block at line 971) with its current disposition. Per Dim 11 of the verifier protocol, "any unresolved question is a BLOCKER" — this section makes the dispositions explicit so the verifier can cross-check.

Insert the following block:

```markdown


## Resolution Log

This section maps every Open Risk + Open Question surfaced in the probes
and discussion above to a final disposition for Phase 5. Dim 11 of the
verifier protocol treats any unresolved question as a BLOCKER; the table
below makes each disposition explicit so the verifier can cross-check
against the plan-set.

**Disposition vocabulary:**
- `resolved` — the question has a concrete answer surfaced in the cited
  plan or amendment.
- `accepted-as-narrative` — the risk is real but mitigated only via
  narrative caveat in CONCLUSIONS / docs (no code-level gate).
- `accepted-as-blocker` — the question depends on an external event
  (e.g. operational publication); Phase 5 ships the auto-detection
  scaffolding and the human-readable signal when the event lands.
- `deferred-to-v1.2` — explicitly out of scope for Phase 5; the v1.2
  follow-up plan is named in REQUIREMENTS.md DIST-V2-05.
- `mitigated-via-amendment` — the original framing was structurally
  inapplicable; the resolution is an amendment to the binding decision
  (D-XX) recorded in ROADMAP Phase 5 scope-amendment block.

### Open Risks A-M (from Probes 1-9 prose)

| ID | Description | Disposition | Resolved by |
|---|---|---|---|
| Risk A | OPERA-ADT notebook commit drift (kwargs change in future commits silently invalidate the gate-key list) | accepted-as-narrative | Plan 05-01 amendment cites notebook SHA; CONCLUSIONS_DIST_N_AM.md v1.1 narrative records it. Operationally moot under scope amendment (DIST-02 deferred to v1.2). |
| Risk B | dist-s1 operational ATBD drift (v2.x defaults differ from 2.0.14 once operational publishes) | deferred-to-v1.2 | DIST-V2-05 re-derives the gate-key list from operational ATBD; v1.1 ships against 2.0.14 baseline. |
| Risk C | Tag-level extraction not useful for drift detection (rasterio tags round-trip subsideo's own metadata, not OPERA-ADT's) | resolved | Direct-kwargs comparison adopted as the only mechanism (D-01..D-04 then deferred to v1.2 entirely under scope amendment). |
| Risk D | Plan-phase may re-derive 7-key list differently from a deeper algoconfig_model.py inspection | deferred-to-v1.2 | DIST-V2-05 re-validates the 7-key list against operational ATBD; v1.1 doesn't ship the gate at all. |
| Risk E | Operational v2.x defaults drifting from 2.0.14 makes the gate meaningful | accepted-as-blocker | Stage 0 CMR probe in Plan 05-06 raises NotImplementedError on operational_found; signal triggers v1.2 re-plan. |
| Risk F | EFFIS rate-limits the getfeature endpoint | resolved | Plan 05-04 lands `RETRY_POLICY['EFFIS']` with 429 / 503 / 504 / ConnectionError / TimeoutError retryable + 401 / 403 / 404 abortable; D-18 amendment routes the dispatch through `effis.py` consulting the policy via tenacity/urllib3 wrapper. |
| Risk G | EFFIS may return GML rather than SHAPEZIP (geopandas parser ambiguity) | resolved | Plan 05-05 `effis.py` uses `geopandas.read_file(BytesIO(raw))` which auto-detects GML / SHAPEZIP / SPATIALITEZIP via pyogrio. |
| Risk H | Romania 2022 clear-cuts not covered by EFFIS → recall ~0 → false-FAIL on class mismatch | resolved | Plan 05-01 amendment substitutes Spain Sierra de la Culebra (RESEARCH Probe 4 ADR); Romania removed from EVENTS list in Plan 05-07. |
| Risk I | ±7 days CMR temporal tolerance may be too narrow if operational granule's BeginningDateTime is offset | accepted-as-narrative | Plan 05-06 hardcodes ±7 days (matches T11SLT 12-day Sentinel-1 cycle + 1 day slop); CONCLUSIONS narrates the choice. v1.2 may widen if operational hits surface this. |
| Risk J | DIST-01 reframing (CloudFront URL → notebook recipe) needs explicit milestone-close surfacing | resolved | Plan 05-01 scope-amendment block in ROADMAP makes the reframing the canonical reading; REL-05 closure reads it directly. |
| Risk K | Evros 2023 specific MGRS+track+post_date triple needs `dist_s1_enumerator` probe at plan-phase | resolved | Plan 05-07 adds a Wave-3-internal probe task (track_number resolution via `dist_s1_enumerator.get_mgrs_tiles_overlapping_geometry` over the AOI bbox + date range) before the EVENTS list locks; the speculative `track_number=29` value gets verified at runtime. |
| Risk L | Romania substitution choice (Spain Culebra) needs DIST-runnable + EFFIS-covered cross-check | resolved | RESEARCH Probe 4 ADR confirms Spain Culebra is fire-only EFFIS-covered + has S1 ascending+descending pass coverage in the post-fire window. Plan 05-07 EVENTS list locks the Spain Culebra entry with citations. |
| Risk M | block_bootstrap_ci concatenate inner loop is O(B × n_blocks × pixels²) ≈ ~3min/call | accepted-as-narrative | Plan 05-03's bootstrap.py docstring cites the runtime; `docs/validation_methodology.md §4.1` (Plan 05-09) records the optimisation hook (numba / vectorised gather) for a 2nd consumer. Acceptable for one-shot Phase 5 use. |

### Open Questions Q1-Q5 (from `## Open Questions` block)

| ID | Description | Disposition | Resolved by |
|---|---|---|---|
| Q1 | EFFIS `firedate` property semantic (ignition vs first-detection) | resolved | Plan 05-02 GetCapabilities + smoke-test probes against known fires (Aveiro 2024 Sept 15-16 ignition window) and locks the property semantic by inspecting returned dates against published ignition events. |
| Q2 | Operational `OPERA_L3_DIST-ALERT-S1_V1` first-publication timing | accepted-as-blocker | Phase 5 ships scaffolding (CMR Stage 0 + DEFERRED metrics.json + auto-supersede `NotImplementedError` trigger); Plan 05-06's run_eval_dist.py auto-fills the cell when operational publishes. |
| Q3 | Romania-vs-Spain substitution: hard requirement? | resolved | User-approved Spain Sierra de la Culebra (Plan 05-01 scope amendment + EVENTS list in Plan 05-07). |
| Q4 | Cache-dir consolidation underscore vs hyphen | resolved | `eval-dist_eu` (underscore) is canonical per matrix_manifest.yml line 79; Plan 05-08 git-mv consolidates the v1.0 hyphenated directories. |
| Q5 | Aveiro Oct 10 dist-s1 invocation missing from v1.0 cache (chained_retry middle stage) | resolved | Plan 05-07 EVENTS list aveiro entry has `post_dates=[date(2024, 9, 28), date(2024, 10, 10), date(2024, 11, 15)]`; the missing Oct 10 invocation runs BEFORE the chained_retry sub-stage produces the threaded Nov 15 final output. |

```

The append is approximately 70 lines. Do NOT modify any text above the new section; verify via `git diff | grep "^-"` shows no deletions. The `## RESEARCH COMPLETE` marker stays at the end of the file (the new section inserts BEFORE it, so the file ends with `## RESEARCH COMPLETE` after the append).

Citations to "Plan 05-XX" use the literal plan identifier so a future verifier or reader can grep the resolution log against the plan files directly.
  </action>
  <verify>
    <automated>grep -c "## Resolution Log" .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "Risk A\|Risk B\|Risk C\|Risk D\|Risk E\|Risk F\|Risk G\|Risk H\|Risk I\|Risk J\|Risk K\|Risk L\|Risk M" .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md | awk '$1 >= 13 {exit 0} {exit 1}' && grep -c "| Q1 \|| Q2 \|| Q3 \|| Q4 \|| Q5 " .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md | awk '$1 >= 5 {exit 0} {exit 1}' && grep -c "## RESEARCH COMPLETE" .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md | awk '$1 == 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md` contains exactly one `## Resolution Log` heading
    - The Resolution Log table contains all 13 Risk rows (Risk A through Risk M) — verify via `grep -c "Risk [A-M] \\|" .planning/phases/.../05-RESEARCH.md` >= 13
    - The Resolution Log contains all 5 Open Question rows (Q1, Q2, Q3, Q4, Q5)
    - Every row has a non-empty Disposition column (one of: `resolved`, `accepted-as-narrative`, `accepted-as-blocker`, `deferred-to-v1.2`, `mitigated-via-amendment`)
    - Every row has a non-empty "Resolved by" column citing a Plan ID (`Plan 05-0X`) or amendment reference
    - The `## RESEARCH COMPLETE` marker still exists exactly once and appears AFTER the Resolution Log section
    - `git diff .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md | grep "^-" | grep -v "^---"` produces NO output (zero deletions; this is a pure append)
  </acceptance_criteria>
  <done>05-RESEARCH.md has a Resolution Log section enumerating Risks A-M + Q1-Q5 with explicit dispositions; existing prose byte-identical; verifier Dim 11 cross-check now has a concrete table to read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → repo (.planning/) | Authored markdown edits cross from the editor (Claude / human) into committed planning documents that downstream agents (verifier, planner, executor) consume as instructions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | .planning/ROADMAP.md (existing prose) | mitigate | Edit only the specific lines in the Phase 5 block (lines ~157-173 + progress table row); leave Goal text and Depends-on text intact; verify via `git diff --stat` (only ROADMAP.md, REQUIREMENTS.md, and 05-RESEARCH.md should appear). |
| T-05-01-02 | Information disclosure | .planning/REQUIREMENTS.md (DIST-V2-05 cross-ref) | accept | Cross-reference is to a public ROADMAP.md scope amendment block; no PII or secrets involved. |
| T-05-01-03 | Repudiation | scope-amendment evidence anchors | mitigate | Cite RESEARCH Probe 1 + Probe 6 (file already committed) and the date stamp 2026-04-25 in both files; auditable via `git log --follow .planning/ROADMAP.md`. |
| T-05-01-04 | Denial of service | accidental wholesale rewrite | mitigate | Use `Edit` tool with surgical changes; reject any patch that replaces > 50 lines of existing prose (`git diff --stat` should show < 50 lines added per file for ROADMAP/REQUIREMENTS; the RESEARCH.md append is larger ~70 lines but is a single logical block). |
| T-05-01-05 | Tampering | 05-RESEARCH.md prose above the new section | mitigate | Append-only insertion before `## RESEARCH COMPLETE` marker; `git diff | grep "^-"` must show no deletions of existing rows. |
</threat_model>

<verification>
- `git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md` shows changes localised to the Phase 5 / DIST sections + a new Resolution Log section in 05-RESEARCH.md (not whole-file rewrites).
- `gsd-sdk query frontmatter.validate` passes on all three files (none have frontmatter except plan files; this is a no-op sanity check).
- `git diff .planning/ROADMAP.md` does NOT show any change outside the Phase 5 block (lines ~157 to ~250) or the v1.1 progress table row.
- `git diff .planning/REQUIREMENTS.md` does NOT show any change outside the v1.1 DIST block (lines 62-70), the traceability rows for DIST-01..07 (lines ~192-198), the v2 DIST Future Work block, the per-phase counts line, or the bottom metadata line.
- `git diff .planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-RESEARCH.md` shows ONLY the new Resolution Log section appended before `## RESEARCH COMPLETE`; zero deletions.
</verification>

<success_criteria>
- All `must_haves.truths` observable via `grep` on the three amended files.
- ROADMAP.md, REQUIREMENTS.md, and 05-RESEARCH.md committed to git as a single commit titled `docs(05): defer DIST-01/02/03 to v1.2 + EMSR686 + Spain Culebra substitution + D-18/D-24 amendments + RESEARCH Resolution Log` (commit step is the closing action of the executor; not a separate task).
- Phase 5 progress row in ROADMAP.md reads `0/9 | Planned`.
- DIST-04, DIST-05, DIST-06, DIST-07 retain `Pending` status (only DIST-01/02/03 are deferred).
- v1.1 requirement coverage math still reads `49 total / 49 mapped / 0 unmapped` (deferral is a status, not a removal).
- Verifier Dim 11 (open-questions resolution) cross-check passes against the new Resolution Log table.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dist-s1-opera-v0-1-effis-eu/05-01-SUMMARY.md` documenting: (1) the lines of ROADMAP.md and REQUIREMENTS.md that were edited (with line-number ranges), (2) the four evidence anchors cited (RESEARCH Probe 1, Probe 4, Probe 6, Probe 8), (3) the unchanged DIST-04..07 status, (4) the EMSR686 + Spain substitution corrections, (5) the D-18 + D-24 amendment paragraphs, (6) the Resolution Log row count (13 Risks + 5 Questions = 18 rows).
</output>
</content>
</invoke>