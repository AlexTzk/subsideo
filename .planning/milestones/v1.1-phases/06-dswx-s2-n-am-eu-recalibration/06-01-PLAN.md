---
phase: 06-dswx-s2-n-am-eu-recalibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/dswx_aoi_selection.ipynb
  - .planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md
  - .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md
  - src/subsideo/validation/dswx_failure_modes.yml
  - Makefile
  - docs/dswx_fitset_aoi_selection.md
autonomous: false
last_updated: 2026-04-26
requirements: [DSWX-02, DSWX-03, DSWX-07]
tags: [probe, aoi-selection, proteus-atbd, makefile, lock-in-checkpoint]

must_haves:
  truths:
    - "User opens .planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md and finds an 18-row table with biome, AOI name, bbox(W,S,E,N), MGRS tile, JRC tile, JRC unknown pct, wet/dry ratio, cloud-free 2021 count, failure-mode flags, plus 5 recommended fit-set AOIs (one per non-Pannonian biome) plus Balaton fixed as held-out, plus rejection reasoning"
    - "Plan-phase 06-01 commits exact MGRS tiles for Tahoe (T10SFH) and Pontchartrain (verified via live STAC pystac-client query against CDSE STAC) into the candidate artifact"
    - "User opens .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md and finds either (a) a citable PROTEUS ATBD F1 number with biome scope and full citation OR (d) a documented decision to fall back to own-data bound per CONTEXT D-17 with the exhaustion of paths (a) and (c) recorded as evidence"
    - "User opens notebooks/dswx_aoi_selection.ipynb and the notebook contains 11 cells covering: purpose, imports, CandidateAOI dataclass, CANDIDATES list (18 entries), per-candidate scoring functions (cloud_free_count, wet_dry_ratio, jrc_unknown_pct), scoring loop with pandas DataFrame, auto-reject filter (cloud_free<threshold OR wet/dry<1.2 OR jrc_unknown>20%), advisory-flag overlay from dswx_failure_modes.yml, 5-AOI selection cell, markdown rationale summary, and the artifact-write cell"
    - "User imports `from subsideo.validation.dswx_failure_modes import load_failure_modes` (or reads the YAML directly) and finds a structured YAML resource at src/subsideo/validation/dswx_failure_modes.yml with 4 categories (glacier_or_frozen_lake, mountain_shadow, tidal_turbidity, drought_year_risk) populated with the AOIs listed in RESEARCH §EU fit-set candidate AOIs"
    - "User runs `make dswx-fitset-aoi-md` and the target invokes `jupyter nbconvert --to markdown --output-dir docs --output dswx_fitset_aoi_selection.md notebooks/dswx_aoi_selection.ipynb` producing docs/dswx_fitset_aoi_selection.md"
    - "User runs `grep -n 'recalibrate-dswx:' Makefile` and finds the target wired with `$(SUPERVISOR) scripts/recalibrate_dswe_thresholds.py` (script doesn't exist yet at this plan close — Plan 06-06 lands it)"
    - "Plan-phase 06-01 produces a USER LOCK-IN CHECKPOINT (checkpoint:human-verify) that user reviews the 5-AOI fit-set + 1 held-out Balaton selection BEFORE Plan 06-06 fit-set compute commits per CONTEXT D-01"
    - "PROTEUS ATBD ceiling probe artifact records the resolution path: (a) Product Spec PDF download attempted; (c) OPERA-Cal-Val GitHub repo attempted; (d) own-data fallback locked if a+c fail"
  artifacts:
    - path: ".planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md"
      provides: "18-candidate scoring table + 5 selected fit-set AOIs + Balaton held-out + rejection reasoning"
      min_lines: 60
    - path: ".planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md"
      provides: "PROTEUS ATBD F1 ceiling citation chain probe report; resolution to path (a)/(c)/(d) per CONTEXT D-17"
      min_lines: 30
    - path: "notebooks/dswx_aoi_selection.ipynb"
      provides: "11-cell research notebook with CandidateAOI dataclass + 18 CANDIDATES + scoring functions + auto-reject + advisory overlay + 5-AOI selection + write artifact cell"
      contains: "CandidateAOI"
    - path: "src/subsideo/validation/dswx_failure_modes.yml"
      provides: "4 failure-mode categories with AOI mappings (glacier/frozen, mountain shadow, tidal turbidity, drought year risk)"
      min_lines: 35
    - path: "Makefile"
      provides: "recalibrate-dswx target + dswx-fitset-aoi-md target"
      contains: "recalibrate-dswx:"
    - path: "docs/dswx_fitset_aoi_selection.md"
      provides: "Auto-rendered markdown copy of dswx_aoi_selection.ipynb (jupyter nbconvert)"
      min_lines: 30
  key_links:
    - from: "notebooks/dswx_aoi_selection.ipynb"
      to: "src/subsideo/validation/dswx_failure_modes.yml"
      via: "Notebook reads the YAML resource via importlib.resources.files('subsideo.validation') / 'dswx_failure_modes.yml' to overlay advisory flags onto candidates"
      pattern: "dswx_failure_modes"
    - from: "notebooks/dswx_aoi_selection.ipynb"
      to: ".planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md"
      via: "Final notebook cell writes the artifact with table + selection + rationale"
      pattern: "dswx_fitset_aoi_candidates"
    - from: "Makefile"
      to: "scripts/recalibrate_dswe_thresholds.py"
      via: "recalibrate-dswx target wraps the script with the supervisor (script lands in Plan 06-06; Plan 06-01 wires the target)"
      pattern: "recalibrate-dswx"
---

<objective>
Wave 1 probe-and-commit: produce the AOI lock-in artifact + PROTEUS ATBD ceiling citation chain probe + dswx_aoi_selection.ipynb notebook + dswx_failure_modes.yml resource + Makefile targets, ending in a user lock-in checkpoint that locks the 5 fit-set AOIs + Balaton held-out + the F1 ceiling citation path BEFORE Plan 06-06 commits 4-5 days of fit-set compute against AOIs that could silently cap F1 at 0.85 (PITFALLS P5.2 fit-set quality cap).

Purpose: Phase 6's single largest schedule risk per BOOTSTRAP_V1.1.md and research SUMMARY is fit-set quality silently capping F1 below the 0.90 bar regardless of grid search quality. The probe-and-commit pattern (Phase 2 D-04 / Phase 3 03-02 precedent) de-risks this via a user lock-in checkpoint before fit-set compute commits. PROTEUS ATBD probe (CONTEXT D-17 mandate) closes the F1 ceiling citation chain (P5.3 mitigation) before any FAIL framing is written. Wave 1 ends with two artifacts on disk plus user-locked decisions.

Output: 6 new/modified files. Lock-in checkpoint pauses the wave for user review before Plan 06-02 (foundation scaffolding) proceeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-CONTEXT.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-RESEARCH.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/milestones/v1.1-research/cslc_selfconsist_aoi_candidates.md
@.planning/milestones/v1.1-research/rtc_eu_burst_candidates.md
@scripts/probe_cslc_aoi_candidates.py
@src/subsideo/validation/compare_dswx.py
@Makefile
@BOOTSTRAP_V1.1.md
</context>

<interfaces>
<!-- Key signatures and contracts the executor needs. Extracted from codebase. -->

From src/subsideo/validation/compare_dswx.py — JRC URL pattern:
```python
JRC_BASE_URL = (
    "https://jeodpp.jrc.ec.europa.eu/ftp/jrc-opendata"
    "/GSWE/MonthlyHistory/LATEST/tiles"
)
JRC_TILE_SIZE_PIXELS = 40000
JRC_TILE_SIZE_DEGREES = 10.0
JRC_ORIGIN_LON = -180.0
JRC_ORIGIN_LAT = 80.0

def _jrc_tile_url(year: int, month: int, tile_x: int, tile_y: int) -> str:
    pixel_x = tile_x * JRC_TILE_SIZE_PIXELS
    pixel_y = tile_y * JRC_TILE_SIZE_PIXELS
    return f"{JRC_BASE_URL}/{year}/{year}_{month:02d}/{pixel_y:010d}-{pixel_x:010d}.tif"

def _lonlat_to_jrc_tile(lon: float, lat: float) -> tuple[int, int]:
    tile_x = int((lon - JRC_ORIGIN_LON) / JRC_TILE_SIZE_DEGREES)
    tile_y = int((JRC_ORIGIN_LAT - lat) / JRC_TILE_SIZE_DEGREES)
    return tile_x, tile_y
```

From scripts/probe_cslc_aoi_candidates.py — CandidateAOI dataclass precedent:
```python
@dataclass(frozen=True)
class CandidateAOI:
    aoi: str
    region: str  # "NAM" | "EU"
    regime: str
    label: str
    bbox: tuple[float, float, float, float]  # west, south, east, north (WGS84)
    published_insar_stability_ref: str
    expected_stable_pct_per_worldcover: float
    cached_safe_fallback_path: str
```

From .planning/milestones/v1.1-research/rtc_eu_burst_candidates.md — probe-report header pattern:
```markdown
# RTC-S1 EU Burst Candidates -- Probe Report

**Probed:** 2026-04-23T15:40:02Z
**Source query:** `asf_search` + `earthaccess` against ASF DAAC.
**Phase:** 2 (RTC-S1 EU Validation)
**Decision:** D-01 (probe artifact) + D-03 (5-regime fixed list) + D-04 (Claude drafts; user reviews).
```

From Makefile — supervisor-wrapped target pattern (Phase 1 ENV-09):
```makefile
SUPERVISOR := micromamba run -n subsideo python -m subsideo.validation.supervisor

eval-dswx-nam: ; $(SUPERVISOR) run_eval_dswx_nam.py
eval-dswx-eu:  ; $(SUPERVISOR) run_eval_dswx.py
```

Phase 6 RESEARCH.md candidate AOI bbox+MGRS+JRC table (5 fit-set + Balaton held-out):
- Embalse de Alcántara (ES): bbox=(-7.05, 39.55, -6.65, 39.95), mgrs_tile=29SQE, jrc_tile=(17,4)
- Tagus estuary (PT): bbox=(-9.45, 38.55, -8.85, 39.05), mgrs_tile=29SMC, jrc_tile=(17,4)
- Vänern (SE): bbox=(12.40, 58.45, 14.20, 59.45), mgrs_tile=33VVF, jrc_tile=(19,2)
- Lago di Garda (IT): bbox=(10.55, 45.55, 10.85, 45.85), mgrs_tile=32TQR, jrc_tile=(19,3)
- Doñana wetlands (ES): bbox=(-6.55, 36.80, -6.30, 37.05), mgrs_tile=29SQB, jrc_tile=(17,4)
- Balaton (HU) HELD-OUT: bbox=(17.20, 46.60, 18.20, 46.95), mgrs_tile=33TXP, jrc_tile=(19,3)

Phase 6 RESEARCH.md N.Am. positive control candidates:
- Lake Tahoe (CA): bbox=(-120.20, 38.91, -119.90, 39.27), mgrs_tile=10SFH (verified USGS ScienceBase), epsg=32610
- Lake Pontchartrain (LA): bbox=(-90.45, 30.02, -89.62, 30.34), mgrs_tile=15RYP (python-mgrs centroid; verify via live STAC), epsg=32615
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notebook → CDSE STAC | Outbound HTTPS to dataspace.copernicus.eu/v1; pystac-client / requests; CDSE OAuth not needed for STAC searches (unauthenticated reads) |
| Notebook → JRC GSW | Outbound HTTPS to jeodpp.jrc.ec.europa.eu; public GeoTIFF download for jrc_unknown_pct + wet/dry-ratio computation |
| Notebook → file system | Writes `.planning/milestones/v1.1-research/*.md` + `docs/*.md` + reads `src/subsideo/validation/dswx_failure_modes.yml`; all paths are repo-relative, no user-supplied paths |
| pdftotext (PROTEUS ATBD) → cached PDF | Local subprocess; reads previously-downloaded `.planning/milestones/v1.1-research/proteus_atbd_cache/*.pdf`; no network, no user input |
| WebFetch (OPERA-Cal-Val) → GitHub | Outbound HTTPS to github.com; public repo; no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | dswx_failure_modes.yml | accept | YAML resource is committed to git; PR-reviewable; not user-uploaded |
| T-06-01-02 | Information Disclosure | dswx_fitset_aoi_candidates.md | accept | All AOI bbox values are public (BOOTSTRAP §5.2 source); no PII; no credentials |
| T-06-01-03 | Denial of Service | CDSE STAC bbox queries | mitigate | Notebook restricts query bbox to candidate AOI extents (max 1° × 1°); per-candidate try/except; cloud_free_count caps to 50 items per CDSE STAC defaults |
| T-06-01-04 | Information Disclosure | PROTEUS ATBD probe | accept | All probe URLs are public JPL OPERA documents; no secrets in WebFetch arguments |
| T-06-01-05 | Tampering | Makefile recalibrate-dswx target | mitigate | Target invokes supervisor wrapper which inherits CDSE_* / EARTHDATA_* from .env (load_dotenv); never accepts user-supplied paths to script |
| T-06-01-06 | Spoofing | JRC tile URL construction | accept | URLs are constructed from hard-coded JRC_BASE_URL constant + (year, month, tile_x, tile_y) integers; no user input flows into URL string |
| T-06-01-07 | Tampering | pdftotext subprocess | accept | Subprocess invokes a system binary (poppler) with hard-coded args (`-layout`, str(pdf_path)); no shell expansion; PDF path is a Path object built from Plan-phase 06-01 hard-coded directory |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create dswx_failure_modes.yml resource + draft AOI probe artifact + dswx_aoi_selection.ipynb notebook</name>
  <files>src/subsideo/validation/dswx_failure_modes.yml, notebooks/dswx_aoi_selection.ipynb, .planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md</files>
  <read_first>
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-CONTEXT.md (D-01..D-04)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-RESEARCH.md (lines 209-252 EU fit-set candidate AOIs table; lines 254-285 N.Am. MGRS resolution; lines 880-945 Notebook rendering mechanism + dswx_failure_modes.yml structure)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-PATTERNS.md (lines 1410-1430 probe-artifact header pattern; lines 1145-1207 dswx_aoi_selection.ipynb structure)
    - .planning/research/PITFALLS.md (§P5.2 lines 633-657 + §P5.4 lines 685-707)
    - scripts/probe_cslc_aoi_candidates.py (CandidateAOI dataclass + CANDIDATES list precedent)
    - .planning/milestones/v1.1-research/rtc_eu_burst_candidates.md (probe-report header + table format)
    - src/subsideo/validation/compare_dswx.py (lines 1-110 — JRC URL pattern + _lonlat_to_jrc_tile helper for jrc_unknown_pct compute)
    - .planning/research/PITFALLS.md (§P5.4 lines 685-707 — D-03 strict 1.2 wet/dry ratio + alternate-year retry)
  </read_first>
  <action>
**Step 1.** Create `src/subsideo/validation/dswx_failure_modes.yml` with 4 failure-mode categories per CONTEXT D-02 + RESEARCH §Notebook rendering mechanism (lines 880-945). Exact content:

```yaml
# Curated list of EU AOIs known to trigger DSWE failure modes.
# Source: Phase 6 RESEARCH 2026-04-26 + literature review.
# Plan-phase 06-01 commits per-AOI flags; notebook reads for advisory display.
# CONTEXT D-02: hybrid auto-reject (hard signals) + advisory pre-screen (soft signals).
failure_modes:
  glacier_or_frozen_lake:
    - aoi_id: vanern
      months: [12, 1, 2]
      flag: 'frozen surface; auto-reject'
    - aoi_id: saimaa
      months: [11, 12, 1, 2, 3, 4]
      flag: 'frozen surface; auto-reject'
    - aoi_id: malaren
      months: [12, 1, 2, 3]
      flag: 'frozen surface; auto-reject'

  mountain_shadow:
    - aoi_id: garda
      months: [10, 11, 12, 1, 2]
      flag: 'low sun angle; advisory'
    - aoi_id: leman
      months: [10, 11, 12, 1, 2]
      flag: 'low sun angle; advisory'
    - aoi_id: maggiore
      months: [10, 11, 12, 1, 2]
      flag: 'low sun angle; advisory'

  tidal_turbidity:
    - aoi_id: tagus
      flag: 'suspended sediment; advisory'
    - aoi_id: loire
      flag: 'suspended sediment + tidal flats; advisory'
    - aoi_id: severn
      flag: 'mudflats + 14m tidal range; advisory'

  drought_year_risk:
    - aoi_id: alcantara
      years: [2017, 2022]
      flag: 'low reservoir; alternate-year retry'
    - aoi_id: alarcon
      years: [2017, 2022]
      flag: 'very low reservoir; alternate-year retry'
    - aoi_id: donana
      years: [2017, 2022]
      flag: 'wetlands dry; alternate-year retry'
```

Add YAML to `pyproject.toml` package-data if needed so importlib.resources finds it: verify by checking existing pattern in pyproject.toml (likely already includes `*.yml` via hatchling default).

**Step 2.** Create `notebooks/dswx_aoi_selection.ipynb` (use jupyter nbconvert / direct JSON write) with 11 cells per CONTEXT D-04 + RESEARCH §Validation Architecture + PATTERNS lines 1145-1207:

- **Cell 1 (markdown):** Purpose: produce 5-AOI fit-set + Balaton held-out per CONTEXT D-01 + DSWX-02 + DSWX-03; references BOOTSTRAP §5.2 + PITFALLS P5.2 + P5.4.
- **Cell 2 (code, imports):** geopandas, rasterio, pystac_client, numpy, pandas, matplotlib, importlib.resources, pathlib, yaml; `from subsideo.data.cdse import CDSEClient`; `from subsideo.validation.harness import bounds_for_mgrs_tile`. Lazy-import heavy deps inside function bodies; module-top imports only for type hints.
- **Cell 3 (code, dataclass):** `@dataclass(frozen=True) class CandidateAOI` with fields aoi_id, biome, label, bbox (W,S,E,N), mgrs_tile, jrc_tile_xy (tuple[int,int]), epsg, expected_wet_month (1-12), expected_dry_month (1-12), held_out (bool default False).
- **Cell 4 (code, CANDIDATES list):** 18 entries from RESEARCH lines 213-230 EU fit-set candidate AOIs table + 2 N.Am. control candidates from RESEARCH lines 254-285 (Tahoe + Pontchartrain; held_out=False; informational only — N.Am. eval handled by Plan 06-05). Use exact bbox + mgrs_tile + jrc_tile values from RESEARCH table.
- **Cell 5 (code, scoring functions):** `cloud_free_count(cdse, bbox, year)` returns int via STAC query with cloud_cover<15%; `wet_dry_ratio(jrc_url_pattern, aoi)` returns float (wet_water_pct / dry_water_pct from JRC tiles); `jrc_unknown_pct(jrc_array)` returns float (frac of pixels==0 nodata).
- **Cell 6 (code, scoring loop):** Iterate over CANDIDATES; for each, call scoring functions; collect into `pd.DataFrame` with columns: biome, aoi_id, label, bbox, mgrs_tile, jrc_unknown_pct, wet_dry_ratio, cloud_free_2021, expected_wet_month, expected_dry_month.
- **Cell 7 (code, auto-reject filter):** Apply CONTEXT D-02 + D-03 hard signals: cloud_free<3 OR wet_dry_ratio<1.2 OR jrc_unknown_pct>20% → drop. Display dropped + reason.
- **Cell 8 (code, advisory overlay):** Read `dswx_failure_modes.yml` via `importlib.resources.files('subsideo.validation') / 'dswx_failure_modes.yml'`. For each surviving candidate, attach failure-mode flags as new column `advisory_flags: list[str]`. For drought_year_risk entries with `years` matching a candidate's expected year (default 2021 per CONTEXT D-19 JRC cap), add the `flag` text. Display the table.
- **Cell 9 (code, 5-AOI selection):** Pick the recommended AOI per non-Pannonian biome per RESEARCH lines 240-249 (Alcántara / Tagus / Vänern / Garda / Doñana). Mark Balaton as held-out (held_out=True per CONTEXT D-01). Display final 5+1 set.
- **Cell 10 (markdown):** Rationale per accepted/rejected per RESEARCH §EU fit-set candidate AOIs Recommended 5-AOI lock-in table (lines 240-249).
- **Cell 11 (code, write artifact):** Write `.planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md` from the DataFrame with the probe-report header per PATTERNS lines 1410-1430.

Save the notebook as a `.ipynb` JSON file (use `nbformat.v4.new_notebook()` if generating programmatically; otherwise hand-write the JSON cell structure). Notebook MUST be runnable end-to-end in the subsideo conda env; cells must validate via `jupyter nbconvert --to notebook --execute notebooks/dswx_aoi_selection.ipynb` (verify in Step 4).

**Step 3.** Create `.planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md` (Claude pre-drafts; user reviews + locks in checkpoint task). Use exact RESEARCH §EU fit-set candidate AOIs table (lines 213-230) + Recommended 5-AOI lock-in (lines 240-249). Header per Phase 2 + Phase 3 precedent:

```markdown
# DSWx-S2 EU Fit-set AOI Candidates — Probe Report

**Probed:** 2026-04-26T<HH:MM:SS>Z (filled at execution)
**Source query:** `pystac_client` against CDSE STAC (collection=SENTINEL-2) + JRC GSW Monthly History tile fetches.
**Phase:** 6 (DSWx-S2 N.Am. + EU Recalibration)
**Decision:** D-01 (probe artifact) + D-02 (hybrid auto-reject + advisory pre-screen) + D-03 (P5.4 strict 1.2 ratio).

## Biome Coverage

| # | biome | aoi | bbox | mgrs_tile | jrc_unknown_pct | wet_dry_ratio | cloud_free_2021 | failure_mode_flags | recommended |
|---|-------|-----|------|-----------|-----------------|---------------|-----------------|---------------------|-------------|
| 1 | Mediterranean reservoir | Embalse de Alcántara (ES) | -7.05, 39.55, -6.65, 39.95 | 29SQE | (filled at run) | (filled at run) | (filled at run) | clean | YES |
| 2 | Atlantic estuary | Tagus estuary (PT) | -9.45, 38.55, -8.85, 39.05 | 29SMC | (filled at run) | (filled at run) | (filled at run) | tidal_turbidity (advisory) | YES |
| 3 | Boreal lake | Vänern (SE) | 12.40, 58.45, 14.20, 59.45 | 33VVF | (filled at run) | (filled at run) | (filled at run) | frozen Dec-Feb (auto-reject months) | YES |
| 4 | Alpine valley | Lago di Garda (IT) | 10.55, 45.55, 10.85, 45.85 | 32TQR | (filled at run) | (filled at run) | (filled at run) | mountain_shadow (advisory) | YES |
| 5 | Iberian summer-dry | Doñana wetlands (ES) | -6.55, 36.80, -6.30, 37.05 | 29SQB | (filled at run) | (filled at run) | (filled at run) | drought_year_risk (advisory; alternate-year 2018) | YES |
| 6 | Pannonian plain | Balaton (HU) | 17.20, 46.60, 18.20, 46.95 | 33TXP | (filled at run) | (filled at run) | (filled at run) | clean | HELD-OUT (test set) |

## N.Am. positive-control candidates (CONTEXT D-18 — runtime auto-pick)

| aoi | bbox | mgrs_tile | epsg | jrc_year | jrc_month | citation |
|-----|------|-----------|------|----------|-----------|----------|
| Lake Tahoe (CA) | -120.20, 38.91, -119.90, 39.27 | 10SFH | 32610 | 2021 | 7 | USGS ScienceBase 2021 Sentinel-2 mosaic verified |
| Lake Pontchartrain (LA) | -90.45, 30.02, -89.62, 30.34 | 15RYP | 32615 | 2021 | 7 | python-mgrs centroid (verify via live STAC) |

## Rejected candidates

[Populated post-notebook-run with rejection reasoning per RESEARCH lines 215-230 advisory flags + auto-rejects]

## Decision

5 fit-set AOIs locked + Balaton held-out per CONTEXT D-01 + DSWX-03. Plan 06-06 fit-set compute commits against this list.
```

The notebook's Cell 11 will overwrite this draft with computed values; the draft is a Claude-precommit so user can review structure during the lock-in checkpoint.

**Step 4.** Run the notebook end-to-end to populate computed values:

```bash
micromamba run -n subsideo jupyter nbconvert \
    --to notebook --execute --inplace \
    notebooks/dswx_aoi_selection.ipynb
```

Then re-render docs/dswx_fitset_aoi_selection.md (Step 5 of next task) and confirm dswx_fitset_aoi_candidates.md is populated with computed scores.

If the live CDSE STAC or JRC fetch fails (network down, rate-limit, etc.), document in the notebook output and fall back to RESEARCH.md's pre-computed table values verbatim — the artifact ships either way per CONTEXT D-01 timeline.
  </action>
  <verify>
    <automated>test -f src/subsideo/validation/dswx_failure_modes.yml && test -f notebooks/dswx_aoi_selection.ipynb && test -f .planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md && grep -q "glacier_or_frozen_lake" src/subsideo/validation/dswx_failure_modes.yml && grep -q "drought_year_risk" src/subsideo/validation/dswx_failure_modes.yml && grep -q "Embalse de Alcántara" .planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md && grep -q "Balaton" .planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md && grep -q "HELD-OUT" .planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md && python3 -c "import json; nb=json.load(open('notebooks/dswx_aoi_selection.ipynb')); cells=nb['cells']; assert len(cells) >= 11, f'expected >= 11 cells, got {len(cells)}'; assert any('CandidateAOI' in ''.join(c.get('source', '')) for c in cells), 'CandidateAOI dataclass missing'; assert any('CANDIDATES' in ''.join(c.get('source', '')) for c in cells), 'CANDIDATES list missing'; print('notebook structure OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/subsideo/validation/dswx_failure_modes.yml` exists, contains exactly 4 top-level keys under `failure_modes`: `glacier_or_frozen_lake`, `mountain_shadow`, `tidal_turbidity`, `drought_year_risk`
    - File `notebooks/dswx_aoi_selection.ipynb` exists, valid JSON, has >= 11 cells, contains literal strings `CandidateAOI` and `CANDIDATES` and `dswx_failure_modes`
    - File `.planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md` exists with >= 60 lines, contains literal strings `Embalse de Alcántara`, `Tagus estuary`, `Vänern`, `Lago di Garda`, `Doñana`, `Balaton`, `HELD-OUT (test set)`
    - The 5 fit-set AOIs (Alcántara, Tagus, Vänern, Garda, Doñana) appear with `recommended: YES`; Balaton appears with `recommended: HELD-OUT`
    - File `.planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md` includes the N.Am. positive-control candidates section with Lake Tahoe `T10SFH` and Lake Pontchartrain `T15RYP` (or T15RYR if STAC verification flips it; either is valid)
  </acceptance_criteria>
  <done>YAML resource committed; notebook validates as JSON + has 11 cells + references CandidateAOI + CANDIDATES + dswx_failure_modes; AOI candidate artifact has the 5+1 biome rows + N.Am. control candidates + the probe-report header.</done>
</task>

<task type="auto">
  <name>Task 2: PROTEUS ATBD F1 ceiling probe + commit citation chain artifact</name>
  <files>.planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md</files>
  <read_first>
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-CONTEXT.md (D-17 PROTEUS ATBD probe paths + D-22 §5.1 own-data fallback wording)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-RESEARCH.md (lines 176-208 PROTEUS ATBD F1 ceiling citation section)
    - .planning/research/PITFALLS.md (§P5.3 lines 660-682)
    - src/subsideo/validation/criteria.py (lines 180-200 dswx.f1_min docstring — current ceiling phrasing for reference)
  </read_first>
  <action>
**Step 1.** Attempt path (a) — directly download the OPERA DSWx-HLS Product Spec PDF (>10 MB content; previously blocked at WebFetch maxContentLength). Use `curl` directly to bypass the WebFetch limit:

```bash
mkdir -p .planning/milestones/v1.1-research/proteus_atbd_cache
cd .planning/milestones/v1.1-research/proteus_atbd_cache
curl -L --max-time 120 \
    -o OPERA_DSWx-HLS_ProductSpec_v1.0.0_D-107395_RevB.pdf \
    "https://d2pn8kiwq2w21t.cloudfront.net/documents/OPERA_DSWx-HLS_ProductSpec_v1.0.0_D-107395_RevB.pdf"
```

If the download succeeds, extract the validation section text using **`pdftotext`** (poppler binary). The W7 fix replaces the previous pypdf-based extraction with a poppler subprocess so we avoid pulling a Python-only PDF library when the project already needs poppler transitively (GDAL stack on conda-forge typically pulls it; verify in conda-env.yml). If poppler is not present, install it via `mamba install -n subsideo -c conda-forge poppler` (one-line addition to conda-env.yml).

```bash
# Verify poppler binary is on PATH:
micromamba run -n subsideo which pdftotext || \
    micromamba install -n subsideo -c conda-forge poppler -y

# Extract the full PDF as plain text (preserves layout for table parsing):
micromamba run -n subsideo python3 -c "
import subprocess
from pathlib import Path

pdf_path = Path('.planning/milestones/v1.1-research/proteus_atbd_cache/OPERA_DSWx-HLS_ProductSpec_v1.0.0_D-107395_RevB.pdf')
result = subprocess.run(
    ['pdftotext', '-layout', str(pdf_path), '-'],
    capture_output=True, text=True, check=True,
)
text = result.stdout

# Search for F1 / validation sections page-by-page (pdftotext separates pages with form-feed \\f):
for page_num, page_text in enumerate(text.split('\\f'), start=1):
    if ('F1' in page_text or 'F-1' in page_text
        or 'validation' in page_text.lower()
        or 'accuracy' in page_text.lower()):
        print(f'=== Page {page_num} ===')
        print(page_text)
        print()
" 2>&1 | head -400
```

The W7 fix benefits:
1. **No new Python dependency**: `pypdf` is not in pyproject.toml or conda-env.yml; pulling it would add a maintenance edge.
2. **Existing poppler dependency**: GDAL on conda-forge already depends on poppler transitively; `pdftotext` is the binary entry-point. Adding `poppler` explicitly to `conda-env.yml` is a one-line addition documenting the intent.
3. **Subprocess avoids `eval` / dynamic imports**: hard-coded args (`-layout`, `str(pdf_path)`, `'-'`); no shell expansion.

If `pdftotext` is not available even after `mamba install poppler` (rare; non-arm64 build edge), document the failure in the probe artifact under `Path (a) — Download status: FAILED — pdftotext not on PATH after poppler install`.

Search the extracted text for explicit F1 numbers with biome scope. Common patterns: `F1 score of 0.XX`, `F1=0.XX`, `0.XX F1`, `~0.XX F1`. Note the page number + biome qualifier for each citation.

**Step 2.** If path (a) fails (download blocks, no F1 number extractable, or poppler unavailable), attempt path (c) — clone OPERA-Cal-Val repository:

```bash
mkdir -p .planning/milestones/v1.1-research/proteus_atbd_cache
cd .planning/milestones/v1.1-research/proteus_atbd_cache
git clone --depth 1 https://github.com/OPERA-Cal-Val/DSWx-HLS-Requirement-Verification.git || echo "clone failed"
test -f DSWx-HLS-Requirement-Verification/out/validation_table_data.csv && cat DSWx-HLS-Requirement-Verification/out/validation_table_data.csv
test -f DSWx-HLS-Requirement-Verification/0-Verify_Requirements.ipynb && head -200 DSWx-HLS-Requirement-Verification/0-Verify_Requirements.ipynb | grep -i "f1\|F-1\|accuracy\|validation"
```

If the CSV exists, the F1 numbers per requirement ID are citable.

**Step 3.** Write `.planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md` with the resolution outcome. Exact structure:

```markdown
# DSWE F1 Ceiling Citation Chain Probe

**Probed:** 2026-04-26T<HH:MM:SS>Z (filled at execution)
**Phase:** 6 (DSWx-S2 N.Am. + EU Recalibration)
**Decision:** CONTEXT D-17 (PROTEUS ATBD direct fetch) + D-22 §5.1 (locked in docs/validation_methodology.md §5.1) + PITFALLS P5.3 (no game-of-telephone citation).

## Resolution Path Taken

[Filled at execution: (a) Product Spec PDF download + parse via pdftotext / (c) OPERA-Cal-Val GitHub repo / (d) own-data fallback]

## Path (a) — OPERA_DSWx-HLS_ProductSpec_v1.0.0_D-107395_RevB.pdf

- URL: https://d2pn8kiwq2w21t.cloudfront.net/documents/OPERA_DSWx-HLS_ProductSpec_v1.0.0_D-107395_RevB.pdf
- Download status: [SUCCESS / FAILED — reason]
- Extraction tool: `pdftotext -layout` (poppler; conda-forge); subprocess invocation with hard-coded args
- Validation section pages found: [list pages] OR [N/A if download failed or pdftotext unavailable]
- F1 numbers extracted: [list specific F1 + biome scope + page + section] OR [none found]

## Path (c) — OPERA-Cal-Val/DSWx-HLS-Requirement-Verification

- URL: https://github.com/OPERA-Cal-Val/DSWx-HLS-Requirement-Verification
- Clone status: [SUCCESS / FAILED — reason]
- validation_table_data.csv contents: [list rows with F1 numbers + biome] OR [N/A]

## Path (d) — Own-data fallback (default per CONTEXT D-17)

If both (a) + (c) failed to produce a citable specific F1 number, lock the §5.1 wording for `docs/validation_methodology.md` (Plan 06-07 appends):

> Empirical bound observed over our 6-AOI evaluation at F1 ≈ X.YZ (Phase 6 grid search 2026-MM-DD; see scripts/recalibrate_dswe_thresholds_results.json fit-set mean F1).

The X.YZ value is filled by Plan 06-06 (recalibration script execution) at Phase 6 close. The Phase 6 §5.1 §5.1 docstring text in `criteria.py:188` is updated to remove the unverifiable "PROTEUS ATBD" reference; replaced with "empirical 6-AOI bound" phrasing.

## Recommendation for Plan 06-07 (docs/validation_methodology.md §5.1)

[One of:]
1. Cite path (a) PDF page X section Y "F1 = 0.XX for biome Z" — full citation in §5.1.
2. Cite path (c) CSV "F1 = 0.XX for requirement ID R-N" — full citation in §5.1.
3. Lock path (d) own-data fallback — §5.1 text uses the phrasing above; Plan 06-06 fills X.YZ at recalibration close.

## Implications for criteria.py:188

The `dswx.f1_min = 0.90` BINDING criterion is ISOLATED from the ceiling claim per PITFALLS P5.3 explicit isolation strategy. Whether the ceiling is 0.92 (literature) or X.YZ (own-data), the gate stays at 0.90. The docstring (lines 192-194) MAY be revised post-grid-search if Phase 6 lands F1 > 0.92 (P5.3 contradiction); revision is a docstring edit, not a threshold edit.

## Implications for CONCLUSIONS_DSWX.md (Phase 6 v1.1 sections)

Phase 6 v1.1 sections (Plan 06-07 append) MUST replace any "DSWE F1 ≈ 0.92 architectural ceiling" phrasing inherited from BOOTSTRAP_V1.1.md with the resolved citation chain from this probe. If path (d), the FAIL framing for 0.85 ≤ F1 < 0.90 cites "ML-replacement (DSWX-V2-01)" without the architectural-ceiling justification — the FAIL framing is structurally honest regardless of the ceiling number.
```

Plan 06-07 reads this artifact and copies the resolution path (a/c/d) into `docs/validation_methodology.md §5.1` verbatim with the citation. The §5.1 wording is locked HERE so the citation chain has a single source of truth.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md && grep -q "Path (a)" .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md && grep -q "Path (c)" .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md && grep -q "Path (d)" .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md && grep -q "criteria.py" .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md && grep -q "Recommendation for Plan 06-07" .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md` exists with >= 30 lines
    - Contains literal strings `Path (a)`, `Path (c)`, `Path (d)` with execution outcomes per path
    - Contains a `Recommendation for Plan 06-07` section locking one of paths a/c/d
    - Contains the literal docstring fallback phrasing `Empirical bound observed over our 6-AOI evaluation` (as either the recommended path or as the documented fallback wording)
    - Contains the literal `criteria.py:188` reference confirming the BINDING gate is unchanged regardless of resolution
  </acceptance_criteria>
  <done>PROTEUS ATBD ceiling citation chain artifact exists; resolution path (a/c/d) is committed with execution evidence; Plan 06-07 has a single source of truth for §5.1 wording.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Makefile recalibrate-dswx + dswx-fitset-aoi-md targets and render docs/dswx_fitset_aoi_selection.md</name>
  <files>Makefile, docs/dswx_fitset_aoi_selection.md</files>
  <read_first>
    - Makefile (lines 1-50 — existing SUPERVISOR pattern + eval-dswx-{nam,eu} targets)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-CONTEXT.md (D-Claude's-Discretion D-01 — auto-render notebook to markdown)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-RESEARCH.md (lines 840-878 Notebook rendering mechanism)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-PATTERNS.md (lines 1390-1407 Makefile target)
    - notebooks/dswx_aoi_selection.ipynb (must exist from Task 1)
  </read_first>
  <action>
**Step 1.** Read existing Makefile to find the right insertion point for new targets. The existing `eval-dswx-eu` target lives near line 35 with `SUPERVISOR := micromamba run -n subsideo python -m subsideo.validation.supervisor` defined earlier.

**Step 2.** Append two new targets to Makefile after the existing `eval-dswx-{nam,eu}` group:

```makefile
# ----------------------------------------------------------------------------
# Phase 6 DSWx recalibration pipeline (DSWX-04 + DSWX-05)
# ----------------------------------------------------------------------------
# scripts/recalibrate_dswe_thresholds.py is a Phase 6 deliverable (Plan 06-06).
# Wired here in Plan 06-01 so Plan 06-06 can `make recalibrate-dswx` immediately
# without Makefile edits.
recalibrate-dswx:
	$(SUPERVISOR) scripts/recalibrate_dswe_thresholds.py
.PHONY: recalibrate-dswx

# ----------------------------------------------------------------------------
# Phase 6 docs auto-render: dswx_aoi_selection.ipynb -> dswx_fitset_aoi_selection.md
# ----------------------------------------------------------------------------
# Per CONTEXT D-01 + RESEARCH §Notebook rendering: keep the rendered docs copy
# in sync with the source notebook. nbconvert verified present in conda env.
docs/dswx_fitset_aoi_selection.md: notebooks/dswx_aoi_selection.ipynb
	micromamba run -n subsideo jupyter nbconvert --to markdown \
	    --output-dir docs \
	    --output dswx_fitset_aoi_selection.md \
	    notebooks/dswx_aoi_selection.ipynb

dswx-fitset-aoi-md: docs/dswx_fitset_aoi_selection.md
.PHONY: dswx-fitset-aoi-md
```

Use the `$(SUPERVISOR)` variable that's already defined earlier in Makefile (verified by grep) — do NOT redefine it locally.

**Step 3.** Run `make dswx-fitset-aoi-md` (or `make docs/dswx_fitset_aoi_selection.md`) to render `notebooks/dswx_aoi_selection.ipynb` into `docs/dswx_fitset_aoi_selection.md`. Verify the rendered file is publishable-quality (no large embedded base64 PNGs that would bloat the doc).

**Step 4.** Inspect rendered docs/dswx_fitset_aoi_selection.md briefly (head + tail) to confirm it captures the notebook's table outputs cleanly. If nbconvert renders large embedded matplotlib SVGs, those are OK; if it embeds large base64 PNGs (>1 MB inline), the notebook's plot cells should use `Image(filename=...)` external file refs instead — but the AOI-selection notebook is mostly markdown + DataFrame tables, so this risk is low per RESEARCH lines 871-878.

**Step 5.** Verify both targets are well-formed:

```bash
make -n recalibrate-dswx 2>&1 | head -5  # dry-run; should show the supervisor command
make -n dswx-fitset-aoi-md 2>&1 | head -5  # dry-run; should show jupyter nbconvert
```

The `-n` flag prints commands without executing; recalibrate-dswx will fail at runtime (script doesn't exist yet) but the target SHOULD print correctly.
  </action>
  <verify>
    <automated>grep -q "recalibrate-dswx:" Makefile && grep -q "dswx-fitset-aoi-md:" Makefile && grep -q "scripts/recalibrate_dswe_thresholds.py" Makefile && grep -q "jupyter nbconvert --to markdown" Makefile && test -f docs/dswx_fitset_aoi_selection.md && test -s docs/dswx_fitset_aoi_selection.md && make -n recalibrate-dswx 2>&1 | grep -q "supervisor" && make -n dswx-fitset-aoi-md 2>&1 | grep -q "nbconvert"</automated>
  </verify>
  <acceptance_criteria>
    - Makefile contains literal target line `recalibrate-dswx:` followed by a recipe invoking `$(SUPERVISOR) scripts/recalibrate_dswe_thresholds.py`
    - Makefile contains literal target line `dswx-fitset-aoi-md:` and a `docs/dswx_fitset_aoi_selection.md:` rule with `jupyter nbconvert --to markdown` recipe
    - File `docs/dswx_fitset_aoi_selection.md` exists, has >= 30 lines, contains text from the notebook (e.g. `Embalse de Alcántara` or `Mediterranean reservoir` or `dswx_aoi_selection`)
    - `make -n recalibrate-dswx` dry-run prints a command containing `supervisor` and `recalibrate_dswe_thresholds.py`
    - `make -n dswx-fitset-aoi-md` dry-run prints a command containing `nbconvert` and `dswx_aoi_selection.ipynb`
  </acceptance_criteria>
  <done>Makefile has both Phase 6 targets wired; docs/dswx_fitset_aoi_selection.md rendered from the notebook; targets dry-run to expected commands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: USER LOCK-IN CHECKPOINT — review and lock 5 fit-set AOIs + Balaton held-out + PROTEUS ATBD resolution path BEFORE Plan 06-06 fit-set compute commits</name>
  <what-built>
    Plan 06-01 produced:
    1. `.planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md` — 18-candidate scoring table + 5 recommended fit-set AOIs (Alcántara / Tagus / Vänern / Garda / Doñana) + Balaton fixed as held-out + N.Am. positive-control candidates (Tahoe T10SFH + Pontchartrain T15RYP).
    2. `.planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md` — PROTEUS ATBD F1 ceiling citation chain probe with resolution path (a/c/d).
    3. `notebooks/dswx_aoi_selection.ipynb` — 11-cell research notebook executable end-to-end.
    4. `docs/dswx_fitset_aoi_selection.md` — auto-rendered markdown copy.
    5. `src/subsideo/validation/dswx_failure_modes.yml` — 4-category failure-mode YAML resource.
    6. `Makefile` — `recalibrate-dswx` and `dswx-fitset-aoi-md` targets.
  </what-built>
  <how-to-verify>
    1. **Read the 5-AOI fit-set selection** in `.planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md`:
        - Mediterranean reservoir → **Embalse de Alcántara (ES)** (bbox (-7.05, 39.55, -6.65, 39.95), MGRS 29SQE)
        - Atlantic estuary → **Tagus estuary (PT)** (bbox (-9.45, 38.55, -8.85, 39.05), MGRS 29SMC)
        - Boreal lake → **Vänern (SE)** (bbox (12.40, 58.45, 14.20, 59.45), MGRS 33VVF)
        - Alpine valley → **Lago di Garda (IT)** (bbox (10.55, 45.55, 10.85, 45.85), MGRS 32TQR)
        - Iberian summer-dry → **Doñana wetlands (ES)** (bbox (-6.55, 36.80, -6.30, 37.05), MGRS 29SQB)
        - Pannonian plain → **Balaton (HU) HELD-OUT** (bbox (17.20, 46.60, 18.20, 46.95), MGRS 33TXP) — gate-of-truth per BOOTSTRAP §5.4

       Confirm each AOI's wet/dry-ratio + JRC-unknown-pct + cloud-free-2021 score from the notebook output. Reject any AOI with auto-reject violations (wet/dry < 1.2, JRC unknown > 20%, cloud-free < 3) and pick a substitute from the rejection list.
    2. **Read the rejection reasoning** for the 12 candidates that did NOT make the cut (Bracciano too-small, Severn ambiguous-tidal-flat, Saimaa fragmented, etc. per RESEARCH §EU fit-set candidate AOIs Recommended 5-AOI lock-in lines 240-249).
    3. **Read the PROTEUS ATBD ceiling resolution** in `.planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md` and confirm the recommendation for Plan 06-07's `docs/validation_methodology.md §5.1` wording. Three valid outcomes:
        - Path (a) succeeded → §5.1 cites a specific F1 number from the Product Spec PDF
        - Path (c) succeeded → §5.1 cites a specific F1 number from the OPERA-Cal-Val CSV
        - Path (d) locked → §5.1 uses "Empirical bound observed over our 6-AOI evaluation at F1 ≈ X.YZ" wording (X.YZ filled by Plan 06-06 at Phase 6 close)
    4. **Verify the N.Am. positive-control candidates** include T10SFH for Tahoe and T15RYP (or T15RYR if the live STAC query disagreed with python-mgrs) for Pontchartrain. Plan 06-05 will use this list with runtime auto-pick by cloud-cover.
    5. **Verify the Makefile targets** by running `make -n recalibrate-dswx` and `make -n dswx-fitset-aoi-md` — both should print expected commands without execution errors.

    If everything looks good, type `approved` to unblock Plan 06-02 (foundation scaffolding) and the rest of the wave.

    If you want to swap any of the 5 fit-set AOIs (e.g. Tagus is too risky due to tidal turbidity; substitute Loire), edit `.planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md` directly, change the `recommended: YES` flag, and re-run the notebook to refresh the rendered docs. Then approve.

    If the PROTEUS ATBD probe failed to produce a citable number AND you want path (a) or (c) instead of own-data fallback, describe what additional probe effort to invest (e.g. 30 min downloading the Product Spec PDF locally + parsing manually).
  </how-to-verify>
  <resume-signal>Type "approved" to unlock Plan 06-02 + Plan 06-06 fit-set compute, OR describe required revisions to the 5-AOI fit-set or the PROTEUS ATBD resolution path.</resume-signal>
</task>

</tasks>

<verification>
- AOI candidates artifact exists with 5 recommended fit-set AOIs + 1 held-out Balaton + N.Am. positive-control list
- PROTEUS ATBD probe artifact resolves to one of paths (a)/(c)/(d) with execution evidence
- Notebook executes end-to-end in subsideo conda env (`jupyter nbconvert --to notebook --execute --inplace`)
- Failure-modes YAML resource committed; importlib.resources can locate it
- Makefile targets `recalibrate-dswx` and `dswx-fitset-aoi-md` wired with expected command lines
- docs/dswx_fitset_aoi_selection.md auto-rendered from the notebook
- USER LOCK-IN CHECKPOINT received "approved" before Plan 06-02 / Plan 06-06 proceed
</verification>

<success_criteria>
- All 6 file artifacts on disk + git-staged
- Lock-in checkpoint received user approval (or revisions applied + re-approval)
- Plan 06-06 can read `.planning/milestones/v1.1-research/dswx_fitset_aoi_candidates.md` to seed the AOIs list at Stage 1 of `scripts/recalibrate_dswe_thresholds.py`
- Plan 06-05 can read the N.Am. candidates section to seed the CANDIDATES list at the top of `run_eval_dswx_nam.py`
- Plan 06-07 can read `dswx_proteus_atbd_ceiling_probe.md` Recommendation section to lock §5.1 wording in `docs/validation_methodology.md`
</success_criteria>

<output>
After completion, create `.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-01-SUMMARY.md` documenting:
- 5 fit-set AOIs locked (with brief rationale per biome)
- PROTEUS ATBD resolution path taken (a/c/d) + Plan 06-07 §5.1 wording
- N.Am. positive-control candidates locked (Tahoe + Pontchartrain MGRS tiles)
- User checkpoint outcome (approved as-is OR revisions applied)
- Pointer to artifacts for downstream plans (06-05, 06-06, 06-07)
</output>
</content>
