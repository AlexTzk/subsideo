---
phase: 06-dswx-s2-n-am-eu-recalibration
plan: 07
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06]
files_modified:
  - run_eval_dswx.py
  - eval-dswx/metrics.json
  - eval-dswx/meta.json
  - CONCLUSIONS_DSWX.md
  - docs/validation_methodology.md
autonomous: true
last_updated: 2026-04-26
requirements: [DSWX-06, DSWX-07]
tags: [eu-rerun, balaton-held-out, conclusions-append, methodology-section-5]

must_haves:
  truths:
    - "User runs `make eval-dswx-eu` and run_eval_dswx.py executes with EXPECTED_WALL_S unchanged (verify 900s sufficient post-Phase-6 changes; raise to 1800 if needed)"
    - "run_eval_dswx.py modified per CONTEXT D-26 5 changes (with B2 + W5 iteration-1 fixes integrated): (1) DSWxConfig(..., region='eu') per D-10; (2) compare_dswx single-return per Plan 06-04 B2 fix — NOT tuple unpack; (3) DswxEUCellMetrics write per Plan 06-02 schema; (4) reads scripts/recalibrate_dswe_thresholds_results.json for fit_set_mean_f1 / loocv_mean_f1 / loocv_gap / loocv_per_fold (10 entries per B1 fix) / per_aoi_breakdown; (5) verify EXPECTED_WALL_S sufficient; (W5) Stage 0 pre-check `assert THRESHOLDS_EU.fit_set_hash != ''` after _mp + credential preflight, before any pipeline work"
    - "Stage 0 W5 pre-check: assert THRESHOLDS_EU.fit_set_hash != '' before any pipeline work, with fail-loud message 'Plan 06-06 grid search did not land. Re-run scripts/recalibrate_dswe_thresholds.py before EU eval.'"
    - "eval-dswx/metrics.json validates as DswxEUCellMetrics; reference_agreement.measurements['f1'] = held-out Balaton F1 (the OFFICIAL EU matrix-cell F1 per D-13)"
    - "metrics.json includes thresholds_used: DSWEThresholdsRef stamping which (region='eu', fit_set_hash, grid_search_run_date) thresholds were applied"
    - "metrics.json includes per_aoi_breakdown[] copied verbatim from scripts/recalibrate_dswe_thresholds_results.json (5 fit-set AOIs)"
    - "metrics.json includes loocv_per_fold[] (10 entries per B1 fix) copied verbatim from results.json"
    - "metrics.json cell_status logic: F1 > 0.90 -> PASS + named_upgrade_path=None; 0.85 <= F1 < 0.90 -> FAIL + named_upgrade_path='ML-replacement (DSWX-V2-01)'; F1 < 0.85 -> FAIL + named_upgrade_path='fit-set quality review' (per CONTEXT D-15 + BOOTSTRAP §5.5)"
    - "CONCLUSIONS_DSWX.md (existing v1.0 narrative) gets a new heading '## v1.0 Balaton baseline (PROTEUS DSWE defaults; F1=0.7957 against JRC)' wrapping lines 1-289 as preamble; v1.1 sections appended below per CONTEXT D-21(b)"
    - "Three v1.1 sections appended to CONCLUSIONS_DSWX.md: 'Recalibrated thresholds (region=eu)' + 'Held-out Balaton F1 + LOO-CV gap' + 'Matrix-cell verdict + named upgrade path'"
    - "docs/validation_methodology.md §5 appended per CONTEXT D-22 with 5 sub-sections: §5.1 DSWE F1 ceiling citation chain (path a/c/d from Plan 06-01 ATBD probe); §5.2 Held-out Balaton vs fit-set methodology; §5.3 Shoreline 1-pixel buffer rationale (W4 fix: cites Pekel et al. 2016 *High-resolution mapping of global surface water and its long-term changes*, Nature 540:418–422 + cross-references PITFALLS §P5.2); §5.4 LOO-CV overfit detection (gap < 0.02; B1 10-fold leave-one-pair-out); §5.5 Threshold module + region selector design"
    - "§5 leads with structural argument (§5.1 ceiling) before empirical evidence (§5.2-§5.4) before design rationale (§5.5) per Phase 3 D-15 + Phase 6 D-22 'kernel argument leads' precedent"
    - "matrix.md regenerated (`make matrix`); dswx:eu cell shows F1 from Balaton + verdict + named_upgrade_path inline + LOO-CV gap inline"
  artifacts:
    - path: "run_eval_dswx.py"
      provides: "5 changes per D-26 + iteration-1 fixes (B2 + W5): region='eu', compare_dswx single-return + .diagnostics attribute access, DswxEUCellMetrics write reading recalibration results.json, EXPECTED_WALL_S verify, Stage 0 W5 fit_set_hash != '' pre-check"
      contains: "DSWxConfig(s2_band_paths"
    - path: "eval-dswx/metrics.json"
      provides: "DswxEUCellMetrics schema with held-out Balaton F1 + thresholds_used + per_aoi_breakdown + loocv_per_fold (10 entries per B1)"
      contains: "thresholds_used"
    - path: "CONCLUSIONS_DSWX.md"
      provides: "v1.0 baseline preamble + 3 v1.1 sections appended (Phase 6 D-21)"
      contains: "## v1.1 Recalibrated thresholds"
    - path: "docs/validation_methodology.md"
      provides: "§5 appended with 5 sub-sections (5.1-5.5) per CONTEXT D-22; §5.3 W4 fix cites Pekel 2016 + PITFALLS P5.2"
      contains: "## 5. DSWE F1 ceiling, held-out Balaton, and threshold-module design"
  key_links:
    - from: "run_eval_dswx.py"
      to: "src/subsideo/products/dswx_thresholds.py"
      via: "Stage 5 calls run_dswx(DSWxConfig(..., region='eu')) -> THRESHOLDS_EU recalibrated values applied (Plan 06-06 grid-search output); Stage 0 W5 fix asserts fit_set_hash != '' before any pipeline work"
      pattern: "region=.eu."
    - from: "run_eval_dswx.py"
      to: "scripts/recalibrate_dswe_thresholds_results.json"
      via: "Stage 9 reads recalibration results to populate fit_set_mean_f1 / loocv_mean_f1 / loocv_gap / loocv_per_fold (10 entries) / per_aoi_breakdown in DswxEUCellMetrics"
      pattern: "recalibrate_dswe_thresholds_results.json"
    - from: "docs/validation_methodology.md"
      to: ".planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md"
      via: "§5.1 cites the resolution path (a/c/d) locked by Plan 06-01 ATBD probe"
      pattern: "PROTEUS"
    - from: "CONCLUSIONS_DSWX.md"
      to: "scripts/recalibrate_dswe_thresholds_results.json"
      via: "v1.1 section 'Recalibrated thresholds (region=eu)' cites WIGT/AWGT/PSWT2_MNDWI verbatim from results.json"
      pattern: "joint_best_gridpoint"
---

<objective>
Wave 5 EU re-run + reporting: modify `run_eval_dswx.py` per CONTEXT D-26 (5 changes; B2 + W5 iteration-1 fixes integrated), execute the EU eval against Balaton with the recalibrated THRESHOLDS_EU, write `eval-dswx/metrics.json` validating as DswxEUCellMetrics, append v1.1 sections to `CONCLUSIONS_DSWX.md` (Phase 4 D-13 + Phase 5 D-22 v1.0-baseline-preamble pattern), and append §5 to `docs/validation_methodology.md` (CONTEXT D-22 + Phase 3 D-15 append-only). Phase 6 closes when matrix.md renders the dswx:eu cell with the official Balaton F1 + named_upgrade_path inline.

Iteration 1 fixes integrated:
- **B2**: Stage 9 binds single DSWxValidationResult; reads diagnostics via `.diagnostics` attribute (NOT tuple unpack).
- **W4**: §5.3 cites Pekel et al. 2016 (Nature) + cross-references PITFALLS §P5.2 for JRC commission/omission accuracy bounds.
- **W5**: Stage 0 pre-check `assert THRESHOLDS_EU.fit_set_hash != ''` AFTER _mp + credential preflight, BEFORE any pipeline work — fail-loud if Plan 06-06 grid search did not land.

Purpose: (1) DSWX-06 acceptance: EU re-run with recalibrated thresholds; F1 reported alongside LOO-CV F1 (gap < 0.02 already enforced by Plan 06-06; just stamps in metrics.json); 0.85 ≤ F1 < 0.90 = FAIL with named ML-replacement upgrade path; F1 < 0.85 = FAIL with fit-set quality review. (2) DSWX-07 acceptance: F1 ceiling claim ground-referenced (PROTEUS ATBD per Plan 06-01) or own-data fallback (path d). (3) Closes the 5/5 product validation matrix on the DSWx row.

Output: 1 modified eval script + 1 new metrics.json + 1 modified CONCLUSIONS doc + 1 modified methodology doc. matrix.md regenerated. Phase 6 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-CONTEXT.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-RESEARCH.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-PATTERNS.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-01-SUMMARY.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-02-SUMMARY.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-03-SUMMARY.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-04-SUMMARY.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-05-SUMMARY.md
@.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-06-SUMMARY.md
@.planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md
@scripts/recalibrate_dswe_thresholds_results.json
@run_eval_dswx.py
@CONCLUSIONS_DSWX.md
@CONCLUSIONS_DISP_EU.md
@CONCLUSIONS_DIST_EU.md
@docs/validation_methodology.md
</context>

<interfaces>
<!-- Key signatures and contracts the executor needs. Extracted from codebase. -->

From run_eval_dswx.py (existing 9-stage EU eval — Plan 06-07 modifies 5 places + adds W5 Stage 0 pre-check):
```python
EXPECTED_WALL_S = 900   # Plan 06-07 verifies still sufficient post-Phase-6 changes

if __name__ == "__main__":
    from subsideo._mp import configure_multiprocessing
    configure_multiprocessing()
    # ... existing imports + load_dotenv + credential_preflight ...
    # W5 fix: BEFORE any pipeline work (immediately after credential_preflight),
    # assert THRESHOLDS_EU.fit_set_hash != '' or fail loud.
    # AOI_NAME = "Lake Balaton, Hungary"
    # MGRS_TILE = "33TXP"
    # AOI_BBOX = bounds_for_mgrs_tile(MGRS_TILE, buffer_deg=0.1)
    # EPSG = 32633

    # Stage 5 (currently):
    cfg = DSWxConfig(
        s2_band_paths=band_paths, scl_path=scl_path,
        output_dir=dswx_out_dir, output_epsg=EPSG, output_posting_m=30.0,
        # Phase 6 D-10 ADD: region="eu",
    )
    result = run_dswx(cfg)

    # Stage 7 (currently): single-return compare_dswx
    validation = compare_dswx(...)
    # Phase 6 D-04 + B2 fix: KEEP single-return; access diagnostics via .diagnostics attribute

    # Stage 9 (currently): no metrics.json write — just print + exit
    # Phase 6 ADD: DswxEUCellMetrics write + eval-dswx/metrics.json + meta.json
```

From src/subsideo/validation/matrix_schema.py (Plan 06-02 + W2 + W3):
```python
class DswxEUCellMetrics(MetricsJson):
    region: Literal["eu"] = "eu"
    thresholds_used: DSWEThresholdsRef
    fit_set_mean_f1: float
    loocv_mean_f1: float
    loocv_gap: float
    loocv_per_fold: list[LOOCVPerFold]  # B1 fix: 10 entries
    per_aoi_breakdown: list[PerAOIF1Breakdown]
    f1_full_pixels: float
    shoreline_buffer_excluded_pixels: int
    cell_status: DswxEUCellStatus  # PASS | FAIL | BLOCKER
    named_upgrade_path: str | None = None
```

From src/subsideo/validation/compare_dswx.py (Plan 06-04 + B2 fix):
```python
def compare_dswx(...) -> DSWxValidationResult:
    """Returns DSWxValidationResult; .reference_agreement.measurements['f1'] is shoreline-excluded F1 (gate);
    .diagnostics carries f1_full_pixels + shoreline_buffer_excluded_pixels (B2 fix: NO breaking return-type change)."""
```

From scripts/recalibrate_dswe_thresholds_results.json (Plan 06-06 + B1 fix):
```json
{
  "cell_status": "PASS",
  "joint_best_gridpoint": {"WIGT": 0.115, "AWGT": 0.005, "PSWT2_MNDWI": -0.48},
  "fit_set_mean_f1": 0.92,
  "loocv_mean_f1": 0.91,
  "loocv_gap": 0.01,
  "loocv_per_fold": [/* 10 entries per B1 fix; each has fold_idx, left_out_aoi, left_out_season, refit_best_*, test_f1 */],
  "per_aoi_breakdown": [
    {"aoi_id": "alcantara", "biome": "Mediterranean reservoir",
     "wet_scene_f1": 0.93, "dry_scene_f1": 0.91, "aoi_mean_f1": 0.92},
    /* ... 5 AOIs total */
  ],
  "held_out_balaton_f1": 0.91,
  "fit_set_hash": "sha256_64_hex_chars",
  "grid_search_run_date": "2026-04-30"
}
```

From docs/validation_methodology.md existing structure:
```markdown
## 1. CSLC cross-version phase impossibility (Phase 3 D-15)
## 2. Product-quality vs reference-agreement distinction (Phase 3)
## 3. Multilook ADR (Phase 4 D-19/D-23)
## 4. DIST cross-resolution methodology (Phase 5 D-23)
## 5. <NEW — Plan 06-07 D-22 appends>
```

From CONCLUSIONS_DSWX.md existing (290 LOC v1.0 Balaton narrative):
- Header (lines 1-23) describes v1.0 PROTEUS-defaults run on Balaton 2021-07
- Body documents F1=0.7957 against JRC + the Pannonia over-fire diagnosis
- Phase 6 D-21(b): wrap entire body as "v1.0 Balaton baseline" preamble; APPEND v1.1 sections below
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_eval_dswx.py → CDSE STAC + S3 | Existing v1.0 Balaton flow; harness retry policies handle transient failures |
| run_eval_dswx.py → scripts/recalibrate_dswe_thresholds_results.json | Stage 9 read; if missing/corrupt, fall back to NaN diagnostics + warning log |
| run_eval_dswx.py → THRESHOLDS_EU | Resolved via DSWxConfig.region='eu' (Plan 06-03 region threading); Plan 06-06 overwrote THRESHOLDS_EU with real values; W5 fix asserts fit_set_hash != '' at Stage 0 (catches stale module before pipeline work) |
| docs/validation_methodology.md append | Plan 06-07 owns §5 only; Phase 7 owns §6+ per Phase 3 D-15 append-only lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering | metrics.json Balaton F1 vs threshold module | mitigate | Eval reads THRESHOLDS_EU at run_dswx call; thresholds_used.fit_set_hash stamped in metrics.json provides audit trail (matches Plan 06-06 fit_set_hash); W5 fix asserts non-empty fit_set_hash at Stage 0 |
| T-06-07-02 | Information Disclosure | CONCLUSIONS_DSWX.md v1.0 narrative preserved | accept | v1.0 narrative is committed git history; Plan 06-07 wraps as preamble heading; no removal |
| T-06-07-03 | Tampering | docs/validation_methodology.md append-only | mitigate | Plan 06-07 appends §5; ZERO edits to existing §1-§4; pre-commit grep `git diff docs/validation_methodology.md | grep -E '^-## [1-4]\.'` returns 0 |
| T-06-07-04 | Repudiation | F1 ceiling claim drift | mitigate | §5.1 cites the Plan 06-01 PROTEUS ATBD probe artifact verbatim with the resolution path (a/c/d); criteria.py:188 docstring isolated from §5 (gate threshold stays 0.90) |
| T-06-07-05 | DoS | recalibrate_dswe_thresholds_results.json missing | mitigate | run_eval_dswx.py Stage 9 catches FileNotFoundError + writes NaN diagnostics + non-blocking warning log; eval still produces metrics.json with cell_status from Balaton F1 alone |
| T-06-07-06 | Tampering | THRESHOLDS_EU stale-module gate | mitigate | W5 fix: Stage 0 pre-check asserts fit_set_hash != '' BEFORE any pipeline work; if Plan 06-06 grid search did not land (placeholder THRESHOLDS_EU still in module), the eval halts loudly with a clear remediation message rather than silently producing a result against PROTEUS placeholders |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Modify run_eval_dswx.py with 5 changes per CONTEXT D-26 + W5 Stage 0 pre-check + B2 attribute access; execute EU re-run</name>
  <files>run_eval_dswx.py, eval-dswx/metrics.json, eval-dswx/meta.json</files>
  <read_first>
    - run_eval_dswx.py (full file — 9-stage EU eval; Plan 06-07 makes 5 changes + adds W5 Stage 0 pre-check)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-CONTEXT.md (D-13 official EU matrix-cell F1 + D-26 5 changes)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-PATTERNS.md (lines 1059-1142 run_eval_dswx.py 5 changes)
    - src/subsideo/validation/matrix_schema.py (Plan 06-02 — DswxEUCellMetrics schema + W2/W3 BLOCKER state)
    - src/subsideo/validation/compare_dswx.py (Plan 06-04 + B2 fix — single return + .diagnostics attribute)
    - src/subsideo/products/dswx_thresholds.py (Plan 06-06 — THRESHOLDS_EU updated; fit_set_hash now non-empty)
    - scripts/recalibrate_dswe_thresholds_results.json (Plan 06-06 output — loocv_per_fold has 10 entries per B1)
  </read_first>
  <action>
**Step 1.** Read the current `run_eval_dswx.py` to find the exact lines to modify:

```bash
grep -n "DSWxConfig\|compare_dswx\|EXPECTED_WALL_S\|metrics\|configure_multiprocessing\|credential_preflight" run_eval_dswx.py
```

Identify the 5 modification points per CONTEXT D-26 + PATTERNS lines 1063-1141 PLUS the W5 + B2 iteration-1 fixes:
1. **W5 Stage 0** (NEW; immediately after `_mp` + `credential_preflight`): assert THRESHOLDS_EU.fit_set_hash != '' or fail loud.
2. **Stage 5** (DSWxConfig instantiation): add `region="eu"` argument.
3. **Stage 7** (compare_dswx call site): keep single-return `validation = compare_dswx(...)`; add `diagnostics = validation.diagnostics` (B2 fix — NOT tuple unpack).
4. **NEW Stage 9** (after existing print + exit): write DswxEUCellMetrics + meta.json reading recalibration results.json.
5. **EXPECTED_WALL_S** verify (likely 900s sufficient; raise to 1800 if needed post-Phase-6 changes).
6. **Add Stage 9 imports** for DswxEUCellMetrics + DSWEThresholdsRef + LOOCVPerFold + PerAOIF1Breakdown + THRESHOLDS_EU.

**Step 2 (W5 fix — Stage 0 pre-check).** Insert immediately after the existing `credential_preflight([...])` call (typically near the top of `if __name__ == "__main__":` body), BEFORE any pipeline work begins:

```python
# Phase 6 W5 fix (Plan 06-07 Stage 0 pre-check):
# Fail loud if Plan 06-06 grid search did not land. THRESHOLDS_EU placeholder
# state has fit_set_hash='' (per Plan 06-02 placeholder); after Plan 06-06
# Stage 10 W1 sentinel-anchor rewrite, fit_set_hash is a 64-char sha256 hex.
# Catching this BEFORE pipeline work prevents silently producing a Balaton F1
# against the PROTEUS placeholder (which would be indistinguishable from the
# v1.0 baseline F1=0.7957 — exactly the regression we are guarding against).
from subsideo.products.dswx_thresholds import THRESHOLDS_EU as _THRESHOLDS_EU_STAGE0
assert _THRESHOLDS_EU_STAGE0.fit_set_hash != "", (
    "THRESHOLDS_EU.fit_set_hash is empty -- Plan 06-06 grid search did not land. "
    "Re-run scripts/recalibrate_dswe_thresholds.py before EU eval."
)
del _THRESHOLDS_EU_STAGE0  # release the local reference so later imports stay clean
```

The temporary `_THRESHOLDS_EU_STAGE0` alias avoids polluting the rest of the module's namespace; the late import via `from subsideo.products.dswx_thresholds import THRESHOLDS_EU` for Stage 9 metrics.json write reuses the same module-level singleton (still a fresh read because `THRESHOLDS_EU` is a singleton instance).

**Step 3.** Apply Change 1 — DSWxConfig region threading. Find the existing `cfg = DSWxConfig(...)` call (likely near lines 235-241). Replace with:

```python
cfg = DSWxConfig(
    s2_band_paths=band_paths,
    scl_path=scl_path,
    output_dir=dswx_out_dir,
    output_epsg=EPSG,
    output_posting_m=30.0,
    region="eu",  # Phase 6 D-10 — applies THRESHOLDS_EU recalibrated values
)
```

**Step 4 (B2 fix).** Apply Change 2 — compare_dswx single-return + .diagnostics attribute access. Find the existing `validation = compare_dswx(...)` call (likely near lines 284-289). Keep it as a SINGLE return (the existing v1.0 binding). Add `diagnostics = validation.diagnostics` immediately after:

```python
# Phase 6 D-04 + B2 fix: single-return compare_dswx; access diagnostics via attribute
validation = compare_dswx(...)
diagnostics = validation.diagnostics  # B2 fix: NOT tuple unpack; .diagnostics is `DSWxValidationDiagnostics | None`
```

The `diagnostics` value carries `f1_full_pixels` + `shoreline_buffer_excluded_pixels` per Plan 06-04 B2 fix attribute side-channel.

**Step 5.** Apply Change 3 — NEW Stage 9 metrics.json + meta.json write. Add after the existing summary print (typically near the end of the file). Per PATTERNS lines 1085-1136 + B2/B1 integration:

```python
# ============================================================================
# Stage 9: DswxEUCellMetrics write (Phase 6 D-26 + D-13 official Balaton F1)
# ============================================================================
import hashlib
import platform
import subprocess
import sys
import time
from datetime import datetime, timezone

from subsideo.products.dswx_thresholds import THRESHOLDS_EU
from subsideo.validation.matrix_schema import (
    DSWEThresholdsRef,
    DswxEUCellMetrics,
    LOOCVPerFold,
    PerAOIF1Breakdown,
)
from subsideo.validation.results import ProductQualityResult, ReferenceAgreementResult

balaton_f1 = validation.reference_agreement.measurements["f1"]
balaton_precision = validation.reference_agreement.measurements["precision"]
balaton_recall = validation.reference_agreement.measurements["recall"]
balaton_accuracy = validation.reference_agreement.measurements["accuracy"]

# Read recalibration aggregate per Plan 06-06 Stage 5 + Stage 7 (B1 fix: 10 LOOCV folds):
recalib_results_path = Path("scripts/recalibrate_dswe_thresholds_results.json")
if recalib_results_path.exists():
    recalib = _json.loads(recalib_results_path.read_text())
    fit_set_mean_f1 = float(recalib["fit_set_mean_f1"])
    loocv_mean_f1 = float(recalib["loocv_mean_f1"])
    loocv_gap = float(recalib["loocv_gap"])
    # B1 fix: loocv_per_fold has 10 entries each with left_out_aoi + left_out_season:
    loocv_per_fold = [LOOCVPerFold(**f) for f in recalib.get("loocv_per_fold", [])]
    per_aoi_breakdown = [
        PerAOIF1Breakdown(**a) for a in recalib.get("per_aoi_breakdown", [])
    ]
else:
    print("WARNING: scripts/recalibrate_dswe_thresholds_results.json missing; "
          "stamping NaN diagnostics in DswxEUCellMetrics. Plan 06-06 must "
          "complete before Plan 06-07 EU re-run.")
    fit_set_mean_f1 = float("nan")
    loocv_mean_f1 = float("nan")
    loocv_gap = float("nan")
    loocv_per_fold = []
    per_aoi_breakdown = []

# B2 fix: diagnostics from validation.diagnostics attribute (NOT tuple unpack):
if diagnostics is not None:
    f1_full_pixels = float(diagnostics.f1_full_pixels)
    shoreline_buffer_excluded_pixels = int(diagnostics.shoreline_buffer_excluded_pixels)
else:
    # Should not happen post-Plan-06-04 since compare_dswx always populates,
    # but defensive default for backward compat:
    f1_full_pixels = float("nan")
    shoreline_buffer_excluded_pixels = 0

# cell_status logic per CONTEXT D-15 + BOOTSTRAP §5.5:
if balaton_f1 > 0.90:
    cell_status = "PASS"
    named_upgrade_path = None
elif balaton_f1 >= 0.85:
    cell_status = "FAIL"
    named_upgrade_path = "ML-replacement (DSWX-V2-01)"
else:
    cell_status = "FAIL"
    named_upgrade_path = "fit-set quality review"

metrics = DswxEUCellMetrics(
    product_quality=ProductQualityResult(measurements={}, criterion_ids=[]),
    reference_agreement=ReferenceAgreementResult(
        measurements={
            "f1": float(balaton_f1),  # GATE per CONTEXT D-13 (held-out Balaton)
            "precision": float(balaton_precision),
            "recall": float(balaton_recall),
            "accuracy": float(balaton_accuracy),
        },
        criterion_ids=["dswx.f1_min"],
    ),
    criterion_ids_applied=["dswx.f1_min"],
    region="eu",
    thresholds_used=DSWEThresholdsRef(
        region="eu",
        grid_search_run_date=THRESHOLDS_EU.grid_search_run_date,
        fit_set_hash=THRESHOLDS_EU.fit_set_hash,
    ),
    fit_set_mean_f1=fit_set_mean_f1,
    loocv_mean_f1=loocv_mean_f1,
    loocv_gap=loocv_gap,
    loocv_per_fold=loocv_per_fold,
    per_aoi_breakdown=per_aoi_breakdown,
    f1_full_pixels=f1_full_pixels,
    shoreline_buffer_excluded_pixels=shoreline_buffer_excluded_pixels,
    cell_status=cell_status,  # type: ignore[arg-type]
    named_upgrade_path=named_upgrade_path,
)
out_dir = Path("./eval-dswx")
out_dir.mkdir(exist_ok=True)
(out_dir / "metrics.json").write_text(metrics.model_dump_json(indent=2))

# meta.json with git SHA + input hashes (Phase 2 D-12)
git_sha = subprocess.check_output(["git", "rev-parse", "HEAD"]).decode().strip()
threshold_module_hash = hashlib.sha256(
    Path("src/subsideo/products/dswx_thresholds.py").read_bytes()
).hexdigest()[:16]
meta = {
    "git_sha": git_sha,
    "run_started_utc": datetime.now(timezone.utc).isoformat(),
    "wall_s": time.time() - t_start if "t_start" in dir() else None,
    "platform": platform.platform(),
    "python_version": sys.version,
    "selected_aoi": AOI_NAME,
    "mgrs_tile": MGRS_TILE,
    "selected_scene_id": safe_id if "safe_id" in dir() else "(unknown)",
    "input_hashes": {
        "threshold_module_sha256_prefix": threshold_module_hash,
        "fit_set_hash_from_recalibration": THRESHOLDS_EU.fit_set_hash,
    },
}
(out_dir / "meta.json").write_text(_json.dumps(meta, indent=2))

print(f"\n=== EU re-run complete ===")
print(f"Held-out Balaton F1 = {balaton_f1:.3f}")
print(f"cell_status = {cell_status}")
if named_upgrade_path:
    print(f"named_upgrade_path = {named_upgrade_path!r}")
print(f"loocv_gap = {loocv_gap:.4f} ({len(loocv_per_fold)} folds; B1 fix expects 10)")
```

Place this AFTER the existing summary print + `sys.exit(0)` is removed (the script should run all the way through to metrics.json write).

**Step 6.** Apply Change 4 — verify EXPECTED_WALL_S = 900 still sufficient. Run the eval:

```bash
make eval-dswx-eu 2>&1 | tee eval-dswx/run.log
```

Time the wall:
- Cold path: SAFE download (~3 min) + DSWx pipeline (~1 min) + JRC fetch (~30s) + compare_dswx with shoreline buffer (~30s) + Stage 9 metrics.json (~1s) ≈ 5-6 min total. Well under 900s budget.
- Warm path (intermediate cache + JRC tile cache hits): ~30-60s.

If the cold path exceeds 900s due to thermal throttling or CDSE rate-limit, raise EXPECTED_WALL_S to 1800. Otherwise leave at 900.

**Step 7.** Apply Change 5 — module-top imports. The new Stage 9 needs DswxEUCellMetrics, DSWEThresholdsRef, LOOCVPerFold, PerAOIF1Breakdown, ProductQualityResult, ReferenceAgreementResult, hashlib, platform, subprocess, time imported. Verify they're at module top in run_eval_dswx.py (some may need lazy-import inside Stage 9 to avoid breaking existing flow). The W5 Stage 0 pre-check imports THRESHOLDS_EU early (under the `_THRESHOLDS_EU_STAGE0` alias).

**Step 8.** Verify outputs:

```bash
test -f eval-dswx/metrics.json && cat eval-dswx/metrics.json | python3 -m json.tool | head -40

micromamba run -n subsideo python -c "
import json
from subsideo.validation.matrix_schema import DswxEUCellMetrics
m = DswxEUCellMetrics.model_validate_json(open('eval-dswx/metrics.json').read())
print(f'cell_status = {m.cell_status}')
print(f'F1 (Balaton) = {m.reference_agreement.measurements[\"f1\"]:.3f}')
print(f'fit_set_mean_f1 = {m.fit_set_mean_f1:.3f}')
print(f'loocv_gap = {m.loocv_gap:.4f}')
print(f'loocv_per_fold count = {len(m.loocv_per_fold)}  # B1 fix: 10')
print(f'named_upgrade_path = {m.named_upgrade_path}')
print(f'thresholds_used.region = {m.thresholds_used.region}')
print(f'thresholds_used.fit_set_hash = {m.thresholds_used.fit_set_hash[:16]}...')
print(f'shoreline_buffer_excluded_pixels = {m.shoreline_buffer_excluded_pixels}')"
```

**Step 9.** Verify matrix_writer renders the cell:

```bash
micromamba run -n subsideo python -c "
from pathlib import Path
from subsideo.validation.matrix_writer import _is_dswx_eu_shape, _render_dswx_eu_cell
p = Path('eval-dswx/metrics.json')
assert _is_dswx_eu_shape(p)
pq, ra = _render_dswx_eu_cell(p)
print(f'rendered: pq={pq!r}, ra={ra!r}')"

make matrix
grep -A 1 "dswx" results/matrix.md
```

**Step 10.** Run ruff on the modified script:

```bash
micromamba run -n subsideo ruff check run_eval_dswx.py
```
  </action>
  <verify>
    <automated>test -f eval-dswx/metrics.json && test -f eval-dswx/meta.json && micromamba run -n subsideo python -c "
import json
from subsideo.validation.matrix_schema import DswxEUCellMetrics
m = DswxEUCellMetrics.model_validate_json(open('eval-dswx/metrics.json').read())
assert m.region == 'eu'
assert m.cell_status in ('PASS', 'FAIL', 'BLOCKER')
assert isinstance(m.fit_set_mean_f1, float)
assert isinstance(m.loocv_gap, float)
print(f'cell_status={m.cell_status} balaton_f1={m.reference_agreement.measurements[\"f1\"]:.3f} loocv_gap={m.loocv_gap:.4f}')" && grep -q 'region="eu"' run_eval_dswx.py && grep -q "validation = compare_dswx" run_eval_dswx.py && grep -q "validation.diagnostics" run_eval_dswx.py && grep -q "DswxEUCellMetrics" run_eval_dswx.py && grep -q "recalibrate_dswe_thresholds_results.json" run_eval_dswx.py && grep -q "THRESHOLDS_EU.fit_set_hash" run_eval_dswx.py && grep -q "Plan 06-06 grid search did not land" run_eval_dswx.py && ! grep -q "validation, diagnostics = compare_dswx" run_eval_dswx.py && micromamba run -n subsideo ruff check run_eval_dswx.py</automated>
  </verify>
  <acceptance_criteria>
    - File `run_eval_dswx.py` contains literal `region="eu"` (or single-quoted) in the DSWxConfig instantiation
    - File `run_eval_dswx.py` contains literal `validation = compare_dswx(` (single return; B2 fix) AND `validation.diagnostics` (attribute access)
    - File `run_eval_dswx.py` does NOT contain `validation, diagnostics = compare_dswx(` (tuple unpack forbidden by B2 fix)
    - File `run_eval_dswx.py` contains literal `DswxEUCellMetrics(` constructor call
    - File `run_eval_dswx.py` contains literal `recalibrate_dswe_thresholds_results.json` (Plan 06-06 read)
    - File `run_eval_dswx.py` contains literal `named_upgrade_path` decision logic with three branches (PASS / 0.85≤F1<0.90 / F1<0.85)
    - W5 fix: `run_eval_dswx.py` contains literal `THRESHOLDS_EU.fit_set_hash != ""` (or `!= ''`) assertion AND the message `Plan 06-06 grid search did not land` (or close variant: `Re-run scripts/recalibrate_dswe_thresholds.py before EU eval`)
    - File `eval-dswx/metrics.json` exists, validates as DswxEUCellMetrics with region='eu' + valid cell_status
    - File `eval-dswx/meta.json` exists with `git_sha` + `mgrs_tile` + `input_hashes`
    - `matrix_writer._is_dswx_eu_shape` returns True on the metrics.json
    - `matrix_writer._render_dswx_eu_cell` returns a non-None tuple
    - `ruff check` exits 0 on run_eval_dswx.py
  </acceptance_criteria>
  <done>run_eval_dswx.py modified per D-26 + iteration-1 fixes (W5 Stage 0 pre-check + B2 attribute access); EU re-run executed; metrics.json + meta.json on disk; matrix.md renders the cell.</done>
</task>

<task type="auto">
  <name>Task 2: Append v1.1 sections to CONCLUSIONS_DSWX.md (D-21 v1.0-baseline-preamble pattern)</name>
  <files>CONCLUSIONS_DSWX.md</files>
  <read_first>
    - CONCLUSIONS_DSWX.md (full file — v1.0 Balaton 2021-07 narrative; 290 LOC)
    - CONCLUSIONS_DISP_EU.md (Phase 4 D-13 v1.0-baseline-preamble + v1.1-section-append precedent)
    - CONCLUSIONS_DIST_EU.md (Phase 5 D-22 v1.0-baseline-preamble + v1.1-section-append precedent)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-CONTEXT.md (D-21(b) v1.1 section append structure)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-PATTERNS.md (lines 1278-1320 CONCLUSIONS_DSWX.md append pattern)
    - eval-dswx/metrics.json (Task 1 output — actual Balaton F1 + verdict + B1 10-fold LOOCV data)
    - scripts/recalibrate_dswe_thresholds_results.json (Plan 06-06 output — joint_best + fit_set_mean + LOO-CV)
  </read_first>
  <action>
**Step 1.** Read the current `CONCLUSIONS_DSWX.md` to identify the existing v1.0 header structure (typically lines 1-23 are the **Date / AOI / Scene / Reference / Result** block). Per CONTEXT D-21(b), wrap the entire current content as a "v1.0 Balaton baseline" preamble heading.

**Step 2.** Insert a new top-of-file heading + wrap the v1.0 content. The current top of CONCLUSIONS_DSWX.md likely starts:

```markdown
# EU DSWx-S2 Validation — Session Conclusions

**Date:** 2026-04-15
...
```

Modify to:

```markdown
# EU DSWx-S2 Validation — Session Conclusions

**Last updated:** 2026-04-XX (Phase 6 v1.1 recalibration appended; v1.0 preamble preserved per CONTEXT D-21).

***

## v1.0 Balaton baseline (PROTEUS DSWE defaults; F1=0.7957 against JRC)

[All existing content from CONCLUSIONS_DSWX.md — wrap lines 1-289 verbatim under this heading]

[Existing **Date / AOI / Scene / Reference / Result** block becomes the body of this section]

[Existing v1.0 narrative about PROTEUS over-fire on dry Pannonia stays here]

[End of v1.0 baseline section]

***

## v1.1 Recalibrated thresholds (region=eu)

After the Phase 6 joint grid search over WIGT × AWGT × PSWT2_MNDWI (`scripts/recalibrate_dswe_thresholds.py`; 8400 gridpoints × 10 fit-set (AOI, scene) pairs per B1 fix — 5 AOIs × 2 wet/dry seasons; Balaton held out), the recalibrated EU thresholds are:

| Threshold | PROTEUS default (v1.0) | Recalibrated (v1.1 EU) | Delta |
|-----------|------------------------|------------------------|-------|
| WIGT | 0.124 | {WIGT_NEW:.4f} | {WIGT_DELTA:+.4f} |
| AWGT | 0.0 | {AWGT_NEW:.4f} | {AWGT_DELTA:+.4f} |
| PSWT2_MNDWI | -0.5 | {PSWT2_MNDWI_NEW:.4f} | {PSWT2_MNDWI_DELTA:+.4f} |

Provenance:
- **fit_set_hash:** `{FIT_SET_HASH_PREFIX}...` (sha256 of sorted (AOI, scene) IDs concatenated)
- **grid_search_run_date:** `{GRID_SEARCH_DATE}`
- **fit_set_mean_f1:** {FIT_SET_MEAN_F1:.4f} across 5 EU biomes × 2 wet/dry (10 pairs per B1 fix)
- **per-AOI breakdown:** {ALCANTARA_F1:.3f} / {TAGUS_F1:.3f} / {VANERN_F1:.3f} / {GARDA_F1:.3f} / {DONANA_F1:.3f} (Mediterranean reservoir / Atlantic estuary / Boreal lake / Alpine valley / Iberian summer-dry)
- **reproducibility:** `notebooks/dswx_recalibration.ipynb` + `scripts/recalibrate_dswe_thresholds_results.json`

The non-tunable DSWE thresholds (PSWT1_*, PSWT2_BLUE/NIR/SWIR1/SWIR2) stay at PROTEUS defaults per CONTEXT D-12.

***

## v1.1 Held-out Balaton F1 + LOO-CV gap

The OFFICIAL EU matrix-cell value is the held-out Balaton F1 per BOOTSTRAP §5.4 + CONTEXT D-13. Fit-set mean F1 + LOO-CV diagnostics are reported alongside for transparency.

| Metric | Value | Acceptance |
|--------|-------|------------|
| **Held-out Balaton F1 (gate; shoreline-excluded)** | **{BALATON_F1:.3f}** | > 0.90 (BINDING) |
| Balaton precision | {BALATON_PREC:.3f} | (diagnostic) |
| Balaton recall | {BALATON_REC:.3f} | (diagnostic) |
| Balaton accuracy | {BALATON_ACC:.3f} | (diagnostic) |
| Fit-set mean F1 (5 AOIs × 2 scenes = 10 pairs) | {FIT_SET_MEAN_F1:.3f} | (diagnostic) |
| LOO-CV mean F1 (10 folds; leave-one-pair-out) | {LOOCV_MEAN_F1:.3f} | (diagnostic) |
| **LOO-CV gap (fit-set − LOO-CV)** | **{LOOCV_GAP:.4f}** | < 0.02 (DSWX-06 acceptance) |
| Balaton F1 (full pixels; no shoreline exclusion) | {F1_FULL_PIXELS:.3f} | (diagnostic) |
| Shoreline buffer excluded pixels | {SHORELINE_PX} | — |

Per PITFALLS P5.1, the LOO-CV gap of {LOOCV_GAP:.4f} {GAP_INTERPRETATION} the 0.02 acceptance threshold; calibration is {GENERALISES_OR_OVERFITS}.

The B1-fix LOO-CV is leave-one-pair-out (10 folds) rather than leave-one-AOI-out (5 folds): each fold leaves out a single (aoi, season) pair, which retains both seasons of the held-out AOI's sibling pair as additional signal. CONTEXT D-14 "rotate 12 times" wording was a writing error that conflated 12 fit-set+held-out total pairs with the 10 fit-set folds; the implementation reflects 10 folds.

***

## v1.1 Matrix-cell verdict + named upgrade path

**`dswx:eu` matrix cell:** F1 = {BALATON_F1:.3f} {VERDICT}.

{IF VERDICT=PASS:} The recalibrated EU thresholds achieve F1 > 0.90 against JRC over the held-out Balaton scene. PROTEUS DSWE recalibration over the 5-biome EU fit-set succeeded.

{IF VERDICT=FAIL with named_upgrade_path='ML-replacement (DSWX-V2-01)':} Honest FAIL framing per BOOTSTRAP §5.5: F1 = {BALATON_F1:.3f} ∈ [0.85, 0.90). The 0.90 bar does NOT move (PITFALLS M4); F1 close-to-but-below the bar signals the architectural ceiling of threshold-based DSWE algorithms over heterogeneous EU biomes. Named upgrade path: **ML-replacement (DSWX-V2-01)** — see Future Work in REQUIREMENTS.md.

{IF VERDICT=FAIL with named_upgrade_path='fit-set quality review':} F1 = {BALATON_F1:.3f} < 0.85. Signals the 5-AOI fit-set was insufficient — the EU biomes did not generalise to Balaton. Named upgrade path: **fit-set quality review** (re-evaluate AOI selection in v1.2).

The DSWE F1 ceiling claim is documented in `docs/validation_methodology.md §5.1` per Phase 6 D-22 — see the methodology document for the citation chain (PROTEUS ATBD or own-data-bound fallback).

***

[End of v1.1 sections; Plan 06-07 close]
```

Replace `{...}` placeholders with actual values from `eval-dswx/metrics.json` + `scripts/recalibrate_dswe_thresholds_results.json`.

**Step 3.** Verify the file structure post-modification:

```bash
grep -E "^## (v1\.[01])" CONCLUSIONS_DSWX.md
```

Expected output:
```
## v1.0 Balaton baseline (PROTEUS DSWE defaults; F1=0.7957 against JRC)
## v1.1 Recalibrated thresholds (region=eu)
## v1.1 Held-out Balaton F1 + LOO-CV gap
## v1.1 Matrix-cell verdict + named upgrade path
```

**Step 4.** Verify the v1.0 narrative is fully preserved (no content removed):

```bash
grep -c "PROTEUS DSWE" CONCLUSIONS_DSWX.md  # at least 1 (v1.0 preamble) + 1 (v1.1 mention) = 2+
grep -c "F1=0.7957" CONCLUSIONS_DSWX.md  # at least 1 (v1.0 baseline)
```
  </action>
  <verify>
    <automated>test -f CONCLUSIONS_DSWX.md && grep -q "## v1.0 Balaton baseline" CONCLUSIONS_DSWX.md && grep -q "## v1.1 Recalibrated thresholds" CONCLUSIONS_DSWX.md && grep -q "## v1.1 Held-out Balaton F1" CONCLUSIONS_DSWX.md && grep -q "## v1.1 Matrix-cell verdict" CONCLUSIONS_DSWX.md && grep -q "F1=0.7957" CONCLUSIONS_DSWX.md && grep -qE "F1 *= *0\.[0-9]+" CONCLUSIONS_DSWX.md && grep -q "LOO-CV gap" CONCLUSIONS_DSWX.md && grep -q "named_upgrade_path\|named upgrade path" CONCLUSIONS_DSWX.md</automated>
  </verify>
  <acceptance_criteria>
    - File `CONCLUSIONS_DSWX.md` contains literal heading `## v1.0 Balaton baseline (PROTEUS DSWE defaults; F1=0.7957 against JRC)` wrapping the existing v1.0 narrative
    - Contains 3 new v1.1 section headings: `## v1.1 Recalibrated thresholds (region=eu)`, `## v1.1 Held-out Balaton F1 + LOO-CV gap`, `## v1.1 Matrix-cell verdict + named upgrade path`
    - v1.0 narrative is preserved (`F1=0.7957` still present)
    - v1.1 sections cite the actual Balaton F1 from eval-dswx/metrics.json + LOO-CV gap (10 folds per B1 fix) from recalibration results.json
    - Named upgrade path text matches the verdict (PASS / ML-replacement / fit-set quality review)
  </acceptance_criteria>
  <done>CONCLUSIONS_DSWX.md has v1.0 baseline preamble + 3 v1.1 sections with actual Phase 6 results.</done>
</task>

<task type="auto">
  <name>Task 3: Append §5 to docs/validation_methodology.md with 5 sub-sections (D-22) + W4 Pekel/PITFALLS citation in §5.3</name>
  <files>docs/validation_methodology.md</files>
  <read_first>
    - docs/validation_methodology.md (full file — verify §1-§4 already present; identify the append point at end of file)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-CONTEXT.md (D-22 §5 5 sub-sections)
    - .planning/phases/06-dswx-s2-n-am-eu-recalibration/06-PATTERNS.md (lines 1322-1384 docs/validation_methodology.md §5 append)
    - .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md (Plan 06-01 — F1 ceiling resolution path a/c/d)
    - .planning/research/PITFALLS.md (§P5.2 — JRC commission/omission accuracy bounds; W4 fix references this section)
    - eval-dswx/metrics.json (Task 1 — actual Balaton F1 + 10-fold LOOCV)
    - scripts/recalibrate_dswe_thresholds_results.json (Plan 06-06 output)
  </read_first>
  <action>
**Step 1.** Read the Plan 06-01 PROTEUS ATBD probe artifact to determine which path (a/c/d) was locked:

```bash
cat .planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md | head -50
```

The artifact's `Recommendation for Plan 06-07` section dictates the §5.1 wording.

**Step 2.** Append §5 to `docs/validation_methodology.md` at the end of the file. Per CONTEXT D-22 + PATTERNS lines 1349-1384 + W4 fix in §5.3, exact structure (5 sub-sections; structural argument leads):

```markdown
***

## 5. DSWE F1 ceiling, held-out Balaton, and threshold-module design

<a name="dswe-recalibration-methodology"></a>

**TL;DR:** The DSWE-family F1 ceiling claim cited in BOOTSTRAP_V1.1.md was not verifiable from publicly indexed PROTEUS / OPERA documents during Plan 06-01 PROTEUS ATBD probe; the §5.1 citation chain falls back to the own-data empirical bound per CONTEXT D-17 path (d) {OR cites PROTEUS ATBD page X if path (a) succeeded}. Held-out Balaton F1 IS the OFFICIAL EU matrix-cell value per BOOTSTRAP §5.4 — fit-set mean F1 cannot collapse the matrix verdict. The shoreline 1-pixel buffer is applied uniformly in BOTH grid search AND final reporting (single source of truth). LOO-CV gap < 0.02 acceptance gates the recalibration against overfit (10-fold leave-one-pair-out per B1 fix). The threshold module uses a frozen+slots dataclass with inline provenance; YAML/runtime-config explicitly forbidden per DSWX-05.

### 5.1 DSWE F1 ceiling citation chain

[Per Plan 06-01 ATBD probe (`./planning/milestones/v1.1-research/dswx_proteus_atbd_ceiling_probe.md`):]

{IF PATH (a) SUCCEEDED:}
The OPERA DSWx-HLS Product Specification v1.0.0 D-107395 RevB section X.Y reports F1 = 0.{XX} for biome {Z} as the validated DSWE algorithm performance ceiling. The Phase 6 v1.1 recalibration over 5 EU biomes lands at fit-set mean F1 = 0.{YY} ({COMPARED_TO_PROTEUS}); held-out Balaton F1 = 0.{ZZ}. {INTERPRETATION}.

{IF PATH (c) SUCCEEDED:}
The OPERA-Cal-Val/DSWx-HLS-Requirement-Verification repository's `out/validation_table_data.csv` reports F1 = 0.{XX} for requirement R-{N} ({biome scope}). [...]

{IF PATH (d) FALLBACK — DEFAULT PER PLAN 06-01:}
PROTEUS ATBD specific F1 numbers were not surfaceable from publicly indexed sources during Plan 06-01 PROTEUS ATBD probe (3 attempts: WebFetch JPL OPERA suite page; WebFetch Product Spec PDF blocked at maxContentLength; WebFetch OPERA-Cal-Val repo README). The "DSWE F1 ≈ 0.92 architectural ceiling" claim in BOOTSTRAP_V1.1.md §5 is therefore inherited without an auditable citation chain (PITFALLS P5.3 explicit warning sign).

For the v1.1 milestone closure, the F1 ceiling claim is replaced with an **empirical bound observed over the Phase 6 6-AOI evaluation:**

> Empirical bound observed over our 6-AOI evaluation at F1 ≈ {FIT_SET_MEAN_F1:.2f} (Phase 6 grid search 2026-{MM}-{DD}; see `scripts/recalibrate_dswe_thresholds_results.json` fit-set mean F1, with held-out Balaton F1 = {BALATON_F1:.2f}).

The criterion threshold in `criteria.py:dswx.f1_min` (0.90) is **isolated** from the ceiling claim per PITFALLS P5.3 explicit isolation strategy: the gate stays at 0.90 regardless of where the architectural ceiling sits. The honest FAIL framing for 0.85 ≤ F1 < 0.90 cites named upgrade path "ML-replacement (DSWX-V2-01)" without dependency on a literature ceiling number.

If a future PROTEUS ATBD release surfaces a citable specific F1 number, the §5.1 wording revises in a future v1.x milestone — that is a methodology-doc edit only, not a criteria.py edit.

### 5.2 Held-out Balaton vs fit-set methodology

Per BOOTSTRAP §5.4 + CONTEXT D-13: the OFFICIAL EU matrix-cell value is the **held-out Balaton F1**, NOT the fit-set mean F1. PITFALLS P5.1 explicit:

> Balaton F1 < 0.90, report FAIL — not "fit-set F1 passed."

Plan 06-06 holds Balaton truly out of the 5-AOI fit-set construction; the 12 (AOI, scene) pairs in the grid search are 5 fit-set AOIs × 2 wet/dry + Balaton wet/dry, but only the 10 fit-set pairs participate in gridpoint F1 aggregation (B1 fix corrects an earlier 12-fold conflation). After grid search converges to a joint best gridpoint via fit-set-only F1, Balaton is scored once at the joint best gridpoint — that single number is the gate.

Fit-set mean F1 + per-AOI F1 breakdown are reported alongside in `eval-dswx/metrics.json` as `reference_agreement.diagnostics` — diagnostics, NOT the gate.

### 5.3 Shoreline 1-pixel buffer rationale

Per **PITFALLS P5.2** (W4 fix cross-reference): JRC Monthly History has documented commission accuracy 98–99% and omission accuracy 74–99% (Pekel et al. 2016, *High-resolution mapping of global surface water and its long-term changes*, Nature 540:418–422; also cross-referenced in subsideo `.planning/research/PITFALLS.md` §P5.2). Specifically:
- **Land-in-shadow classified as water** (mountain shadow, deep urban canyon shadow). Alpine valley AOIs (Garda) most affected.
- **Light clouds over water classified as land.** Atlantic estuary AOIs (Tagus) in variable-weather months affected.
- **Shoreline pixels ambiguous.** JRC uses 30 m Landsat pixels; a pixel 50% water / 50% shore gets classified one way but the ground truth is mixed.

The shoreline 1-pixel buffer (`scipy.ndimage.binary_dilation` XOR-of-water-and-non-water-dilations on JRC native 4326 grid; reprojected to S2 UTM via Resampling.nearest) excludes these structurally-ambiguous boundary pixels from the F1 evaluation. CONTEXT D-16 mandate: applied UNIFORMLY in BOTH the grid search (Plan 06-06 Stage 4) AND final reporting (Plan 06-07 Task 1 + Plan 06-05 N.Am. positive control). Single source of truth; auditable via `f1_full_pixels` diagnostic stamped alongside the gate `f1` value.

The buffer reduces false negatives at the shoreline (DSWx classifies the pixel as water; JRC labels it as land due to 50% shore) and false positives (DSWx classifies as non-water; JRC labels as water). The decision is methodologically conservative — F1 is computed on pixels where both classifiers can confidently agree.

### 5.4 LOO-CV overfit detection (gap < 0.02; B1 10-fold leave-one-pair-out)

Per PITFALLS P5.1: a 3-parameter joint grid search over WIGT × AWGT × PSWT2_MNDWI on 10 (AOI, season) fit-set pairs has 8400 combinations; with effective spatial autocorrelation reducing 14M-pixel sample to ~140K independent samples, the multimodal F1 landscape has multiple local optima. Standard overfitting symptom: fit-set mean F1 = 0.91; held-out F1 = 0.85; train/test gap = 0.06.

DSWX-06 acceptance gate: **gap = fit_set_mean_f1 − loocv_mean_f1 < 0.02**.

Plan 06-06 Stage 7 implements LOO-CV post-hoc on the joint best gridpoint via **leave-one-pair-out** (B1 fix; 10 folds, NOT 12 nor 5):
1. For each of the 10 (aoi, season) pairs in the fit set: refit thresholds on the remaining 9 pairs; pick the LOO-best gridpoint per fold.
2. Score the left-out pair at the per-fold refit_best.
3. Average across 10 folds = `loocv_mean_f1`.

The B1 fix corrects an earlier writing error in CONTEXT D-14 ("rotate 12 times") that conflated 12 fit-set+held-out total pairs with the actual 10 fit-set folds. Leave-one-pair-out preserves the fit-set's wet/dry seasonal signal (each fold's refit set retains the held-out AOI's other-season pair), whereas leave-one-AOI-out (5 folds) collapses both seasons of the held-out AOI together and loses that signal.

The Phase 6 v1.1 recalibration result: `loocv_gap` = {LOOCV_GAP:.4f}, {WITHIN_OR_VIOLATES} the 0.02 acceptance threshold.

If `loocv_gap >= 0.02`: Plan 06-06 Stage 8 hard-gates with `cell_status='BLOCKER'` + `loocv_gap_violation=True` + `sys.exit(2)`. Resolution paths per CONTEXT D-14 user CHECKPOINT 2: (a) expand fit-set AOI count by substituting an alternate biome AOI; (b) accept the overfit-flagged calibration with documented rationale; (c) halt Phase 6 with FAIL + named_upgrade_path='fit-set quality review'.

### 5.5 Threshold module + region selector design

Per DSWX-05 + CONTEXT D-09..D-12: recalibrated thresholds live in `src/subsideo/products/dswx_thresholds.py` as a **frozen+slots dataclass** `DSWEThresholds` carrying the 3 grid-tunable thresholds (WIGT, AWGT, PSWT2_MNDWI) plus 9 provenance fields (grid_search_run_date, fit_set_hash, fit_set_mean_f1, held_out_balaton_f1, loocv_mean_f1, loocv_gap, notebook_path, results_json_path, provenance_note). Two singleton instances:

- `THRESHOLDS_NAM`: PROTEUS defaults (WIGT=0.124, AWGT=0.0, PSWT2_MNDWI=−0.5) with sentinel provenance values (`grid_search_run_date='1996-01-01-PROTEUS-baseline'`, `fit_set_hash='n/a'`, F1 fields = NaN).
- `THRESHOLDS_EU`: Phase 6 grid-search output (overwritten by Plan 06-06 Stage 10 via W1 fail-loud sentinel-anchor slicing) with full provenance.

Region selection at `run_dswx` call time:
```python
# subsideo/config.py
class Settings(BaseSettings):
    dswx_region: Literal["nam", "eu"] = "nam"  # SUBSIDEO_DSWX_REGION env-var

# subsideo/products/types.py
@dataclass
class DSWxConfig:
    region: Literal["nam", "eu"] | None = None  # config-time override

# subsideo/products/dswx.py
def run_dswx(config: DSWxConfig) -> DSWxResult:
    settings = Settings()
    region = config.region or settings.dswx_region  # config > env > default 'nam'
    thresholds = THRESHOLDS_BY_REGION[region]
    diagnostic = _compute_diagnostic_tests(..., thresholds=thresholds)
```

YAML / runtime file I/O is explicitly forbidden per DSWX-05 (REQUIREMENTS.md): provenance flows as dataclass attributes, grep-discoverable, immutable (slots=True), git-diff-visible at PR review time.

The decomposition of `_compute_diagnostic_tests` into public `compute_index_bands` (band-driven, threshold-free) + `score_water_class_from_indices` (threshold-driven, takes pre-computed IndexBands) per CONTEXT D-05 is the architectural enabler for Plan 06-06's 8400-gridpoint grid search to run in ~25-45 min realistic on M3 Max instead of ~6 hr — the index bands are computed ONCE per (AOI, scene) and cached as float32 .npy; the inner threshold loop scores the cached arrays via `score_water_class_from_indices` 8400 times per pair.

W5 fix (Plan 06-07 Stage 0): `run_eval_dswx.py` asserts `THRESHOLDS_EU.fit_set_hash != ""` immediately after `_mp` + `credential_preflight`, BEFORE any pipeline work. Catches the Plan-06-06-not-yet-landed case loudly rather than silently producing a Balaton F1 against PROTEUS placeholders (which would be indistinguishable from the v1.0 baseline F1=0.7957 — exactly the regression we are guarding against).
```

Replace `{...}` placeholders with actual values from `eval-dswx/metrics.json` + `scripts/recalibrate_dswe_thresholds_results.json` + the Plan 06-01 PROTEUS ATBD probe artifact resolution path.

**Step 3.** Verify §5 was appended without editing §1-§4:

```bash
grep -E "^## [12345]\." docs/validation_methodology.md
```

Expected output: 5 lines in order:
```
## 1. CSLC cross-version phase impossibility
## 2. Product-quality vs reference-agreement distinction
## 3. Multilook ADR
## 4. DIST cross-resolution methodology
## 5. DSWE F1 ceiling, held-out Balaton, and threshold-module design
```

Verify ZERO edits to existing §1-§4 via git diff:

```bash
git diff docs/validation_methodology.md | grep -E "^-## [1-4]\." | wc -l
```

Should output 0.

**Step 4.** Verify the §5 sub-sections all present + W4 fix citations:

```bash
grep -E "^### 5\." docs/validation_methodology.md
grep -q "Pekel et al. 2016" docs/validation_methodology.md  # W4 fix
grep -q "Nature 540" docs/validation_methodology.md          # W4 fix
grep -q "PITFALLS.*P5\.2\|PITFALLS §P5.2\|PITFALLS P5\.2" docs/validation_methodology.md  # W4 fix
```

Expected:
```
### 5.1 DSWE F1 ceiling citation chain
### 5.2 Held-out Balaton vs fit-set methodology
### 5.3 Shoreline 1-pixel buffer rationale
### 5.4 LOO-CV overfit detection (gap < 0.02; B1 10-fold leave-one-pair-out)
### 5.5 Threshold module + region selector design
```
  </action>
  <verify>
    <automated>test -f docs/validation_methodology.md && grep -q "## 5\. DSWE F1 ceiling" docs/validation_methodology.md && test "$(grep -cE '^### 5\.[1-5]' docs/validation_methodology.md)" -eq 5 && grep -q "### 5.1" docs/validation_methodology.md && grep -q "### 5.2" docs/validation_methodology.md && grep -q "### 5.3" docs/validation_methodology.md && grep -q "### 5.4" docs/validation_methodology.md && grep -q "### 5.5" docs/validation_methodology.md && test "$(git diff docs/validation_methodology.md | grep -E '^-## [1-4]\.' | wc -l)" -eq 0 && grep -q "PITFALLS" docs/validation_methodology.md && grep -q "BOOTSTRAP" docs/validation_methodology.md && grep -q "Pekel et al. 2016" docs/validation_methodology.md && grep -q "Nature 540" docs/validation_methodology.md && grep -qE "PITFALLS.*P5\.2|PITFALLS §P5\.2|PITFALLS P5\.2" docs/validation_methodology.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/validation_methodology.md` contains literal heading `## 5. DSWE F1 ceiling, held-out Balaton, and threshold-module design`
    - Contains exactly 5 third-level §5 sub-headings: `### 5.1` through `### 5.5`
    - §5.1 references the PROTEUS ATBD probe path (a/c/d) per Plan 06-01 outcome
    - §5.2 cites `BOOTSTRAP §5.4` and `PITFALLS P5.1`
    - §5.3 (W4 fix) cites `Pekel et al. 2016` AND `Nature 540` AND `PITFALLS §P5.2` (or `PITFALLS P5.2`)
    - §5.4 cites `DSWX-06` and `PITFALLS P5.1`; references B1 10-fold leave-one-pair-out
    - §5.5 cites `DSWX-05` and references the dswx_thresholds.py + DSWxConfig.region + Settings.dswx_region precedence; references W1 sentinel-anchor slicing + W5 Stage 0 pre-check
    - ZERO edits to existing §1-§4 (verified via git diff)
  </acceptance_criteria>
  <done>docs/validation_methodology.md §5 appended with 5 sub-sections; W4 fix Pekel 2016 + PITFALLS P5.2 citation present in §5.3; existing §1-§4 unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate matrix.md and verify dswx:eu cell renders correctly</name>
  <files>results/matrix.md</files>
  <read_first>
    - eval-dswx/metrics.json (Task 1 output)
    - eval-dswx_nam/metrics.json (Plan 06-05 output)
    - results/matrix_manifest.yml (verifies dswx:nam + dswx:eu wired per CONTEXT D-30)
  </read_first>
  <action>
**Step 1.** Run the matrix writer to regenerate `results/matrix.md`:

```bash
make matrix
```

**Step 2.** Inspect the dswx rows:

```bash
grep -A 1 "dswx" results/matrix.md
```

Expected: two rows showing:
- `dswx:nam` cell with Plan 06-05 N.Am. positive control F1 + verdict + named_upgrade_path inline + investigation flag inline if regression
- `dswx:eu` cell with Plan 06-07 EU re-run held-out Balaton F1 + verdict + named_upgrade_path inline + LOO-CV gap inline

**Step 3.** Verify all 10 cells in the 5-products × 2-regions matrix are populated (REL-01 + REL-05 close at Phase 7 — but Phase 6 leaves dswx:nam + dswx:eu populated):

```bash
grep -E "(rtc|cslc|disp|dist|dswx):(nam|eu)" results/matrix.md | wc -l
```

Should output 10 (5 products × 2 regions). Empty cells (n/a) indicate Phase 7 work is needed; Phase 6 only owns dswx:* rows.
  </action>
  <verify>
    <automated>test -f results/matrix.md && grep -q "dswx:nam" results/matrix.md && grep -q "dswx:eu" results/matrix.md && grep -qE "F1 *= *0\.[0-9]+" results/matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - File `results/matrix.md` exists and has been regenerated (mtime updated)
    - Contains both `dswx:nam` and `dswx:eu` row labels
    - At least one cell shows an F1 value (e.g., `F1=0.XX PASS` or `F1=0.XX FAIL`)
  </acceptance_criteria>
  <done>matrix.md regenerated with both DSWx cells populated.</done>
</task>

</tasks>

<verification>
- run_eval_dswx.py modified per CONTEXT D-26 5 changes + iteration-1 fixes (B2 single-return + W5 Stage 0 fit_set_hash assertion); EU re-run executed
- eval-dswx/metrics.json validates as DswxEUCellMetrics with held-out Balaton F1 + thresholds_used + per_aoi_breakdown + 10-entry loocv_per_fold (B1)
- CONCLUSIONS_DSWX.md has v1.0 baseline preamble + 3 v1.1 sections
- docs/validation_methodology.md §5 appended with 5 sub-sections; §5.3 W4 cites Pekel 2016 + PITFALLS P5.2; ZERO edits to existing §1-§4
- results/matrix.md regenerated; dswx:nam + dswx:eu cells populated
- ruff clean on run_eval_dswx.py
</verification>

<success_criteria>
- DSWX-06 acceptance: EU re-run with recalibrated thresholds; F1 alongside LOO-CV F1 (gap < 0.02 already enforced; 10-fold per B1); 0.85 ≤ F1 < 0.90 = FAIL with named ML-replacement upgrade path
- DSWX-07 acceptance: F1 ceiling claim ground-referenced (PROTEUS ATBD per Plan 06-01) or own-data-bound fallback documented in §5.1
- Phase 6 closes the 5/5 product validation matrix on the DSWx row (modulo Phase 7 results-matrix close)
- D-21 v1.0-baseline-preamble pattern + v1.1-section-append pattern honored
- D-22 §5 5 sub-sections appended; structural-argument-leads order honored (§5.1 ceiling → §5.2-§5.4 evidence → §5.5 design); W4 fix integrates Pekel 2016 + PITFALLS P5.2 in §5.3
- Iteration 1 fixes integrated: B2 (single-return + .diagnostics attribute), W4 (Pekel/PITFALLS citation), W5 (Stage 0 fit_set_hash pre-check)
</success_criteria>

<output>
After completion, create `.planning/phases/06-dswx-s2-n-am-eu-recalibration/06-07-SUMMARY.md` documenting:
- run_eval_dswx.py 5 changes applied + iteration-1 fixes (W5 Stage 0 pre-check + B2 attribute access)
- Held-out Balaton F1 final value + matrix-cell verdict + named_upgrade_path
- LOO-CV gap stamped in DswxEUCellMetrics (10-fold per B1 fix)
- CONCLUSIONS_DSWX.md v1.0 baseline preamble + 3 v1.1 sections appended
- docs/validation_methodology.md §5 appended (5.1 ceiling citation chain — path a/c/d outcome; 5.2 held-out methodology; 5.3 shoreline buffer with W4 Pekel 2016 + PITFALLS P5.2 citation; 5.4 LOO-CV gap with B1 10-fold note; 5.5 module design with W1 sentinel anchors + W5 Stage 0 pre-check note)
- matrix.md regenerated
- Iteration 1 fixes integrated: B2 (compare_dswx single return + .diagnostics), W4 (Pekel/PITFALLS citation), W5 (fit_set_hash assertion)
- Phase 6 close complete
</output>
</content>
