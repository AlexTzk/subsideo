---
phase: 08-cslc-gate-promotion-and-aoi-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/subsideo/validation/stable_terrain.py
  - tests/unit/test_stable_terrain.py
  - run_eval_cslc_selfconsist_nam.py
  - run_eval_cslc_selfconsist_eu.py
autonomous: true
requirements:
  - CSLC-08
  - CSLC-07

must_haves:
  truths:
    - "Covers CONTEXT decisions D-01, D-02, and D-03"
    - "stable_terrain.py reprojects GeoSeries geometries with CRS into the raster CRS before metre buffering"
    - "existing raw shapely/BaseGeometry tests still pass because raw geometries remain assumed in raster CRS"
    - "tests cover projected-metre buffer behavior across EPSG:32611 and EPSG:32630"
    - "CSLC eval mask metadata includes retention metrics for SoCal, Mojave/Coso-Searles, and Iberian"
  artifacts:
    - path: "src/subsideo/validation/stable_terrain.py"
      provides: "Projected-metre stable terrain buffer implementation"
      contains: "to_crs"
    - path: "tests/unit/test_stable_terrain.py"
      provides: "Multi-UTM projected buffer regression tests"
      contains: "EPSG:32611"
    - path: "run_eval_cslc_selfconsist_nam.py"
      provides: "N.Am. stable-mask retention diagnostics"
      contains: "stable_mask_retention_pct"
    - path: "run_eval_cslc_selfconsist_eu.py"
      provides: "EU stable-mask retention diagnostics"
      contains: "stable_mask_retention_pct"
---

<objective>
Fix stable-terrain coast/water buffers so GeoSeries geometries are projected into the raster CRS before metre buffering, then add retention diagnostics to the CSLC eval scripts.

Purpose: CSLC-08 and CSLC-07. The Phase 8 threshold proposal must be grounded in masks whose coast/water exclusions are geometrically correct and whose retention changes are auditable.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-CONTEXT.md
@.planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-RESEARCH.md
@.planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-PATTERNS.md

Key locked decisions:
- Preserve defaults: `coast_buffer_m=5000.0`, `water_buffer_m=500.0`, `slope_max_deg=10.0`.
- Fix projected-metre behavior and emit diagnostics.
- Visual artifacts remain optional unless diagnostics are surprising.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproject GeoSeries geometries before buffering</name>
  <files>src/subsideo/validation/stable_terrain.py, tests/unit/test_stable_terrain.py</files>
  <read_first>
    - src/subsideo/validation/stable_terrain.py
    - tests/unit/test_stable_terrain.py
    - .planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-PATTERNS.md
  </read_first>
  <action>
Update `src/subsideo/validation/stable_terrain.py` inside `_buffered_geometry_mask()`:

1. Preserve current raw shapely `BaseGeometry` behavior: construct `gpd.GeoSeries([geometry], crs=crs)` so existing tests using UTM-coordinate shapely geometries continue to pass.
2. For a `geopandas.GeoSeries`, preserve its own CRS by assigning `gs = geometry`.
3. For other iterables, construct `gpd.GeoSeries(geometry, crs=crs)`.
4. After `gs` is constructed, if `gs.crs` is not `None` and differs from the raster `crs`, call `gs = gs.to_crs(crs)` before `gs.buffer(buffer_m)`.
5. Keep lazy imports inside `_buffered_geometry_mask`; do not add module-top geopandas/rasterio imports.

Add tests to `tests/unit/test_stable_terrain.py`:

1. `test_coast_buffer_reprojects_epsg4326_geoseries_to_utm11()`:
   - Use `geopandas.GeoSeries([LineString([...lon/lat...])], crs="EPSG:4326")`.
   - Use raster CRS `CRS.from_epsg(32611)` and a UTM transform around SoCal-like coordinates.
   - Assert the buffer excludes some pixels and leaves far pixels stable.
2. `test_waterbody_buffer_reprojects_epsg4326_geoseries_to_utm30()`:
   - Use `geopandas.GeoSeries([Polygon([...lon/lat...])], crs="EPSG:4326")`.
   - Use raster CRS `CRS.from_epsg(32630)` and a UTM transform around Iberian-like coordinates.
   - Assert larger `water_buffer_m` excludes at least as many pixels as default.

Use `pytest.importorskip()` for `geopandas`, `affine`, `pyproj`, `shapely`, and `rasterio` as needed. Keep tests offline.
  </action>
  <verify>
    <automated>pytest tests/unit/test_stable_terrain.py -q</automated>
    <automated>grep -c "to_crs" src/subsideo/validation/stable_terrain.py</automated>
    <automated>grep -c "EPSG:32611" tests/unit/test_stable_terrain.py</automated>
    <automated>grep -c "EPSG:32630" tests/unit/test_stable_terrain.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_stable_terrain.py -q` exits 0
    - `grep -c "to_crs" src/subsideo/validation/stable_terrain.py` returns at least 1
    - `grep -c "EPSG:32611" tests/unit/test_stable_terrain.py` returns at least 1
    - `grep -c "EPSG:32630" tests/unit/test_stable_terrain.py` returns at least 1
  </acceptance_criteria>
  <done>Stable-terrain buffers reproject CRS-carrying geometries before metre buffering and tests prove two UTM zones.</done>
</task>

<task type="auto">
  <name>Task 2: Add stable-mask retention diagnostics to CSLC eval scripts</name>
  <files>run_eval_cslc_selfconsist_nam.py, run_eval_cslc_selfconsist_eu.py</files>
  <read_first>
    - run_eval_cslc_selfconsist_nam.py
    - run_eval_cslc_selfconsist_eu.py
    - src/subsideo/validation/matrix_schema.py
    - CONCLUSIONS_CSLC_SELFCONSIST_NAM.md
    - CONCLUSIONS_CSLC_SELFCONSIST_EU.md
  </read_first>
  <action>
Add deterministic stable-mask retention diagnostics to both CSLC self-consistency eval scripts where `worldcover`, `slope`, `coast`, `water`, and final `stable_mask` are all in scope.

Compute and persist these exact keys per AOI, either in existing `mask_metadata.json` outputs or in the per-AOI product-quality measurements if that is the current local pattern:

```python
stable_mask_class60_count = int((worldcover == 60).sum())
stable_mask_slope_ok_count = int(np.isfinite(slope_deg).sum() if exact slope mask is not exposed else slope_ok_count)
stable_mask_coast_excluded_count = int(coast_excluded_count)
stable_mask_water_excluded_count = int(water_excluded_count)
stable_mask_final_count = int(stable_mask.sum())
stable_mask_retention_pct = float(stable_mask_final_count / max(stable_mask_class60_count, 1))
stable_mask_buffer_crs = str(<raster_crs_or_epsg>)
```

If `coast_excluded_count` / `water_excluded_count` are not directly exposed by `build_stable_mask`, derive them in-script by calling the same buffered-mask helper only if it is already public or by computing before/after counts around separate coast/water calls. Do not change the public `build_stable_mask()` return type in this plan.

Ensure SoCal, Mojave/Coso-Searles, and Iberian all get these diagnostics on warm paths as well as fresh runs.
  </action>
  <verify>
    <automated>python3 -m py_compile run_eval_cslc_selfconsist_nam.py run_eval_cslc_selfconsist_eu.py</automated>
    <automated>grep -c "stable_mask_retention_pct" run_eval_cslc_selfconsist_nam.py</automated>
    <automated>grep -c "stable_mask_retention_pct" run_eval_cslc_selfconsist_eu.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile run_eval_cslc_selfconsist_nam.py run_eval_cslc_selfconsist_eu.py` exits 0
    - `grep -c "stable_mask_retention_pct" run_eval_cslc_selfconsist_nam.py` returns at least 1
    - `grep -c "stable_mask_retention_pct" run_eval_cslc_selfconsist_eu.py` returns at least 1
    - Both scripts contain `stable_mask_final_count`
    - Both scripts contain `stable_mask_buffer_crs`
  </acceptance_criteria>
  <done>CSLC eval scripts emit stable-mask retention diagnostics suitable for the Phase 8 threshold rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Geospatial geometry inputs | Natural Earth or probe-derived geometries may arrive as EPSG:4326 while raster grids are projected |
| Metrics sidecars | Retention diagnostics are used to justify threshold proposals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | Geometry CRS metadata | mitigate | Honor `GeoSeries.crs` and reproject to raster CRS before buffering; add multi-UTM tests. |
| T-08-01-02 | Information Disclosure | Mask metadata | accept | Diagnostics contain counts/CRS only, no credentials or private paths beyond existing eval artifacts. |
| T-08-01-03 | Denial of Service | GeoSeries reprojection | accept | Reprojection happens on small coastline/water geometry collections already used by eval scripts. |
</threat_model>

<verification>
Run:

```bash
pytest tests/unit/test_stable_terrain.py -q
python3 -m py_compile run_eval_cslc_selfconsist_nam.py run_eval_cslc_selfconsist_eu.py
grep -c "stable_mask_retention_pct" run_eval_cslc_selfconsist_nam.py
grep -c "stable_mask_retention_pct" run_eval_cslc_selfconsist_eu.py
```
</verification>

<success_criteria>
- GeoSeries coast/water geometries with CRS are reprojected before `.buffer(buffer_m)`.
- Stable-terrain tests cover EPSG:32611 and EPSG:32630.
- Existing shapely/BaseGeometry buffer tests still pass.
- CSLC eval scripts expose retention diagnostics for threshold evidence.
</success_criteria>
