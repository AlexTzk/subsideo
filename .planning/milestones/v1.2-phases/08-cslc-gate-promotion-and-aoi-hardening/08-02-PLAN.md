---
phase: 08-cslc-gate-promotion-and-aoi-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/probe_cslc_aoi_candidates.py
  - .planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md
  - run_eval_cslc_selfconsist_nam.py
  - run_eval_cslc_selfconsist_eu.py
  - tests/unit/test_run_eval_cslc_selfconsist_eu.py
  - tests/unit/test_probe_cslc_aoi_candidates.py
autonomous: true
requirements:
  - CSLC-09
  - CSLC-12

must_haves:
  truths:
    - "Covers CONTEXT decisions D-04, D-05, D-06, D-14, and D-15"
    - "Canonical v1.2 AOI probe artifact contains no SYNTHETIC FALLBACK rows"
    - "Probe artifact covers SoCal, four Mojave fallbacks, Iberian Meseta-North, and at least two EU fallback AOIs"
    - "Old v1.1 invalid Alentejo/Massif Central rows are replaced or explicitly rejected with evidence"
    - "Stale EU fallback tests are updated, relaxed, or removed with rationale based on regenerated artifact behavior"
  artifacts:
    - path: ".planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md"
      provides: "Full acquisition-backed CSLC AOI probe artifact"
      contains: "Rejected Candidates"
    - path: "scripts/probe_cslc_aoi_candidates.py"
      provides: "Regeneratable v1.2 AOI probe script"
      contains: "cslc_gate_promotion_aoi_candidates.md"
    - path: "tests/unit/test_probe_cslc_aoi_candidates.py"
      provides: "Offline structural tests for canonical probe artifact rules"
      contains: "SYNTHETIC FALLBACK"
---

<objective>
Regenerate the CSLC AOI probe path for v1.2 so Phase 9 reruns use real acquisition-backed sensing windows and validated fallbacks, then retire or reshape stale v1.1 fallback tests.

Purpose: CSLC-09 and CSLC-12. This plan replaces old fabricated/invalid probe evidence rather than patching around it.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-CONTEXT.md
@.planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-RESEARCH.md
@.planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-PATTERNS.md

Locked decisions:
- Full AOI probe regeneration, not targeted repair.
- Regenerated artifact is the new source of truth.
- Stale tests should guard real behavior, not accidental v1.1 structure.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert CSLC probe script to v1.2 acquisition-backed artifact</name>
  <files>scripts/probe_cslc_aoi_candidates.py, tests/unit/test_probe_cslc_aoi_candidates.py</files>
  <read_first>
    - scripts/probe_cslc_aoi_candidates.py
    - .planning/milestones/v1.1-research/cslc_selfconsist_aoi_candidates.md
    - CONCLUSIONS_CSLC_SELFCONSIST_NAM.md
    - CONCLUSIONS_CSLC_SELFCONSIST_EU.md
    - .planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-PATTERNS.md
  </read_first>
  <action>
Update `scripts/probe_cslc_aoi_candidates.py` for Phase 8/v1.2:

1. Change the output path to `.planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md`.
2. Remove the canonical synthetic fallback behavior for selected sensing windows. The v1.2 canonical artifact must not render `[SYNTHETIC FALLBACK]`; if live ASF/CDSE searches cannot produce enough evidence for a candidate, write that candidate under `## Rejected Candidates` with a reason instead of fabricating dates.
3. Include these AOI categories:
   - SoCal locked anchor: `t144_308029_iw1`
   - Mojave/Coso-Searles
   - Mojave/Pahranagat
   - Mojave/Amargosa
   - Mojave/Hualapai
   - Iberian Meseta-North
   - at least two EU fallback AOIs selected from regenerated evidence; do not blindly reuse the invalid v1.1 Alentejo/Massif Central rows if searches prove they are misbound.
4. Add a `## Rejected Candidates` section with columns `aoi`, `reason`, and `evidence`.
5. Add a `## Query Parameters` section listing ASF/CDSE temporal windows, platform, processing level, and bbox/burst query strategy.
6. Add a `## Selected Sensing Windows` section where every accepted AOI has 15 concrete acquisition-backed sensing timestamps and selected SAFE/acquisition identifiers.

Add `tests/unit/test_probe_cslc_aoi_candidates.py` with offline tests that inspect a sample rendered artifact or fixture string:

- Accepted artifact contains `SoCal`, `Mojave/Coso-Searles`, `Mojave/Pahranagat`, `Mojave/Amargosa`, `Mojave/Hualapai`, `Iberian Meseta-North`.
- Accepted artifact contains at least two rows whose region/category is EU fallback.
- Accepted artifact does not contain `SYNTHETIC FALLBACK`.
- Accepted artifact contains `## Rejected Candidates`.
- Accepted artifact contains `## Query Parameters`.
  </action>
  <verify>
    <automated>python3 -m py_compile scripts/probe_cslc_aoi_candidates.py</automated>
    <automated>pytest tests/unit/test_probe_cslc_aoi_candidates.py -q</automated>
    <automated>grep -c "cslc_gate_promotion_aoi_candidates.md" scripts/probe_cslc_aoi_candidates.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile scripts/probe_cslc_aoi_candidates.py` exits 0
    - `pytest tests/unit/test_probe_cslc_aoi_candidates.py -q` exits 0
    - `grep -c "cslc_gate_promotion_aoi_candidates.md" scripts/probe_cslc_aoi_candidates.py` returns at least 1
    - `grep -c "SYNTHETIC FALLBACK" tests/unit/test_probe_cslc_aoi_candidates.py` returns at least 1 as a negative assertion
  </acceptance_criteria>
  <done>Probe script writes the v1.2 artifact shape and offline tests pin artifact invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Run probe and commit canonical v1.2 artifact</name>
  <files>.planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md</files>
  <read_first>
    - scripts/probe_cslc_aoi_candidates.py
    - .env (only to confirm required credentials are present; do not print secrets)
    - .planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-CONTEXT.md
  </read_first>
  <action>
Run the regenerated probe with the project environment:

```bash
micromamba run -n subsideo python scripts/probe_cslc_aoi_candidates.py
```

If credentials or network access are unavailable, stop this task and record the blocker in the plan SUMMARY. Do not write a fake canonical artifact.

After the probe writes `.planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md`, inspect it manually and verify:

- No `SYNTHETIC FALLBACK` text appears.
- SoCal is present.
- Four Mojave fallback AOIs are present or rejected with evidence.
- Iberian Meseta-North is present.
- At least two EU fallback AOIs are accepted with 15 timestamps each.
- Invalid v1.1 Alentejo/Massif Central rows are either replaced with valid AOIs or listed under rejected candidates with evidence.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md</automated>
    <automated>grep -c "SYNTHETIC FALLBACK" .planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md</automated>
    <automated>grep -c "Rejected Candidates" .planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md` exists
    - `grep -c "SYNTHETIC FALLBACK" .planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md` returns 0
    - `grep -c "Rejected Candidates" .planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md` returns at least 1
    - The artifact contains at least two EU fallback AOI accepted windows
  </acceptance_criteria>
  <done>Canonical v1.2 CSLC AOI artifact exists and is acquisition-backed.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh eval-script fallback references and stale tests</name>
  <files>run_eval_cslc_selfconsist_nam.py, run_eval_cslc_selfconsist_eu.py, tests/unit/test_run_eval_cslc_selfconsist_eu.py</files>
  <read_first>
    - .planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md
    - run_eval_cslc_selfconsist_nam.py
    - run_eval_cslc_selfconsist_eu.py
    - tests/unit/test_run_eval_cslc_selfconsist_eu.py
    - CONCLUSIONS_CSLC_SELFCONSIST_EU.md
  </read_first>
  <action>
Use the regenerated v1.2 artifact to refresh script comments, fallback constants, and stale tests:

1. In `run_eval_cslc_selfconsist_nam.py`, update comments and any hard-coded probe references from v1.1 to `.planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md`.
2. In `run_eval_cslc_selfconsist_eu.py`, replace invalid stale fallback references only if the regenerated artifact provides valid replacements. If Phase 8 keeps Iberian primary only, remove stale assertions that demand `_IBERIAN_FALLBACKS` and document the rationale in the test file comment.
3. In `tests/unit/test_run_eval_cslc_selfconsist_eu.py`, implement the policy from CONTEXT D-14/D-15:
   - keep behavioral checks for artifact-backed fallback semantics;
   - allow the old `test_iberian_aoi_fallback_chain_two_entries` to be updated, relaxed, or removed;
   - keep `test_env07_diff_discipline` only if it still protects useful script parity. If it is retained, its skip/fail threshold must describe the intended invariant.
4. Add an assertion that the canonical v1.2 artifact path is referenced by at least one CSLC eval script or test.
  </action>
  <verify>
    <automated>pytest tests/unit/test_run_eval_cslc_selfconsist_eu.py -q</automated>
    <automated>python3 -m py_compile run_eval_cslc_selfconsist_nam.py run_eval_cslc_selfconsist_eu.py</automated>
    <automated>grep -R "cslc_gate_promotion_aoi_candidates.md" run_eval_cslc_selfconsist_nam.py run_eval_cslc_selfconsist_eu.py tests/unit/test_run_eval_cslc_selfconsist_eu.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_run_eval_cslc_selfconsist_eu.py -q` exits 0
    - `python3 -m py_compile run_eval_cslc_selfconsist_nam.py run_eval_cslc_selfconsist_eu.py` exits 0
    - `grep -R "cslc_gate_promotion_aoi_candidates.md" run_eval_cslc_selfconsist_nam.py run_eval_cslc_selfconsist_eu.py tests/unit/test_run_eval_cslc_selfconsist_eu.py` returns at least one match
    - No test requires invalid v1.1 Alentejo/Massif Central burst IDs to remain wired
  </acceptance_criteria>
  <done>Stale AOI/fallback tests now reflect regenerated artifact behavior instead of accidental v1.1 structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ASF/CDSE live search | External APIs provide acquisition metadata used in planning and eval scripts |
| Probe artifact | Markdown artifact becomes source of truth for expensive Phase 9 reruns |
| `.env` credentials | Probe needs Earthdata/CDSE credentials but must not print or commit secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Spoofing | Acquisition metadata | mitigate | Record query parameters, selected acquisition IDs, and rejected candidates in artifact. |
| T-08-02-02 | Tampering | Probe artifact | mitigate | Add offline structural tests proving required sections and no synthetic fallback rows. |
| T-08-02-03 | Information Disclosure | Credentials | mitigate | Read `.env` only for presence; never print credential values; artifact contains no tokens. |
| T-08-02-04 | Denial of Service | Live probe | accept | Probe is manual and bounded; unit tests remain offline. |
</threat_model>

<verification>
Run:

```bash
python3 -m py_compile scripts/probe_cslc_aoi_candidates.py
pytest tests/unit/test_probe_cslc_aoi_candidates.py -q
micromamba run -n subsideo python scripts/probe_cslc_aoi_candidates.py
pytest tests/unit/test_run_eval_cslc_selfconsist_eu.py -q
python3 -m py_compile run_eval_cslc_selfconsist_nam.py run_eval_cslc_selfconsist_eu.py
```
</verification>

<success_criteria>
- Canonical v1.2 AOI probe artifact is acquisition-backed and contains no synthetic fallback rows.
- Existing SoCal/Mojave/Iberian evidence is regenerated or explicitly rejected with reasons.
- At least two EU fallback AOIs are validated.
- Stale tests are fixed, relaxed, or removed according to behavioral value.
</success_criteria>
