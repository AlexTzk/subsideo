---
phase: 08-cslc-gate-promotion-and-aoi-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - .planning/REQUIREMENTS.md
  - docs/validation_methodology.md
  - CONCLUSIONS_CSLC_SELFCONSIST_NAM.md
  - CONCLUSIONS_CSLC_SELFCONSIST_EU.md
  - tests/unit/test_run_eval_cslc_selfconsist_eu.py
autonomous: true
requirements:
  - CSLC-07
  - CSLC-12
  - RTCSUP-01
  - RTCSUP-03

must_haves:
  truths:
    - "Covers CONTEXT decisions D-10, D-11, D-12, D-14, and D-15"
    - ".planning/REQUIREMENTS.md records the proposed CSLC BINDING threshold rationale: coherence >= 0.75 and residual <= 2.0 mm/yr"
    - "Threshold rationale cites SoCal 0.887/-0.109, Mojave 0.804/+1.127, and Iberian 0.868/+0.347"
    - "Phase 8 closure tests include stable terrain, harness, compare_disp, probe artifact, and stale EU CSLC tests"
    - "Stale v1.1 tests are either passing with useful behavior or removed/relaxed with documented rationale"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CSLC-07 Phase 8 proposed BINDING threshold rationale"
      contains: "coherence >= 0.75"
    - path: "docs/validation_methodology.md"
      provides: "Methodology note for CSLC v1.2 binding proposal"
      contains: "CSLC v1.2 binding proposal"
    - path: "CONCLUSIONS_CSLC_SELFCONSIST_NAM.md"
      provides: "N.Am. conclusion note for Phase 8 diagnostics"
      contains: "Phase 8"
    - path: "CONCLUSIONS_CSLC_SELFCONSIST_EU.md"
      provides: "EU conclusion note for Phase 8 diagnostics"
      contains: "Phase 8"
---

<objective>
Document the conservative CSLC BINDING threshold proposal and close Phase 8 with a focused regression suite proving the buffer, probe, SAFE cache, shared-infra, and stale-test work.

Purpose: CSLC-07, CSLC-12, RTCSUP-01, and RTCSUP-03. This plan turns the technical fixes from Plans 01-03 into a readable Phase 8 science/validation handoff for Phase 9.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-CONTEXT.md
@.planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-RESEARCH.md
@.planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-PATTERNS.md
@.planning/REQUIREMENTS.md
@docs/validation_methodology.md

Locked threshold proposal:
- `median_of_persistent >= 0.75`
- stable-terrain residual `<= 2.0 mm/yr`
- Rationale values: SoCal `0.887` / `-0.109 mm/yr`; Mojave/Coso-Searles `0.804` / `+1.127 mm/yr`; Iberian `0.868` / `+0.347 mm/yr`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSLC v1.2 binding proposal rationale to requirements/methodology docs</name>
  <files>.planning/REQUIREMENTS.md, docs/validation_methodology.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - docs/validation_methodology.md
    - .planning/milestones/v1.2-phases/08-cslc-gate-promotion-and-aoi-hardening/08-CONTEXT.md
    - CONCLUSIONS_CSLC_SELFCONSIST_NAM.md
    - CONCLUSIONS_CSLC_SELFCONSIST_EU.md
  </read_first>
  <action>
Add a concise Phase 8 threshold proposal note in `.planning/REQUIREMENTS.md` near requirement `CSLC-07` or in the CSLC-S1 requirements section. The text must include these exact values:

```markdown
Phase 8 proposed BINDING threshold rationale: use `median_of_persistent >= 0.75` and stable-terrain residual `<= 2.0 mm/yr` for the Phase 9 promotion candidate. This is conservative relative to the v1.1 calibration points: SoCal coherence `0.887` / residual `-0.109 mm/yr`, Mojave/Coso-Searles coherence `0.804` / residual `+1.127 mm/yr`, and Iberian Meseta-North coherence `0.868` / residual `+0.347 mm/yr`. Phase 9 reruns may confirm, narrow, or block promotion based on regenerated AOI and stable-mask diagnostics; Phase 8 does not silently move the gate.
```

Append a short subsection to `docs/validation_methodology.md` under the CSLC/product-quality area, or add a new clearly named subsection if that is cleaner:

```markdown
### CSLC v1.2 binding proposal (Phase 8)
```

The subsection must explain:

- gate metric remains `median_of_persistent`;
- proposed coherence threshold is `>= 0.75`;
- proposed residual threshold is `<= 2.0 mm/yr`;
- Phase 9 performs final reruns/promotion;
- product-quality and reference-agreement remain separate.
  </action>
  <verify>
    <automated>grep -c "coherence >= 0.75" .planning/REQUIREMENTS.md</automated>
    <automated>grep -c "residual <= 2.0 mm/yr" .planning/REQUIREMENTS.md</automated>
    <automated>grep -c "CSLC v1.2 binding proposal" docs/validation_methodology.md</automated>
    <automated>grep -c "median_of_persistent" docs/validation_methodology.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "coherence >= 0.75" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "residual <= 2.0 mm/yr" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "0.887" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "0.804" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "0.868" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "CSLC v1.2 binding proposal" docs/validation_methodology.md` returns at least 1
  </acceptance_criteria>
  <done>Requirements and methodology docs contain the conservative Phase 8 binding proposal and rationale values.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 8 diagnostic notes to CSLC conclusions</name>
  <files>CONCLUSIONS_CSLC_SELFCONSIST_NAM.md, CONCLUSIONS_CSLC_SELFCONSIST_EU.md</files>
  <read_first>
    - CONCLUSIONS_CSLC_SELFCONSIST_NAM.md
    - CONCLUSIONS_CSLC_SELFCONSIST_EU.md
    - .planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Append a Phase 8 note to both CSLC conclusions files.

For `CONCLUSIONS_CSLC_SELFCONSIST_NAM.md`, add a subsection:

```markdown
## Phase 8 v1.2 input-hardening note
```

It must state:

- stable-terrain buffers now reproject CRS-carrying geometries before metre buffering;
- SoCal and Mojave retention diagnostics are available in mask metadata or metrics sidecars;
- canonical AOI windows now come from `.planning/milestones/v1.2-research/cslc_gate_promotion_aoi_candidates.md`;
- proposed Phase 9 binding candidate is `median_of_persistent >= 0.75` and residual `<= 2.0 mm/yr`.

For `CONCLUSIONS_CSLC_SELFCONSIST_EU.md`, add the same subsection but mention Iberian Meseta-North and the two regenerated EU fallback AOIs from the v1.2 artifact. If the artifact rejected old Alentejo/Massif Central, name that rejection and point to the artifact's `Rejected Candidates` section.
  </action>
  <verify>
    <automated>grep -c "Phase 8 v1.2 input-hardening note" CONCLUSIONS_CSLC_SELFCONSIST_NAM.md</automated>
    <automated>grep -c "Phase 8 v1.2 input-hardening note" CONCLUSIONS_CSLC_SELFCONSIST_EU.md</automated>
    <automated>grep -c "cslc_gate_promotion_aoi_candidates.md" CONCLUSIONS_CSLC_SELFCONSIST_NAM.md</automated>
    <automated>grep -c "cslc_gate_promotion_aoi_candidates.md" CONCLUSIONS_CSLC_SELFCONSIST_EU.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 8 v1.2 input-hardening note" CONCLUSIONS_CSLC_SELFCONSIST_NAM.md` returns 1
    - `grep -c "Phase 8 v1.2 input-hardening note" CONCLUSIONS_CSLC_SELFCONSIST_EU.md` returns 1
    - Both conclusions files reference `cslc_gate_promotion_aoi_candidates.md`
    - Both conclusions files reference `median_of_persistent >= 0.75`
    - Both conclusions files reference `residual <= 2.0 mm/yr`
  </acceptance_criteria>
  <done>CSLC conclusions explain the Phase 8 input-hardening handoff for Phase 9.</done>
</task>

<task type="auto">
  <name>Task 3: Final Phase 8 closure test suite</name>
  <files>tests/unit/test_run_eval_cslc_selfconsist_eu.py</files>
  <read_first>
    - tests/unit/test_stable_terrain.py
    - tests/unit/test_harness.py
    - tests/unit/test_compare_disp.py
    - tests/unit/test_probe_cslc_aoi_candidates.py
    - tests/unit/test_run_eval_cslc_selfconsist_eu.py
  </read_first>
  <action>
Run the focused Phase 8 regression suite:

```bash
pytest tests/unit/test_stable_terrain.py tests/unit/test_harness.py tests/unit/test_compare_disp.py tests/unit/test_probe_cslc_aoi_candidates.py tests/unit/test_run_eval_cslc_selfconsist_eu.py -q
```

Then run the broader unit suite:

```bash
pytest tests/unit/ -x -q --tb=short
```

If stale v1.1 tests still fail:

- For `test_env07_diff_discipline`, keep it only if the failure identifies real unwanted divergence after Phase 8. Otherwise skip/relax with a comment naming Phase 8 and the regenerated artifact.
- For `test_iberian_aoi_fallback_chain_two_entries`, update it to assert regenerated artifact-backed behavior or remove it with a nearby rationale comment/doc note. Do not force invalid v1.1 fallback shape.
  </action>
  <verify>
    <automated>pytest tests/unit/test_stable_terrain.py tests/unit/test_harness.py tests/unit/test_compare_disp.py tests/unit/test_probe_cslc_aoi_candidates.py tests/unit/test_run_eval_cslc_selfconsist_eu.py -q</automated>
    <automated>pytest tests/unit/ -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 8 pytest command exits 0
    - `pytest tests/unit/ -x -q --tb=short` exits 0
    - Any remaining skip in `test_env07_diff_discipline` names intentional script divergence or regenerated artifact policy
    - No unit test requires invalid v1.1 EU fallback burst IDs
  </acceptance_criteria>
  <done>Focused and broad unit tests pass; stale tests are intentionally resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning requirements docs | Human-readable threshold rationale guides Phase 9 promotion |
| Conclusions docs | External contributors may use these notes to interpret rerun outcomes |
| Test suite | Confirms Phase 8 did not leave stale tests or shared-infra regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | Threshold rationale | mitigate | Include exact values and source v1.1 metrics in requirements/methodology docs. |
| T-08-04-02 | Repudiation | Phase 9 handoff | mitigate | Conclusions files reference the canonical v1.2 AOI artifact and Phase 8 diagnostics. |
| T-08-04-03 | Denial of Service | Broad unit suite | accept | Unit-only suite avoids live credentials and should remain bounded. |
</threat_model>

<verification>
Run:

```bash
grep -c "coherence >= 0.75" .planning/REQUIREMENTS.md
grep -c "CSLC v1.2 binding proposal" docs/validation_methodology.md
pytest tests/unit/test_stable_terrain.py tests/unit/test_harness.py tests/unit/test_compare_disp.py tests/unit/test_probe_cslc_aoi_candidates.py tests/unit/test_run_eval_cslc_selfconsist_eu.py -q
pytest tests/unit/ -x -q --tb=short
```
</verification>

<success_criteria>
- Phase 8 threshold proposal is documented where Phase 9 agents will read it.
- Conclusions files point to the hardened inputs and regenerated AOI artifact.
- Stale tests are intentionally resolved.
- Focused and broad unit tests pass.
</success_criteria>
